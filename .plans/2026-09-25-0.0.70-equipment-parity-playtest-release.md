# 0.0.70 — Equipment parity playtest release

> **STATUS (2026-09-25): ACTIVE — Derrick explicitly instructed us to build and serve the completed equipment parity/defaults changes, and established build+serve as the default after changes so he can playtest.**

## Goal

Create exactly one append-only immutable raw `0.0.70` from the already QA/audited equipment Test→Play parity source, independently audit it, then replace the active raw `0.0.69` playtest service with raw `0.0.70` on port `5173`.

## Authorization and boundaries

- Authorized: patch bump to `0.0.70`, required version/provenance authority updates, exactly one canonical `AEROBEAT_BASE_PATH=/ npm run build-release`, raw audit, commit/push, and replacement of the existing transient local playtest service.
- Not authorized: npm publication, Git tag, GitHub Release, Funnel/public route changes, rewriting any historical raw, or claiming Derrick's physical playtest PASS.
- Preserve immutable raw `0.0.69` tree `59367ea923b7a6773f624e7e2f0673dcce3ddb5e` and every predecessor.
- Build target `release/raw/0.0.70` must be absent before the sole canonical invocation and must never be rerun, removed, or edited afterward.

## Source authority

- Source tip before release work: `a77b3b71efaf32fda9b2d3a29258cb727fcb0f04` on clean pushed `main`.
- Completed implementation plan: `.plans/2026-09-25-test-to-play-equipment-parity-and-flow-defaults.md`.
- Functional QA: focused equipment/config/orientation/freeze gates, `npm test`, and a complete uncapped `npm run test:browser` all passed.
- Independent source audit: GO; final follow-up confirmed the only stale helper was removed and `main` remained clean/upstream.

## Beads

- Umbrella: `aerobeat-web-assembly-viro`
- Build: `aerobeat-web-assembly-e0rn`
- Raw audit: `aerobeat-web-assembly-4qxf` (depends on build)
- Serve/verify: `aerobeat-web-assembly-sqz0` (depends on audit)

## Tasks

### Task 1 — Version and preflight (`e0rn`)
- [ ] Claim the Bead and verify clean/upstream source identity.
- [ ] Prove raw `0.0.70` absent and raw `0.0.69` exact.
- [ ] Run the canonical patch bump once, update required version/provenance allowlists, and commit/push.
- [ ] Run bounded release preflight gates on the bumped source.

### Task 2 — Exactly-once immutable build (`e0rn`)
- [ ] Invoke `AEROBEAT_BASE_PATH=/ npm run build-release` exactly once.
- [ ] Record proof identity, counts, byte sizes, raw tree, source fingerprint, and predecessor preservation.
- [ ] Commit/push the immutable raw and builder evidence. Do not publish or serve yet.

### Task 3 — Independent raw audit (`4qxf`)
- [ ] Independently verify source/raw provenance, proof, manifests, embedded equipment behavior, predecessor immutability, version alignment, and pushed state.
- [ ] Triage findings; only a likely normal-use or acceptance risk can block serving.
- [ ] Record GO/NO-GO and serving authorization.

### Task 4 — Serve and verify (`sqz0`)
- [ ] On audit GO, stop raw `0.0.69` and create one transient `aerobeat-raw-0070.service` rooted at exact raw `0.0.70` on port `5173`.
- [ ] Verify active service identity, loopback proof/assets, tailnet route, real Chromium mount, and console/page noise policy.
- [ ] Finalize plan, close Beads, commit/push, and leave raw `0.0.70` ready for Derrick's physical playtest.

## Completion condition

Raw `0.0.70` is immutable, independently audited, committed and pushed; `aerobeat-raw-0070.service` is active and serving its exact proof on port `5173`; raw `0.0.69` is unchanged; all release Beads and this plan are complete. Physical gameplay approval remains Derrick's call.
