# Handcrafted 3D Gameplay Visuals and Environment

**Status:** RAW `0.0.38` PHYSICALLY REJECTED — SUCCESSOR DESIGN AND ASSET AUTHORING IN PROGRESS; IMMUTABLE SUCCESSOR REQUIRED
**Owning repo:** `/home/derrick/.dsh/projects/aerobeat/aerobeat-web-assembly`
**Planning Bead:** `aerobeat-web-assembly-k72`
**Initial Task 1 chain:** coder `aerobeat-web-assembly-k72.1` → QA `aerobeat-web-assembly-k72.2` → audit `aerobeat-web-assembly-k72.3`
**Task 1 revision chain:** coder `aerobeat-web-assembly-k72.4` → QA `aerobeat-web-assembly-k72.5` → audit `aerobeat-web-assembly-k72.6`
**Task 2 asset chain:** coder `aerobeat-web-assembly-k72.8` → QA `aerobeat-web-assembly-k72.9` → audit `aerobeat-web-assembly-k72.10` → Derrick 3D review gate `aerobeat-web-assembly-k72.18`
**Task 3 renderer chain:** coder `aerobeat-web-assembly-k72.11` → QA `aerobeat-web-assembly-k72.12`
**Tasks 4–5 assembly chain:** coder `aerobeat-web-assembly-k72.13` → matrix QA `aerobeat-web-assembly-k72.14` → final audit `aerobeat-web-assembly-k72.15`
**Task 6 release chain:** release `aerobeat-web-assembly-k72.16` → physical gate `aerobeat-web-assembly-k72.17`
**Supersedes:** closed bloq/platform-conversion PoC `aerobeat-web-assembly-zd0`

## Goal

Replace the discarded block/background conversion direction with jointly authored, rights-clean 3D gameplay models, a perspective-defining track, and one locally owned test environment. Preserve PlayCanvas as the single runtime renderer and keep all assets self-contained.

## Approved visual direction to plan

- **Directional note:** DDR-style 3D arrow with a white outline treatment, shared by Flow and Boxing.
- **Any note:** circular 3D note with the same coherent visual language.
- **Guard:** one canonical shield model/icon reused identically for both hands. A guard beat displays two simultaneous instances—one in each applicable left/right grid cell or lane—so both hands see the same shield at once. “Shared” means shared geometry/icon identity, not a single displayed instance; do not distinguish the two shields with separate geometry.
- **Athlete markers:** replace flat 2D-looking nose and wrist circles with full 3D spheres placed at the truthful nose/wrist world positions. Preserve existing role colors and occlusion/depth behavior.
- **Timing rows and beat tint:** keep the near-athlete grid rows colored red/yellow/green. As an approaching beat enters the success area, tint its 3D model white to communicate timing without replacing its silhouette.
- **Hit/miss feedback:** on threshold resolution, remove the beat quickly and spawn short-lived world-space text at the beat’s crossing position and height. Successful hits show `Great` in white; misses show `Miss` in red with a white outline. Feedback must remain only briefly and must not distract or obscure later beats.
- **Wall obstacle:** semi-transparent red rectangular volume. In Flow its world-Z length must span the complete authoritative obstacle interval rather than appearing as a short temporary slab.
- **Bomb:** black sea-urchin silhouette—a sphere with sharp cone spines—with red emissive glow/outline signaling avoidance.
- **Track:** semi-transparent blue-tinted glass surface below the athlete and event lanes, extending through the visible approach path so camera perspective and beat travel are immediately legible.
- **Test environment:** use the already-owned local GLB at `/home/derrick/.dsh/projects/aerobeat/aerobeat-environment-community/.testbed/assets/models/alien-moon-icescape/alien-moon-icescape.glb` with its sibling transform config. Do not acquire or redistribute third-party assets.
- **Future environments:** locally owned Gaussian splats remain a later explicitly approved experiment, not part of this first implementation.

## Constraints

- Handcrafted/model-generation work must be reviewed visually by Derrick before runtime integration.
- Use one canonical asset per semantic role where specified; do not duplicate left/right shield geometry.
- White outlines must remain visible against the ice environment and transparent track without breaking depth ordering or alpha.
- Translucent walls and track must retain truthful depth, interval length, and gameplay visibility.
- Preserve role colors, handedness, timing, scoring, event IDs, camera conventions, privacy, and public contracts.
- No Theme runtime, network asset fetch, public release, upload, third-party acquisition, or Gaussian-splat integration in this slice.

## Approved future asset ownership and packaging direction

- Keep the canonical gameplay art in its own cross-engine polyrepo named `aerobeat-asset-gameplay`, following the separation used by the Godot asset lane. Use `aerobeat-web-asset-gameplay` only if the payload later gains web-specific assets or contracts.
- Preserve engine-neutral ownership: editable Blender/source files, provenance, export tooling, validation, individual GLBs, and mix-and-match set manifests belong to the asset polyrepo; PlayCanvas/Godot consumers own only engine integration and rendering behavior.
- Keep every swappable semantic role in a separate editable source directory and separate GLB rather than a combined gameplay GLB. Initial roles are directional arrow, any-note circle, guard shield, bomb, normalized wall, track, and shared athlete-marker sphere.
- Name the initial directional-arrow variation `outline-v1`; do not encode the inspiration source in the asset identifier.
- A set manifest selects one versioned variation independently per role so complete sets and arbitrary combinations can coexist. The shield GLB remains one canonical asset instantiated twice for a guard event.
- `Great`/`Miss` remains renderer-owned world text rather than a GLB. Dynamic interval scaling, timing tint, row colors, instancing, outline passes, and scoring/timing behavior remain consumer/runtime responsibilities.
- Future immutable asset releases should preserve separate GLBs, manifests, per-file hashes, coordinate/pivot/bounds metadata, provenance, and export-tool versions. A consuming assembly pins an exact asset release and copies only its selected assets into that assembly's immutable release.
- This section originally approved architecture and naming only; the later explicit execution approval below now authorizes Tasks 2–6 and creation of the public asset polyrepo while preserving all listed ownership boundaries.

## Proposed tasks

### Task 1 — Author visual specifications and review renders

- Define dimensions, pivots, forward axes, materials, outline technique, polygon/texture budgets, and collision-free render bounds for arrow, circle, shield, wall, bomb, and track.
- Produce neutral and in-context review renders before integration.
- Derrick approves silhouettes/materials before Task 2.

**Task 1 results (2026-09-02):**

- Authored `docs/handcrafted-3d-visual-spec-v1.md` with exact role dimensions, pivots, +Y-up/local−Z-forward convention, materials, outline/depth/transparency rules, polygon/texture budgets, collision-free render bounds, reuse constraints, and acceptance criteria.
- Produced exactly two labeled deterministic `1600 × 900` RGB PNG review boards in `.plans/review/2026-09-02-handcrafted-3d-task1/`. Both visibly cover directional arrow, any-note circle, the one shared shield, full interval wall, spiny bomb, and glass track.
- Gameplay context uses only locally drawn abstract ice-toned gradients/polygons; it does not import the owned alien-moon environment or any other environment asset.
- Recorded dimensions, SHA-256 hashes, rights/provenance, non-runtime disclaimer, and review camera in `manifest.json`. The boards are review evidence only and no generator, model, mesh, GLB, texture, or runtime code is retained in the repository.
- Validation passed: both PNGs are nonempty RGB `1600 × 900`, manifest hashes reproduce, directory contains exactly the two PNGs plus manifest, JSON parses, and `git diff --check` passes.
- Coder Bead `aerobeat-web-assembly-k72.1` completed; visual approval remains the gate before Task 2, and Tasks 2–6 remain untouched/unapproved.

**Task 1 independent QA (2026-09-02) — PASS:**

- Verified exact coder commit `b149f28`: scope is limited to this plan, Beads metadata, the specification, manifest, and two PNG review boards. No runtime/model/GLB/environment/release file changed, and fetched `origin/main` exactly matched the clean coder commit before QA metadata.
- Confirmed all six roles are specified and visibly labeled/readable on both boards: directional arrow, any-note circle, exactly one shared shield, complete-interval translucent wall, spiny bomb, and glass track. Shared Flow/Boxing arrow reuse, material-only left/right shield reuse, authoritative full-interval wall scaling/endpoints, and deterministic track segment reuse are explicit.
- Confirmed exact right-handed `+Y`-up/local `−Z`-forward convention and review camera position `[0.05, 1, 5]`, Euler `[0, 0, 0]`, vertical FOV `48°`; per-role dimensions, pivots, materials, depth/transparency/outline behavior, budgets, collision-free bounds, and aggregate limits are complete and coherent.
- Independently decoded both boards as nonempty RGB `1600 × 900` PNGs. SHA-256 reproduced exactly: neutral `f5c5f6879ed18bcf007d5f6b4c721ff65d4f55e528db44e93f3189f054ed42ca`; gameplay context `47eade8a7b160b34fdbffb027b71c067db0d1f97f82e51917cde738d2ddfb1da`. The review directory contains exactly those two PNGs and `manifest.json`.
- Visual QA confirms white/dark rims remain legible on neutral and pale ice-toned fields, arrow/circle/shield silhouettes stay distinct, bomb remains black with a separate red hazard cue, track perspective is immediate, and the translucent wall visibly exposes front/side/full Z span without hiding gameplay objects.
- Rights/provenance and non-runtime disclaimers are explicit; the context artwork is abstract and locally authored, with no imported environment or third-party asset. `git diff --check` passed. This QA does **not** grant Derrick's required visual approval and does not authorize or touch Tasks 2–6.
- QA Bead `aerobeat-web-assembly-k72.2` closed PASS; audit Bead `aerobeat-web-assembly-k72.3` and Derrick visual approval remain pending.

**FINAL AUDIT PASS (2026-09-02):**

- Audited exact coder commit `b149f28b52c8989b0378d785c2a9feb13fac0e01` and QA commit `acec27b999dd9bed5a6eec128eca0471f04daf79`; both are committed in order and the pre-audit checkout matched clean `origin/main` at the QA commit.
- Recomputed both nonempty RGB `1600 × 900` PNGs and reproduced manifest SHA-256 exactly: neutral `f5c5f6879ed18bcf007d5f6b4c721ff65d4f55e528db44e93f3189f054ed42ca`; gameplay context `47eade8a7b160b34fdbffb027b71c067db0d1f97f82e51917cde738d2ddfb1da`. The review directory contains exactly those two PNGs plus `manifest.json`.
- Independently inspected both boards and the complete specification/manifest. All six required roles are present and labeled on both boards: directional arrow, any-note circle, one shared shield, full interval wall, spiny bomb, and glass track. Required reuse, dimensions, pivots, axes, material/depth/outline behavior, budgets, bounds, camera, provenance, and non-runtime constraints are explicit.
- Exact committed scope from the Task 1 chain is limited to plan/Beads metadata, the specification, manifest, and two review PNGs. No runtime/model/GLB/environment/release file or Task 2–6 implementation changed; `git diff --check` passed.
- Task 1 is review-ready only. This audit does **not** approve silhouettes/materials on Derrick's behalf and does not authorize Tasks 2–6. Parent Bead `aerobeat-web-assembly-k72` remains open/in progress pending Derrick's separate visual review and approval.

### Task 2 — Create canonical self-contained model assets

- If separately approved, create the cross-engine `aerobeat-asset-gameplay` polyrepo; use `aerobeat-web-asset-gameplay` instead only if concrete web-specific payload becomes necessary.
- Generate or model each role as an independently swappable source/export pair: directional arrow variation `outline-v1`, any-note circle, shield, bomb, normalized wall, track, and shared athlete-marker sphere.
- Keep one separate GLB per role/variation and author mix-and-match set manifests; do not combine all gameplay models into one GLB.
- Author red translucent wall and blue glass track materials while leaving runtime scaling, tint, instancing, outline passes, and world text to engine consumers.
- Normalize transforms and deterministic source/export files; record provenance, rights ownership, per-file hashes, bounds, pivots, and coordinate metadata for every asset.

### Task 3 — Add renderer-owned 3D asset contract

- Load canonical local assets through the renderer without introducing runtime network dependencies.
- Map Flow and Boxing directional events to the shared arrow, directionless events to the circle, each guard event to two simultaneous instances of the one canonical shield in both applicable cells/lanes, obstacles to interval-sized walls, and bombs to the urchin model.
- Replace flat-looking nose/wrist markers with depth-correct 3D spheres; add success-area white beat tint and threshold-positioned short-lived `Great`/`Miss` world feedback without changing timing/scoring truth.
- Keep fallback behavior explicit and testable during development; remove obsolete production sprite/block paths when landing.

### Task 4 — Correct full-length Flow walls and add the glass track

- Derive wall center and world-Z scale from authoritative `centerTimestampMs → endTimestampMs` interval truth.
- Add a stable blue-glass approach track below the athlete/grid and through the visible timeline depth.
- Preserve red/yellow/green near-athlete row bands while ensuring the white success-area beat tint remains distinct from the white-outlined track and environment.
- Validate sorting, clipping, transparency, perspective, 3D athlete-marker occlusion, and short-lived feedback readability at supported viewports/DPRs.

### Task 5 — Integrate alien moon ice test environment

- Import the local GLB and sibling transform config as a self-contained test background.
- Establish camera/environment/track framing without changing gameplay coordinates.
- Keep environment selection and lifecycle teardown deterministic.

### Task 6 — QA, audit, and physical review

- Unit/model tests for semantic mapping, reuse, full obstacle length, transforms, and deterministic manifests.
- Chromium direct/iframe portrait/landscape DPR matrix for depth, alpha, outlines, handedness, and full-length walls.
- Visual evidence for Flow, Boxing Lanes, and Boxing Grid.
- Derrick physical review before immutable successor release work.

## Approval gate

Derrick approved Task 1 after the camera-loader successor was ready and approved its revised visual package on 2026-09-02. Derrick then explicitly approved Tasks 2–6 and explicitly authorized creation of the **public** `AeroBeat-Workouts/aerobeat-asset-gameplay` GitHub repository. The asset repository must remain cross-engine; if concrete web-specific payload becomes necessary, use a separately approved `aerobeat-web-asset-gameplay` repo instead. Temporary sudo was offered only if a required local tool installation needs it. This approval does not authorize third-party asset acquisition, Gaussian splats, unrelated feature work, package publication, or GitHub Releases beyond the planned repository/commits.

## Task 1 revision coder result (2026-09-02)

- Revised `docs/handcrafted-3d-visual-spec-v1.md` so every guard beat displays two simultaneous visible identical instances of one canonical shield model/icon in the applicable left/right cells or lanes. The only distinction is placement; geometry, icon, scale, orientation, material, Y, and beat Z remain identical.
- Added review-only, non-scoring specifications for full 3D nose/left-wrist/right-wrist spheres at truthful role-colored world positions; red/yellow/green near-athlete rows; a `60 ms` white success-area core tint that preserves silhouette; `80 ms` resolved-beat removal; and same-height threshold-positioned `Great`/`Miss` world text with `180 ms` hold plus `170 ms` fade (`350 ms` total).
- Fixed depth/occlusion ordering, per-object bounds, coexistence limits, triangle/texture budgets, and explicit exclusions from judgement thresholds, score, combo, timing truth, event IDs, persistence, public contracts, obstacles, and bombs.
- Regenerated the two existing RGB `1600 × 900` review boards. Both retain and label the original six roles while visibly demonstrating dual identical shields, 3D role-colored athlete spheres, red/yellow/green rows, white success tint with silhouette retained, rapid disappearance, and `Great` / white-outlined `Miss` world feedback.
- Updated `manifest.json` revision, fixed review camera/provenance, board coverage, SHA-256 records, and non-runtime disclaimer. Temporary Pillow generators were removed; no generator, model, mesh, GLB, texture, runtime code, environment, or Tasks 2–6 implementation is retained.
- Coder validation passed: exact directory inventory is two PNGs plus one manifest; both PNGs are nonempty RGB `1600 × 900`; JSON parses; hashes reproduce; repository scope and diff checks pass. Bead `aerobeat-web-assembly-k72.4` is complete; `k72.5`, `k72.6`, and parent `k72` remain open for independent QA/audit and Derrick review.

## Task 1 revision independent QA — FAIL (2026-09-02)

- Mechanical scope, image, manifest, and hash checks passed, but native-resolution visual inspection found clipped/overlapping neutral-board A/C headings and white-tint annotation, plus overlapping or duplicated gameplay-context row, dual-shield, world-text, and white-tint labels.
- Root cause is fixed text placement without measured bounds/exclusive annotation zones; coverage-only validation did not detect presentation collisions. Diagnostic `.plans/debug/2026-09-02-task1-review-board-layout.md` records the full analysis.
- Repair Bead `aerobeat-web-assembly-k72.7` blocks reopened QA `k72.5`. Regenerate only the two review PNGs and manifest using measured/wrapped text, unique callouts, and reserved row/footer zones; specification semantics and all runtime/model/environment files remain untouched.

## Task 1 revision independent re-QA — PASS (2026-09-02)

- Re-QA inspected exact repair commit `f6e4ac3283cdfe8f304ec408f8e5e6e72f61f47e`; before QA metadata, fetched `origin/main` exactly matched the clean repair commit. Its scope is limited to the two review PNGs, `manifest.json`, the layout diagnostic result, and Beads metadata. No specification, runtime, model, mesh, GLB, texture, environment, release, or Tasks 2–6 implementation file changed.
- Independently inspected both complete boards at native `1600 × 900`. Every prior defect is gone: neutral A/C headings and the white-core annotation are fully contained; gameplay RED, GREEN / SUCCESS, dual-shield, world-text, and white-success-tint labels are separated; duplicate callouts are removed; headings, labels, callouts, and footer copy are unique, contained, non-overlapping, and readable.
- Both boards retain visible coverage of all six canonical roles: directional arrow, any-note circle, one canonical shared shield model/icon shown as two simultaneous identical instances, full interval wall, spiny bomb, and glass track. They also visibly retain truthful-depth nose/left-wrist/right-wrist spheres, red/yellow/green timing rows, white success-area core tint with silhouette retained, resolved-beat disappearance in `80 ms`, and same-height `Great` / outlined `Miss` world text with `350 ms` total lifetime.
- Cross-checked the complete specification and manifest: right-handed `+Y`-up/local `−Z`-forward coordinates, fixed review camera, dimensions, pivots, materials, bounds, depth/occlusion, budgets, reuse rules, clarified state semantics, rights/provenance, and exact review-only/non-runtime boundary remain explicit and coherent.
- Independently decoded both boards as nonempty RGB PNGs at exactly `1600 × 900`; the review directory contains exactly those two PNGs plus `manifest.json`. Manifest SHA-256 values reproduce exactly: neutral `3aa318ef432a8678dcb87e59b4df81299537b96081d8e3b9695acff9be7b229f`; gameplay context `e8bf7d79b6ed3a5ef66e1f03f1809677cf2c16c735f017553d8ab34960de63ee`. JSON parsing and `git diff --check` passed.
- QA Bead `aerobeat-web-assembly-k72.5` closed PASS. This re-QA does **not** approve visuals for Derrick, does not authorize or touch Tasks 2–6, and leaves `k72.6` plus the parent visual-review/approval gate open.

## Task 1 revision final independent audit — PASS (2026-09-02)

- Audited the required linear chain at exact commits: requirements `8a8b2d575216f72576a490e62344e742a88fe0b7`, revision `ab150be14e6efc8375d8494ab54b9a7eb780e9ca`, diagnosis `41a9e07979b88c6ab2a579c9bdef05faa90fae16`, repair `f6e4ac3283cdfe8f304ec408f8e5e6e72f61f47e`, and re-QA `e94eceea201748261ced321c89d3b3a785d68aa5`. Before audit metadata, fetched `origin/main` exactly matched the clean re-QA commit.
- Independently inspected the complete specification, both native `1600 × 900` boards, revision-3 manifest, layout diagnostic/repair record, revised Bead chain, and cumulative Git scope. Both boards are readable and collision-free and visibly show one canonical shield model/icon as two simultaneous identical instances, truthful-depth nose/left-wrist/right-wrist 3D spheres, red/yellow/green rows, white success-area core tint with silhouette retained, `80 ms` resolved-beat removal, and threshold-positioned same-height `Great` in white plus `Miss` in red with a white outline and `350 ms` total lifetime.
- Confirmed the six canonical roles remain visible and labeled, all headings/callouts/footer copy are unique, contained, non-overlapping, and readable, and the specification/manifest preserve coordinate, camera, dimensions, pivots, material, bounds, depth/occlusion, budget, provenance, and review-only/non-runtime constraints.
- Independently reproduced the exact three-file review inventory, RGB dimensions, JSON parse, and SHA-256 values: neutral `3aa318ef432a8678dcb87e59b4df81299537b96081d8e3b9695acff9be7b229f`; gameplay context `e8bf7d79b6ed3a5ef66e1f03f1809677cf2c16c735f017553d8ab34960de63ee`. Cumulative `git diff --check` passed.
- The audited chain changes only plan/Beads metadata, the Task 1 specification, two PNG review boards, their manifest, and the layout diagnostic. No runtime, model, mesh, GLB, texture, environment, release, or Tasks 2–6 implementation changed.
- Task 1 is revised-review ready only. This audit does **not** approve the visuals for Derrick and does **not** authorize Tasks 2–6. Audit Bead `aerobeat-web-assembly-k72.6` closes PASS; parent `aerobeat-web-assembly-k72` remains open/in progress pending Derrick's visual approval.

## Derrick Task 1 visual approval — PASS (2026-09-02)

- Derrick explicitly approved the revised Task 1 visual direction after reviewing the collision-free neutral and gameplay-context boards.
- Approved scope includes the six canonical 3D role designs, simultaneous identical dual-shield instances, 3D nose/wrist spheres, red/yellow/green timing rows, white success-area beat tint, and short-lived threshold-positioned `Great`/`Miss` feedback exactly as specified.
- This approval completed Task 1 review only at that time. The later execution approval below supersedes that stop condition for Tasks 2–6 while third-party acquisition and unrelated publication remain forbidden.

## Derrick Tasks 2–6 execution approval (2026-09-02)

- Derrick explicitly approved the full remaining plan: Task 2 asset creation, Task 3 renderer integration, Tasks 4–5 track/wall/environment integration, and Task 6 QA/audit/release/physical review.
- Derrick explicitly authorized creation of the public GitHub repository `AeroBeat-Workouts/aerobeat-asset-gameplay`. GitHub read-back confirmed it exists with `PUBLIC` visibility at `https://github.com/AeroBeat-Workouts/aerobeat-asset-gameplay`; its local checkout is `/home/derrick/.dsh/projects/aerobeat/aerobeat-asset-gameplay`.
- Execution Bead graph: asset coder `k72.8` → asset QA `k72.9` → asset audit `k72.10` → Derrick 3D asset review `k72.18` → renderer coder `k72.11` → renderer QA `k72.12` → assembly/environment coder `k72.13` → cross-mode matrix QA `k72.14` → final audit `k72.15` → immutable release `k72.16` → Derrick physical gate `k72.17`.
- Local inspection found Blender absent and Ubuntu candidate `4.0.2+dfsg-1ubuntu8`; Derrick explicitly offered temporary sudo for required installation, `sudo -n` succeeded, and managed job `bash-11` installed Blender `4.0.2+dfsg-1ubuntu8` successfully. No third-party art acquisition is authorized.
- Research architect subagent `b26fd796-601f-4120-9477-d134d554fd73` launched and was verified running to recommend the cross-engine source/export/validation pipeline. Asset coder subagent `227815d2-f8d3-49e4-90df-c070d32245c0` then launched and was verified running for `k72.8`. The added Derrick 3D review gate `k72.18` blocks renderer integration `k72.11`, preserving the requirement that real model renders be visually approved before Task 3.
- Owned alien-moon source read-back: GLB SHA-256 `40e38a7bdce9eab4266d8bb19510a95bb4e0410534f3f14a500f36fac2b65077` (`6,149,400` bytes), sibling PNG SHA-256 `7654e9450ddc16ed613bf7c5bbd442c0466d89cad8ea4a59a3d1ff12ef8f513c` (`6,117,570` bytes), and config transform is identity position/rotation/scale. Environment repo is clean at `origin/main`.

## Task 2 candidate asset package (2026-09-02)

- Initial background asset coder produced only the README for multiple observation intervals and was retired together with the nonessential research child. Bounded recovery coder completed the shared checkout; the original coder later independently observed and reported the same completed state, with no conflicting second commit.
- Public `AeroBeat-Workouts/aerobeat-asset-gameplay` is clean on `main == origin/main` at candidate commit `673fc75dec250392b79565dfd186a26261513df4`; GitHub read-back confirms `PUBLIC` visibility and default branch `main`.
- Candidate contains seven independent editable `.blend` / GLB / manifest identities: `directional-arrow/outline-v1`, `any-note/circle-v1`, `guard/shield-v1`, `bomb/urchin-v1`, `wall/red-glass-v1`, `track/blue-glass-v1`, and `athlete-marker/sphere-v1`; no combined GLB, texture, font, external asset, or engine runtime/import metadata exists.
- Immutable asset release `release/raw/0.0.1` contains exactly 17 files. Inventory SHA-256 is `2f448b2efe8a9cb53f32580b94272ab2b08e3d3ba5474ee12bed9a4c8f5aec57`; proof SHA-256 is `6bd6eb5f83cdd4646d22ad1b1de5b6786901eb8a5bd321728f015e2e5dbd7c57`. Two temporary rebuilds reproduced all 17 release bytes exactly. Editable `.blend` container bytes are truthfully excluded from determinism claims; `tools/generate.py` is source authority.
- Nine actual Blender EEVEE RGB `1600 × 900` review renders exist under `review/0.0.1/`. Parent read-back inspected the neutral, gameplay-context, arrow, shield, and bomb renders and separately reran `python3 tools/validate.py --root . --release 0.0.1`, which passed seven asset bounds/budgets, release inventory/hashes/provenance/dependencies/review hashes, and clean Blender imports.
- Candidate Bead `k72.8` remains in progress pending strict independent visual/mechanical QA `k72.9`; no Derrick approval or renderer integration is implied.

## Task 2 independent visual QA — FAIL (2026-09-02)

- QA mechanically reproduced strict validation, seven clean Blender source opens, all 17 release files byte-identical across two temporary builds, and the absence of external URIs/textures/fonts/engine metadata/third-party dependencies.
- Native render inspection failed the approved visual contract: arrow, shield, and any-note use predominantly black perimeter silhouettes with white reading only as detached/top highlights; the neutral board is unlabeled and crops wall/arrow/track; the context omits the arrow and all three marker spheres; the individual wall is cropped and track is too narrow/small for useful review.
- Root cause: `tools/generate.py` used nested complete solids with tiny depth offsets rather than explicit front-facing white ring geometry, and hard-coded cameras/placements without projected-bounds fitting. Full diagnosis is in asset-repo `.plans/debug/2026-09-02-initial-asset-visual-qa.md` at commit `2a82327`.
- Finalized `release/raw/0.0.1` must remain byte-immutable. Repair Bead `k72.19` blocks `k72.9` and must create successor `0.0.2` with explicit white rings, bounds-fitted/labeled complete reviews, and projected containment/role-count validation. `k72.8` and `k72.9` remain open/in progress.

## Task 2 visual repair and independent re-QA — PASS (2026-09-02)

- Repair commit `85e55eb50b1fb5f0aa27baa1dc99e8bae602a56d` created immutable successor asset release `0.0.2`; `0.0.1` release/review bytes remain unchanged from diagnostic baseline `2a82327`.
- Arrow/circle/shield now use explicit continuous white front perimeter geometry with charcoal separation and role-color inset. The labeled neutral board contains all seven complete assets; context contains arrow, circle, exactly two canonical shield instances, bomb, scaled wall, track, and three marker spheres; individual wall/track reviews are complete and useful.
- `0.0.2` contains exactly 17 release files. Inventory SHA-256 `1a5b66f543bae940b8bb789e9ab9979d073663b5f6ff12382e08f4ad10c0ff1b`; proof SHA-256 `90dcbe52b35d2ec11a01784a96f195b5cd01ac141000886cb950c74864eec288`.
- Parent and independent re-QA visually inspected all nine RGB `1600 × 900` renders. Re-QA verified local/origin/public Git equality, strict validation, seven source opens and clean GLB imports, exact bounds/pivots/names/budgets/dependencies, absent external URIs/textures/fonts/engine metadata, Python compilation, and two complete reproducibility passes. Generator rejects missing release, finalized `0.0.1`, and unsupported `0.0.3`.
- Repair `k72.19` and QA `k72.9` closed PASS. Asset implementation `k72.8` remains in progress for final asset audit `k72.10`; Derrick 3D approval gate remains pending.

## Task 2 license audit repair and re-QA — PASS (2026-09-02)

- Final audit found that `LICENSE.md` inherited a noncanonical self-defeating CC BY-NC clause from `aerobeat-template-asset`; asset manifest-label validation could not detect the legal-text corruption. Audit created blocker `k72.20` and left `k72.8`/`k72.10` open.
- Debug record `.plans/debug/2026-09-02-license-text-integrity.md` was committed in asset repo at `345f05c`. A separate nonblocking template-lane issue `aerobeat-template-asset` Bead `oc-n2m` tracks the same inherited clause across eight discovered AeroBeat asset/template/environment repos.
- Repair commit `0ed97676a0a816b797b12b9f8d19a9d281b9da03` replaced the file with exact official CC BY-NC 4.0 plain text: SHA-256 `41003d4a74749c0220e33dd415042164b5a1093ed401f36277234f772d22d3d0`, `19,347` bytes, `408` lines. Validation now pins that identity and rejects a one-bit mutation with `license integrity mismatch`.
- Independent re-QA matched official/local/public bytes, reproduced mutation rejection, strict validation, seven source/GLB imports, and release reproducibility. All 70 protected source/manifest/set/release/review files remained byte-identical to pre-license-fix commit `85e55eb`; only `LICENSE.md`, `tools/validate.py`, and `tools/reproducibility.py` changed.
- License repair `k72.20` closed PASS. Final asset audit `k72.10` remains in progress; no Derrick model approval or runtime integration is implied.

## Task 2 final independent asset audit — PASS (2026-09-02)

- Audited the complete public asset-repository chain in exact linear order: initial package `673fc75dec250392b79565dfd186a26261513df4`, visual diagnosis `2a823272e4626ba4ff97b159faeaefc946a7d1d1`, corrected successor `85e55eb50b1fb5f0aa27baa1dc99e8bae602a56d`, license diagnosis `345f05ccb30086bd43204be426a22ac1dc7eaffe`, and license repair `0ed97676a0a816b797b12b9f8d19a9d281b9da03`. GitHub and Git independently resolve public, non-archived `AeroBeat-Workouts/aerobeat-asset-gameplay`, default branch `main`, with local `main == origin/main ==` public `main` at `0ed97676`; the repository has no GitHub Releases.
- Confirmed the exact seven independently swappable identities: `directional-arrow/outline-v1`, `any-note/circle-v1`, `guard/shield-v1`, `bomb/urchin-v1`, `wall/red-glass-v1`, `track/blue-glass-v1`, and `athlete-marker/sphere-v1`. Each has one separate editable `.blend`, root manifest, release manifest, and separate GLB; every GLB contains exactly one identity node and one mesh. `sets/default-v1.json` maps every role independently and requires two instances of the one canonical `guard/shield-v1`; there is no combined GLB.
- Reproduced strict `0.0.2` validation under exact Blender `4.0.2`: schemas, identities, hashes/byte sizes, canonical names, dimensions, measured AABBs, pivots, identity transforms, collision-free bounds, triangle budgets, coordinates, analytic materials, rights/provenance, set constraints, inventories, review hashes/layout, dependency policy, and all seven clean GLB smoke imports passed. Separately factory-opened all seven tracked `.blend` sources and asserted one identity mesh, identity transforms, analytic materials, and no images, fonts, libraries, cameras, or actions. Python compilation and the reviewed whitespace check passed.
- Independently reran reproducibility: primary plus two fresh temporary builds matched all 17 immutable `0.0.2` release files byte-for-byte. Generator negative guards reject omitted `--release`, finalized `0.0.1`, and unsupported `0.0.3`. Both releases contain exactly 17 files: seven GLBs, seven manifests, one set, inventory, and proof only. Original `0.0.1` release/review trees are byte-unchanged from `673fc75`; inventory/proof SHA-256 remain `2f448b2efe8a9cb53f32580b94272ab2b08e3d3ba5474ee12bed9a4c8f5aec57` / `6bd6eb5f83cdd4646d22ad1b1de5b6786901eb8a5bd321728f015e2e5dbd7c57`. Corrected `0.0.2` inventory/proof SHA-256 reproduce as `1a5b66f543bae940b8bb789e9ab9979d073663b5f6ff12382e08f4ad10c0ff1b` / `90dcbe52b35d2ec11a01784a96f195b5cd01ac141000886cb950c74864eec288`. Both release trees remain finalized read-only.
- Natively inspected all nine RGB `1600 × 900` `review/0.0.2` PNGs. Arrow, circle, and shield have continuous visible white front perimeters with charcoal separation; bomb, marker, translucent wall, and full-depth track are complete and readable. The labeled neutral board contains all seven roles without clipping. The context frame visibly contains one arrow, one circle, exactly two identical canonical shields, one bomb, one scaled full-interval wall, one track, and three distinct marker spheres. Recorded hashes and calculated containment/count evidence reproduce. This confirms the visual repair and justifies closure of `k72.19`.
- Fetched the official Creative Commons BY-NC 4.0 plain text and public raw `LICENSE.md` independently. Official, local, and public bytes are identical at exactly `19,347` bytes / `408` lines / SHA-256 `41003d4a74749c0220e33dd415042164b5a1093ed401f36277234f772d22d3d0`. A fresh same-size one-bit mutation failed closed with nonzero status and `license integrity mismatch`. From `85e55eb` to `0ed9767`, `source/`, `manifests/`, `sets/`, `release/`, and `review/` retain identical Git tree IDs; only `LICENSE.md`, `tools/validate.py`, and `tools/reproducibility.py` change in the repair commit. This confirms the rights/provenance repair and justifies closures of `k72.20` and the full-package QA gate `k72.9`.
- Repository scope is cross-engine and self-contained: tracked inventory has no package manifest, JavaScript/TypeScript, engine runtime/integration/import metadata, textures, external images, font files, external GLB URIs, compression dependencies, downloaded or third-party assets. Manifest provenance consistently records locally authored deterministic procedural primitives, no network, no external assets, and no third-party content. No GitHub Release or npm package/publication exists.
- Final disposition: Task 2 implementation `k72.8` and final audit `k72.10` close PASS. Separate template issue `oc-n2m` is nonblocking here because this gameplay repository's official license bytes are repaired and mutation-guarded. Derrick-only model review gate `k72.18` remains open, and renderer integration `k72.11` remains blocked by that gate. This audit does not approve the models for Derrick and does not start integration.

## Derrick Task 2 canonical 3D asset approval — PASS (2026-09-02)

- Derrick reviewed the linked actual Blender-rendered `0.0.2` neutral board, gameplay context, and individual asset directory, then explicitly selected **Approve 3D assets**.
- Immutable cross-engine asset release `0.0.2` is therefore approved as the pinned source for Tasks 3–5. Approval covers the seven separate role assets and their current silhouettes/materials; it does not waive renderer/assembly QA, final audit, immutable web release proof, or physical review.
- Gate `k72.18` closes PASS and renderer implementation `k72.11` becomes eligible to start.

## Task 3 renderer implementation candidate (2026-09-03)

- Initial broad renderer child claimed `k72.11` but produced no files across bounded observation, so it was retired. The task was decomposed into loader `k72.11.1` and visual behavior `k72.11.2` to reduce execution risk.
- Loader commit `0a9e9a5a790f79567f5ec9d0738c3475f7a881a3` copied/verifies the exact approved 17-file asset release `0.0.2`, packages only runtime assets, and added generation/abort/context-safe PlayCanvas container preload with explicit fallback diagnostics. Parent independently reran `npm test` and full `npm run test:browser`; both passed. `k72.11.1` closed PASS.
- Visual commit `8e67860ecb33f288ac1e4b25507f0fe0acf6583f` instantiates canonical GLBs for arrows/circles/bombs/full-Z walls/track, identical two-instance guard pairs, and truthful marker spheres; preserves red/yellow/green rows; implements white success tint, exact `80 ms` removal, and bounded stationary `Great`/`Miss` feedback (`180 + 170 = 350 ms`). It removes normal-path legacy target blocks/icons while retaining only explicit loader/context fallback.
- Coder validation passed unit/browser matrices, direct/real-iframe portrait/landscape DPR1/3, GLB parsing/material cloning, fallback/context/reconnect/destroy, no RAF/console noise, exact 37-file npm pack containing the 17 pinned asset files, and no source/review/old-release/tool payload. Candidate renderer is clean at `main == origin/main`; `k72.11.2` closed and parent `k72.11` remains open for independent QA `k72.12`.
- Current assembly still intentionally omits bomb/arc/burst projection; that consumer mapping remains in approved assembly Task `k72.13` rather than being invented inside renderer semantics.

## Task 3 renderer loader slice (2026-09-02)

- Initial renderer coder `88a60316-4dca-41cb-80b1-d267fc2cbcb2` claimed `k72.11` but produced no observable file/test activity over the bounded startup window and was retired cleanly. The parent task was decomposed into loader `k72.11.1` → visual behavior `k72.11.2`.
- Recovery loader commit `0a9e9a5a790f79567f5ec9d0738c3475f7a881a3` copies and verifies the exact 17-file approved asset release under renderer `assets/gameplay/0.0.2`, exposes immutable identities/package-relative URLs, and preloads all seven local GLB containers with byte/hash verification, abort/stale-generation rejection, context-loss fallback/reload, independent renderer ownership, and deterministic disposal.
- Packaging includes only renderer source/docs and the 17 runtime asset files; `.blend`, review, old `0.0.1`, and asset tools are excluded. PlayCanvas 2.21.4 successfully parsed all seven containers and synthesizes omitted GLB normals.
- Parent independently reran `npm test` and `npm run test:browser`; asset sync/identity/hash/lifecycle/pack checks and the existing Chromium direct/iframe portrait/landscape DPR matrix passed. Loader Bead `k72.11.1` closed; parent `k72.11` and visual behavior `k72.11.2` remain in progress/open.

## Task 3 independent renderer QA — PASS (2026-09-03)

- Independently audited the exact clean renderer chain `0a9e9a5a790f79567f5ec9d0738c3475f7a881a3` → `8e67860ecb33f288ac1e4b25507f0fe0acf6583f`; fetched local `main`, `origin/main`, and `HEAD` all matched `8e67860` before and after QA. Reviewed renderer README, accepted decisions, complete source, loader/visual diffs, and unit/browser validation code. QA made no renderer, assembly-runtime, environment, or asset-source edit.
- Reproduced the exact approved immutable gameplay payload: 17 packaged `0.0.2` files, seven separate GLBs, source commit `0ed97676a0a816b797b12b9f8d19a9d281b9da03`, inventory SHA-256 `1a5b66f543bae940b8bb789e9ab9979d073663b5f6ff12382e08f4ad10c0ff1b`, proof SHA-256 `90dcbe52b35d2ec11a01784a96f195b5cd01ac141000886cb950c74864eec288`, and all seven exact GLB SHA-256/byte identities. `npm test` passed the full type/JSDoc/import/component/console, sync/hash/inventory, loader stale/error/context/disposal/independence, camera, semantic mapping, hostile bounds, timing, and cap suite.
- Full `npm run test:browser` passed. Actual PlayCanvas 2.21.4 container resources parsed for all seven GLBs in two independent applications; normal ready rendering instantiated canonical roots with per-instance cloned materials while canonical source materials remained unchanged. Loading rendered no target substitute, and primitive target/marker fallback appeared only after explicit preload error or context-loss state. Context restore created a fresh generation; stale/aborted work could not commit. Detach/reconnect, idempotent destroy, independent multi-instance resources, pointer/camera cleanup, and atlas/container/material/texture/entity release all passed with zero engine RAF and no unexpected console, page, or WebGL noise.
- Actual render entities and pixels confirmed Flow and Boxing Grid/Lanes directional arrow versus directionless circle roles, bomb support, and exactly two independently instantiated but byte-equivalent non-mirrored shield roots per guard. Shield geometry/source, scale, orientation, material appearance, Y, and Z matched; X was the only placement difference. Three canonical marker spheres occupied distinct truthful projected world coordinates with normal depth testing/writes and retained nose/left-wrist/right-wrist role colors. Existing camera and right-handed `+Y`/future `−Z` projection remained unchanged.
- Verified red/yellow/green timing rows; core-only 60 ms white success tint with structural white/charcoal rim materials preserved; exact 80 ms resolved-target removal at crossing Z; stationary same-height world feedback at exact crossing coordinates; `Great` white face/dark separation and `Miss` red face/white separation glyph pixels; 180 ms hold plus 170 ms fade with exact 350 ms absence; newest-four cap; and obstacle/bomb exclusions. Diagnostics expose only bounded aggregates and timings, not target IDs, judgement labels, score, pixels, or media.
- Verified normalized Flow walls preserve authoritative interval center and complete Z length with X/Y scale exactly `1`; three canonical glass-track roots remain unscaled at centers `−12/−36/−60`, covering Z `0…−72` without stretching lane lines. Opaque assets/markers depth-write; deterministic ordering is opaque assets → timing/grid surfaces → track body/edges → wall body/edges → depth-tested/write-off world feedback, with authored transparent bodies and edge cages preserved.
- Independently captured and read eight fresh actual-pixel renders across direct and real cross-origin iframe embedding, portrait `390×844` and landscape `844×390`, requested DPR `1` and `3` (renderer cap applied DPR `2`). Every case reported ready seven-asset generation, zero fallback instances, two shield instances, three marker spheres, three track segments, one full wall, two feedback glyphs, non-background pixels, and no console/page noise; direct and iframe pixels were visually equivalent. This is renderer QA evidence only, not Derrick physical review.
- `npm pack --dry-run --json` passed with exactly 37 files, 62,267 packed bytes / 207,790 unpacked bytes, SHA-1 `1cdc4e9f027dbf4fddc3cff3243bcfc8b5677bed`, exactly the 17 pinned runtime asset files, and no `.blend`, review, `0.0.1`, source-asset tool, or unrelated payload. Public exports/service identity, caller-owned cadence, camera contract, timing/scoring/event IDs, and assembly-facing contracts were not changed by the audited commits.
- QA disposition: `k72.12` closes PASS. Parent implementation `k72.11` intentionally remains `in_progress` for the later final audit/coordination lane; assembly/environment integration and Derrick physical review remain untouched.

## Bead dependency-direction diagnosis and repair (2026-09-03)

- `k72.12` initially required forced closure because the initial graph used `bd create --deps blocks:<predecessor>` as though it meant “depends on predecessor”; it actually made each newly created task block its predecessor. This reversed the execution chain and masked readiness because agents were assigned explicitly.
- Full diagnosis is preserved in `.plans/debug/2026-09-03-bead-dependency-direction.md`. Every erroneous `blocks` edge was removed and re-added with explicit `bd dep add <successor> <predecessor>` syntax. Parent-child and discovered-from links were preserved.
- `bd dep cycles --json` returns `[]`; `k72.13` is now the sole ready execution task, while `k72.14` → `k72.15` → `k72.16` → `k72.17` are correctly blocked in order. Renderer parent `k72.11` then closed normally without force because its implementation, visual, and QA prerequisites are complete.

## Tasks 4–5 candidate foundation and provenance blocker (2026-09-03)

- Assembly candidate commit `2e84f8818a798d5751c51744afc2919ec4427844` added exact local moon GLB/config copies and strict validation, truthful non-scoring Flow bomb projection, and removed obsolete branding-atlas initialization/dependency. Its unit/browser/build/package checks passed, but `k72.13.1` was reopened after later provenance inspection.
- Embedded GLB `asset.extras` identifies author **Luis Vidal**, title **Icescape 1 Alien Moon Skybox**, source Sketchfab model `bfc9a041814f4112b016904edfaad0c5`, and license **CC BY 4.0**. This contradicts the candidate manifest’s `owned-local-source`/`externalPayloadCopied:false` claims and the approved plan’s no-third-party redistribution boundary. Both source and assembly GitHub repositories are public; no separate ownership-transfer record was found.
- Full diagnosis is preserved in `.plans/debug/2026-09-03-alien-moon-provenance-conflict.md`. Blocking bug `k72.13.4` now gates reopened payload `k72.13.1` and final wiring `k72.13.3`. Renderer environment child was retired with uncommitted partial work preserved in the renderer checkout; no further environment code or release work may proceed until Derrick chooses the rights/scope path.
- Technically, the self-contained GLB is a radius-30 inward-facing unlit sky sphere whose embedded PNG renders correctly from the approved camera. If CC BY use is explicitly approved, compliant author/title/source/license notices and per-asset license handling must replace the inaccurate ownership claim and survive built/released output. Otherwise the copied payload must be removed or replaced with AeroBeat-authored art. Bomb projection and atlas removal remain independently valid.

## Derrick environment replacement direction (2026-09-03)

- Derrick rejected continued use of the discovered Luis Vidal/Sketchfab skybox and directed an AeroBeat-controlled replacement: identify a visually similar local Gaussian splat from the generated-and-purchased splat catalog, capture an equirectangular 360 view, and use that owned image on a lightweight photosphere.
- Derrick explicitly confirmed AeroBeat purchased the candidate splats after generation and may use them commercially without attribution. This confirmation applies to the eventual selected splat/photosphere only; it does not reauthorize the third-party GLB already diagnosed above.
- Candidate-selection gate `k72.13.5` now blocks provenance repair `k72.13.4`. The next action is to inventory and render comparable icy candidates for Derrick selection. After selection, new implementation Beads will own 360 capture/photosphere generation, replacement of the third-party assembly payload/history-forward manifest, and resumed renderer/assembly integration.
- The technically validated but uncommitted renderer environment-owner candidate remains preserved and quarantined at renderer baseline `8e67860` pending adaptation to the selected owned photosphere descriptor. No old moon payload may enter the immutable successor release.

## Owned icy splat candidate review — awaiting Derrick selection (2026-09-03)

- Inventoried and hash-pinned eight nearby purchased candidates from `aerobeat-environment-community` commit `d46ad5b254df4f718fa935490be3b51dc3dffeae`: `luminious-ice-cave`, `icebergs-on-sea-shore`, `snow-mountain-with-lake`, `iceland-waterfall`, `igloo-toon`, `salt-lake`, `salt-lake-2`, and `alpine-river-valley`. Each compressed PLY contains `2,200,000` points and is recorded byte-for-byte in the review manifest.
- Existing Godot/GDGS GPU pipeline rendered all eight from the same `[0,0,4] → [0,0,0]` camera at RGB `960×540` on Vulkan Forward+ / RTX 3080. The review package contains exactly eight individual PNGs, one labeled `1920×648` contact sheet, and one bounded manifest; no PLY or absolute local path was copied into assembly review evidence.
- Contact sheet SHA-256 is `71550a30e8c8abd77052530f979a4b9ff96a8fb5bb08e7e8e10dbaf10e7561ad`. Parent visually inspected it: candidate 1 is a saturated blue crystalline alien ice cave, candidates 2/3 are realistic shore/glacier ice, candidate 5 is a bright stylized ice world, and the remaining candidates are less icy comparators.
- Headless preview capture was rejected after timing out awaiting a draw event; visible Vulkan capture succeeded. A first background batch saved its image but failed to exit and was retired as managed job `bash-48`; subsequent visible one-shot captures completed normally. This affects review automation only, not splat content.
- Gate `k72.13.5` remains in progress until Derrick identifies the source candidate and any preferred 360 camera/view direction.

## Derrick splat selection and owned photosphere generation — PASS (2026-09-03)

- Derrick explicitly selected candidate 1, `luminious-ice-cave`, from the eight-image contact sheet. Selection gate `k72.13.5` closed PASS. The pinned source is `55,619,663` bytes / `2,200,000` points / SHA-256 `a5732239888a0c6967d85693b504248733dbd48e7981cf39a348c32badc3c56a`.
- Environment commit `2a3072ff21236ee3d47c3ff3eb813d85eb1ef6c2` added reusable visible-Vulkan Godot/GDGS six-face capture tooling and deterministic Pillow cubemap-to-equirect stitching. One splat load at viewpoint `[0,0,4]`, with selected preview direction `−Z` centered, produced an RGB `4096×2048` JPEG of `2,210,289` bytes and SHA-256 `ff142b3ce3d3509ab3cfafcfc6a8cc2d3b0ff737852072d3a7aea8075478eed5`.
- Parent inspected the downscaled equirect panorama and cubemap cross: all six directions contain coherent saturated blue crystalline ice-cave content; forward view retains the selected corridor; up/down faces provide ceiling/floor coverage. Validation passes seam mean `3.4544`, seam p95 `12.6667`, top pole spread `1.9617`, and bottom pole spread `1.4678`, all within pinned thresholds.
- The per-asset manifest records Derrick’s commercial-control/no-attribution statement, expressly excludes this derivative from the defective root CC BY-NC text, and does not claim broader public sublicensing. It also records the truthful reproducibility limit: fixed face bytes stitch deterministically, but fresh GPU splat captures are not claimed byte-deterministic and full-resolution intermediate faces are not retained.
- Photosphere Bead `k72.13.6` closed PASS at clean public environment `main == origin/main == 2a3072f`. Reopened assembly payload `k72.13.1` and preserved renderer lifecycle `k72.13.2` are now eligible to replace the rejected third-party GLB with this owned JPEG/photosphere contract.

## Owned photosphere renderer and assembly foundations — PASS (2026-09-03)

- Renderer commit `aaebf80739164dcfc65463856a98cddda61f0ad4` adapted the preserved lifecycle owner to the exact owned JPEG descriptor. It verifies same-origin URL, redirect/credential/fragment/MIME/bytes/hash/dimensions; decodes locally; and renders one radius-30 procedural UV sphere with 16×32 bands / `1,024` triangles, unlit emissive texture, front-face culling, no depth test/write, and `−Z` source center camera-forward. It packages no environment payload.
- Renderer validation passed unit/browser/pack and actual Chromium orientation, visibility, fallback, context/reconnect/destroy and multi-instance tests. Environment pixels changed in `329,148` positions; hide/reshow was byte-identical; correct orientation scored `104.36` against mirrored/inverted `418.73`; fixed camera/gameplay coordinates were unchanged. `k72.13.2` closed PASS at clean `main == origin/main == aaebf80`.
- Assembly commit `9e32bd305fb1a7797b14e802eb8f94e5e51939b3` removed the rejected third-party GLB/config/manifest from current source and replaced them with exact owned photosphere JPEG/config/manifest under version `1.0.0`. Current `src/` and `assets/` contain no rejected asset/hash/author/source reference. The internal descriptor matches renderer `aaebf80`.
- Assembly sync/validation pins environment source commit `2a3072f` and the exact three-file inventory; release fingerprint now covers assembly assets plus the renderer’s 17 gameplay files. Vite build emitted exact JPEG and all seven approved gameplay GLBs. Unit/build/pack validation passed; Flow bomb and atlas-removal changes remain intact. `k72.13.1` closed PASS.
- Provenance blocker `k72.13.4` closed as resolved: no old moon payload may enter current builds or the successor release, while explanatory plan/debug history remains. Final wiring `k72.13.3` is eligible to connect the descriptor and computed Aero/Camera visibility lifecycle.

## Tasks 4–5 integrated candidate (2026-09-03)

- Assembly commit `e59df98daf34ca471fb1d8c8f850c11296fbcdc2` supplies the frozen owned photosphere descriptor exactly once before renderer attach. Computed composite mode drives visibility: Aero shows the resident photosphere; explicit or forced Camera hides it and clears transparently without re-fetch/redecode or mutating the stored Aero preference.
- Coder validation passed direct and real cross-origin iframe, portrait/landscape, requested DPR1/3, exact Aero framebuffer restoration after Camera, context/reconnect/destroy, and independent instances. Flow bomb/no-feedback, arc/burst omission, walls, three track roots, dual shields, sphere cursors, feedback, coordinates, and gameplay pixels passed without console/page/WebGL noise.
- Vite build emitted the exact `2,210,289`-byte owned JPEG plus config/manifest and all seven approved gameplay GLBs. No source PLY, cubemap faces, obsolete atlas, or rejected third-party payload was emitted. Dry pack contains 131 files; nonfinalizing fingerprint is `5ad6c8662f36efb07d76e261012eebff15434665d0c83e776ad9ffc21cdbb944`.
- Wiring Bead `k72.13.3` closed. Parent `k72.13` remains in progress for independent cross-mode matrix QA `k72.14`; no successor release or Derrick physical approval is implied.

## Tasks 4–5 independent cross-mode matrix QA — PASS (2026-09-03)

- QA started from exact clean pins: assembly `main == origin/main == 0e8fd4568722f4d74faa847cf6702964cd9bec1e`, renderer `main == origin/main == aaebf80739164dcfc65463856a98cddda61f0ad4`, environment `main == origin/main == 2a3072ff21236ee3d47c3ff3eb813d85eb1ef6c2`, and gameplay assets `main == origin/main == 0ed97676a0a816b797b12b9f8d19a9d281b9da03`. The assembly dependency is an actual symlink to the pinned renderer checkout and its `package.json` bytes match exactly. QA read the four root READMEs, this complete plan/spec/debug set, renderer decision `0004`, Tasks 4–5 diffs, and Beads `k72.13`, `k72.13.1/.2/.3/.4/.6`, and `k72.14` before validation.
- Re-ran the environment source validator at the pinned source commit. It passed with source PLY `55,619,663` bytes / `2,200,000` points / SHA-256 `a5732239888a0c6967d85693b504248733dbd48e7981cf39a348c32badc3c56a`; canonical RGB JPEG `4096×2048`, `2,210,289` bytes / `ff142b3ce3d3509ab3cfafcfc6a8cc2d3b0ff737852072d3a7aea8075478eed5`; config `216` bytes / `d415e7de8cdc9c78cfc2d3261b9f50a0d9cb626fe8e368bec43de2c8e686fb42`; and manifest `4,869` bytes / `524c7a5623dfbafb65590a9b3b78dc894d4341165d564ac267d470f03acf7e80`. Independently inspected the panorama preview, labeled cubemap cross, stitch report, and rights manifest. Orientation remains zero yaw/pitch/roll, center `−Z`, right quarter `+X`, left quarter `−X`, seam `+Z`, top/up `+Y`; seam mean `3.4544270833 < 5`, seam p95 `12.6666666667 < 16`, top pole spread `1.9617457577 < 3`, and bottom pole spread `1.4677751021 < 3`. Rights remain narrowly tied to Derrick's purchased/generated commercial-control statement, no attribution requirement, root-license exception, and no broader public-sublicensing claim; transient full-resolution face bytes are truthfully not reproducible from retained hashes.
- Verified the corrective assembly diff deletes the rejected Luis Vidal/Sketchfab alien-moon GLB/config/manifest and descriptor. Current `src/`, `assets/`, fresh `dist/`, and dry-run package/build payload contain no rejected GLB, author/source/model identity, PLY, cubemap face, source `.blend`, old gameplay atlas, or external gameplay/environment fetch URL. Only explanatory plan/debug/candidate-history references remain. The whole application still intentionally contains unrelated BeatSaver/MediaPipe network endpoints; this QA makes no false whole-app offline claim.
- Renderer gates passed: `npm test`; full `npm run test:browser`; and `npm pack --dry-run --json`. The dry run contains exactly `39` files, including the exact `17`-file gameplay release and no environment payload/source/review/old release, at `68,108` packed / `227,840` unpacked bytes and SHA-1 `0a6b14f81baae0b99f1daedf77e52566a26200ce`. Actual PlayCanvas Chromium evidence decoded the real JPEG, verified same-origin/credential/fragment/redirect/MIME/byte/hash/dimension gates and loading/error gradient fallback, and rendered one radius-30, `16×32`-band / `1,024`-triangle procedural sphere with direct non-mirrored `−Z`/`+Y` UV orientation, front-face culling, unlit/emissive opaque material, frustum culling disabled, depth test/write disabled, and Skybox-layer ordering. Environment pixels changed at `329,148` positions; hide/reshow pixels were byte-identical; orientation error favored correct `104.36` over mirrored/inverted `418.73`; and gameplay remained visible through world `z=−72` without camera/coordinate mutation.
- Renderer lifecycle/adversarial coverage passed: hidden residency without refetch, stale fetch/decode rejection, fresh context-restoration generations, detach/reconnect/destroy abort and GPU/entity/image/container cleanup, two independent applications/instances, explicit gameplay fallback only on loader/context errors, and zero engine RAF, autonomous timers, unexpected console/page/WebGL noise, or public pixel/media/URL leakage.
- Assembly gates passed: `npm test`; a complete managed-background `npm run test:browser` with exit `0`; `npm run build`; `npm pack --dry-run --json`; and release fingerprint recomputation. A first foreground browser invocation reached 17 of 24 mode/context rows before the harness's fixed `600 s` command cap terminated it; the required unbounded managed rerun then completed every suite and all 24 rows, so this was executor duration rather than a product/test defect. The pre-QA-metadata dry run contained the expected `131` files at `9,846,180` packed / `11,127,536` unpacked bytes. The deterministic nonfinalizing release fingerprint is `5ad6c8662f36efb07d76e261012eebff15434665d0c83e776ad9ffc21cdbb944` over `158` inputs, including all `17` renderer gameplay-release files.
- Fresh actual-pixel assembly matrix passed all eight embedding/view/DPR contexts: direct and real cross-origin iframe at portrait `390×844` and landscape `844×390`, requested DPR `1` and `3` (renderer cap truthfully applied DPR `2`, producing `780×1688` / `1688×780` framebuffers). Every context exercised Flow, Boxing Lanes, and Boxing Grid through actual UI selection/Test startup, distinct scene and framebuffer checksums, full-opacity/background partition, right-handed camera reset/legacy-yaw negative proof, pause/resume, responsive safe areas, stable surfaces, and zero console/page noise. Direct/iframe camera-compositor screenshots additionally passed Aero, explicit Camera, calibration/tracking-forced Camera, preference-preserving return to Aero, mirrored camera coverage, hide/show byte identity without reload, reconnect, context recovery, and exactly one renderer graph. Deterministic private camera-pose downloads were byte-identical across the matrix at SHA-256 `b04663252307ae8be9ba0f0f542a88f6660b6bc09fbe88c658f098b103b67bee`. Screenshots were decoded and inspected in-memory; no redundant repo screenshot payload was retained.
- Gameplay truth passed in renderer and every relevant assembly presentation: canonical directional arrow / directionless circle / urchin bomb mapping; exactly two independently instantiated but source/material/scale/orientation/Y/Z-identical non-mirrored shields with X-only placement difference; three truthful depth-writing nose/left-wrist/right-wrist marker spheres; red/yellow/green timing rows; role-core-only white success tint; exact `80 ms` resolved removal; stationary crossing-coordinate `Great` white/dark and `Miss` red/white feedback with `180 ms` hold + `170 ms` fade (`350 ms` total), newest-four cap, and obstacle/bomb exclusions; complete interval wall endpoints with unit X/Y; three unscaled track segments over `z=0…−72`; deterministic transparent/opaque/feedback depth order; and conventional right-handed camera/handedness. Flow's valid authored bomb is visible in the same bounded approach window but creates no scoring/judgement/feedback; invalid bombs, arcs, and bursts remain omitted rather than remapped.
- Strict gameplay-source validation passed all seven clean Blender/GLB imports and budgets. Fresh build output contains exactly the owned JPEG/config/manifest plus seven GLBs with these exact GLB SHA-256 values: circle `0cd90824251657825a9c84499a43a59ce7e39112e32f9903b1e1836c2466945e`, marker sphere `be91826b05a9643a3923ad7d3af9d2e0e0c22bb29bac4bfa97a9758b43700bd3`, bomb `63d61feff050c284f2e3a228d345ea794c25bab48ab56cd4801d554c923def85`, arrow `2355b83eaa954354685ac92f91d72558002d7439aea906494a2f20056d8056ce`, shield `50eb6b6835ec6f2acfceb866d53a8f3f8b424d48259ac59c74a39f5bab731eee`, track `802088bad3cc9174e7a94ae769ec5cdbf28b3942c08dc2b369216b5aeb1cfee6`, and wall `ed6511c1ce1c196c9b735e40c0b4bf96375cd2f9b9dd0723c5fe5c5209b5d27a`. Source release, renderer package, assembly link, and built GLB bytes match exactly; inventory/proof hashes remain `1a5b66f543bae940b8bb789e9ab9979d073663b5f6ff12382e08f4ad10c0ff1b` / `90dcbe52b35d2ec11a01784a96f195b5cd01ac141000886cb950c74864eec288`.
- No timing, scoring, event-ID, host API, iframe-origin/source/message-bound, lifecycle, or privacy regression was found. QA disposition is PASS: close only `k72.14`; keep `k72.13` in progress for final audit `k72.15`. This is independent software QA evidence only and does not claim Derrick physical review or create/approve an immutable successor release.

## First final audit — FAIL; rejected-source remediation (2026-09-03)

- Final audit reproduced all gameplay-asset, renderer, owned-photosphere, assembly, build, package, fingerprint, and immutable `0.0.34` evidence but found one blocking residual: the rejected third-party GLB/config still existed as current tracked files in public `aerobeat-environment-community`, despite no longer entering assembly source/build/package. Audit created bug `wom`; `k72.13`/`k72.15` remained in progress and `k72.16` remained blocked. Exact successor remains `0.0.35`.
- Environment remediation commit `543686003c36eb0ddface684925b373260d8f1d9` removed the two tracked source files plus exact ignored/generated PNG/import/cache/editor derivatives without rewriting Git history. Current filesystem glob has no file with the rejected stem. Selected PLY, owned JPEG/config/manifest, four tools, and three review artifacts remain byte-identical to `2a3072f` at aggregate SHA-256 `68608299b5e5b5cea43cd680bb4247f3c7af50942c8be89f41c5af08944101b2`.
- Remediation validation passed the focused photosphere gate, Godot 4.6.2 import/parse, `33/33` GUT tests / `193` assertions, clean generated-addon state, diff check, and clean local/origin/public main. Existing nonfatal `glb_test.tscn` invalid-UID/text-path fallback warning remains unchanged. Bug `wom` and final audit `k72.15` remain open/in progress for independent re-audit.

## Final re-audit — two remediation defects (2026-09-03)

- Re-audit confirmed the rejected source and generated derivatives are absent across `1,064` environment files / `2,798,871,529` bytes outside allowed negative-denylist guard strings, and reproduced every prior asset/renderer/build/package/Bead/release PASS. It nevertheless remained FAIL for two release-readiness defects.
- Assembly source sync still pinned environment commit `2a3072f` after removal-only commit `5436860`; fresh `npm test` truthfully failed `environment source repository HEAD drifted` even though the selected photosphere files were byte-identical. Bug `gyo` tracks the exact pin refresh.
- Independent contention exposed nondeterminism in `validate-mobile-gameplay-menu.js`: calibration could enter `tracking_lost`, show `T-pose` instead of required `Hold T-pose`, or time out before countdown. Parent reproduced three failures in three concurrent runs while an isolated run passed. Debug record `2026-09-03-mobile-calibration-validator-nondeterminism.md` traces the static mock pose timestamp against the production 100 ms stale-frame threshold; bug `5oq` tracks a production-like 15 Hz fresh-frame mock and stress verification. No production threshold/assertion weakening is approved.
- `wom`, `k72.13`, and `k72.15` remain in progress; `k72.16` remains blocked. Current fingerprint remains `5ad6c8662f36efb07d76e261012eebff15434665d0c83e776ad9ffc21cdbb944` until the source pin changes; exact successor remains `0.0.35`.

## Re-audit remediation candidate — PASS (2026-09-03)

- Assembly sync and README now pin exact clean environment removal commit `543686003c36eb0ddface684925b373260d8f1d9`; verify confirms the same exact three selected payload files and hashes. Since the release fingerprint covers runtime `src/`/`assets/` rather than source-policy scripts, its correct value remains `5ad6c8662f36efb07d76e261012eebff15434665d0c83e776ad9ffc21cdbb944` across 158 inputs.
- Debugging reproduced and separated three browser-fixture scheduler races: stale static pose timestamps, arbitrary 80 ms pose-delivery sleeps, and screenshot work consuming subsequent countdown states. The corrected test-only seam emits fresh poses at 15 Hz, synchronously consumes explicit synthetic pose frames, registers recovery observation before audio unfreeze, and requires every numeric cue to remain unchanged for a cue-specific 850 ms pre-freeze proof before stopping for a painted screenshot. Production frame/input/countdown source, 100 ms stale threshold, timeout bounds, `Hold T-pose`, ordered `3,2,1`, ≥800 ms dwell, contrast, and gameplay assertions are unchanged.
- An isolated mobile validator plus two fresh three-process contention rounds passed `7/7` total. Independent review first caught that immediate freezing could mask a duration regression; after cue-specific 850 ms pre-freeze proof became mandatory for both initial and recovery countdowns, re-review closed the finding with no new findings.
- Fresh environment verify, `npm test`, complete `npm run test:browser` 24-row matrix, build, 132-file dry pack (`9,856,034` packed / `11,156,527` unpacked bytes; SHA-1 `b91f07d5bf93ebb0c0860ca9b7de12fb76fea37e`), release-pack-policy, and diff checks passed. An orchestration command initially called nonexistent `check:release-pack-policy` after all earlier gates; this failed honestly and was corrected by running the actual `test:release-pack-policy`, which passed.
- Bugs `gyo` and `5oq` closed PASS. Provenance cleanup bug `wom`, parent `k72.13`, and final audit `k72.15` remain in progress for independent re-audit; no successor release or physical approval is claimed.

## Independent final certification re-audit — FAIL on release immutability tooling (2026-09-03)

- Audited exact clean pushed source heads assembly `a0720c78983b80b641a02514108a62e0d64e88d1`, renderer `aaebf80739164dcfc65463856a98cddda61f0ad4`, environment-community `543686003c36eb0ddface684925b373260d8f1d9`, and gameplay assets `0ed97676a0a816b797b12b9f8d19a9d281b9da03`; local `HEAD`, local `origin/main`, and public `refs/heads/main` matched for all four before audit metadata. Read the complete plan/debug tail and prior `k72.15`/`wom`/`gyo`/`5oq` evidence.
- Rejected-source remediation independently passes. An exhaustive current environment scan covered `1,064` regular files / `2,798,871,529` bytes and found no rejected stem/path, historical GLB hash `40e38a7b…`, config hash `1e50a941…`, derived PNG hash `7654e945…`, or unique Luis Vidal/Sketchfab/model-id bytes outside the intentional photosphere validator denylist. The selected 11-file photosphere set is byte-identical to `2a3072f` with aggregate SHA-256 `68608299b5e5b5cea43cd680bb4247f3c7af50942c8be89f41c5af08944101b2`; source PLY remains `a5732239…`, and JPEG/config/manifest remain `ff142b3c…` / `d415e7de…` / `524c7a56…`. Focused validation reproduced seam/pole metrics `3.4544270833 / 12.6666666667 / 1.9617457577 / 1.4677751021`.
- Assembly pin/build/package remediation independently passes. The source verifier binds exact clean environment `5436860`; assembly `src/`/`assets/`, the fresh 16-file Vite output, and all 132 dry-pack members contain none of the three rejected asset byte identities or rejected path. Fresh output contains exactly the owned three-file environment payload and seven gameplay GLBs with the previously audited hashes. The dry pack is `9,857,224` packed / `11,159,715` unpacked bytes, SHA-1 `147924e79e8d53e481f7e2ee4b046480ed32edbb`; fingerprint is unchanged and correct at `5ad6c8662f36efb07d76e261012eebff15434665d0c83e776ad9ffc21cdbb944` over `158` inputs.
- Mobile correction independently passes. Two fresh three-process contention rounds passed `6/6`; the full browser gate also passed. Each numeric cue must remain the same cue for its own independently scheduled `850 ms` probe; only then does the callback mark proof and stop the display loop, and screenshot/style/pixel capture waits for that marker before running. A sub-800 ms regression changes the cue before the probe and cannot obtain the marker. Existing `>=800 ms` dwell assertions and `4,000`/`2,500 ms` waits remain; production `src/`, `100 ms` stale threshold, 15 Hz CV ceiling, countdown timing, and runtime assertions were not changed.
- Full independent gates passed: gameplay `0.0.2` strict validation/finalization and primary-plus-two-build 17-file reproducibility; renderer `npm test`, full actual-PlayCanvas browser matrix, and 39-file dry pack (`68,108` / `227,840`, SHA-1 `0a6b14f81baae0b99f1daedf77e52566a26200ce`); assembly `npm test`, complete 24-row direct/real-iframe portrait/landscape requested-DPR1/3 browser matrix, compositor/cursor/lifecycle/privacy/multi-instance coverage, build, dry pack, and release-pack-policy. Declared local dependency graph is 14 packages / 18 file edges with no cycles; Beads cycles are also empty. Gameplay inventory/proof hashes remain `1a5b66f5…` / `90dcbe52…`; tracked raw `0.0.34` remains exact tree `1c8db5dff7c0136a6e5a22c6622f4a5409511f70` with proof SHA-256 `46ca87869bae0016639277832520eec48031532c5ab509a2cfca36f8fc3d12a2`.
- Final certification nevertheless **FAILS** on newly filed blocker `aerobeat-web-assembly-m92`. `README.md` still falsely calls `0.0.33` the current raw proof, while the immutable predecessor is `0.0.34` and exact successor is `0.0.35`. More importantly, `scripts/build-release.js` derives `release/raw/<current-version>` and unconditionally recursively deletes that directory before building; at current version `0.0.34`, an accidental `npm run build-release` can overwrite the immutable predecessor, and the release-pack-policy suite has no guard for this path. The tracked `0.0.34` Git tree is unchanged, but ignored generated gameplay/environment additions are already present beneath the local raw directory, demonstrating why filesystem target protection and clean-target handling must be fail-closed.
- `m92` is linked as a blocker of `k72.15`; `wom`, `k72.13`, and `k72.15` remain in progress, and `k72.16` remains blocked. The superseded assembly-owned code-license warning is not treated as a blocker under Derrick's explicit Task 6 authorization. No successor was built, no npm/GitHub Release publication occurred, and no Derrick physical review is claimed. Next step is to repair and independently verify `m92` without touching raw `0.0.34`, then repeat final certification; exact successor remains `0.0.35`.

## Append-only release-target remediation — PASS (2026-09-03)

- Release-tooling commit `f1da25c2e84533cec080507e1bf9d088d5876084` removes final-target recursive deletion and atomically claims a fresh version directory with non-recursive exclusive `mkdirSync` before fingerprinting/Vite. Existing targets fail before build; Vite uses `emptyOutDir:false`; a failed build deliberately leaves its claimed directory blocked for explicit inspection rather than automatic deletion.
- A focused regression validator proves existing sentinel byte preservation, one successful absent-target claim, second/concurrent rejection, claim-before-build ordering, no final-target delete path, safe current-version rejection, and future-missing-version test skip. It is part of normal unit and release-policy gates. README now identifies immutable `0.0.34` and exact approved successor `0.0.35`.
- Removed only the ten ignored generated payload files contaminating raw `0.0.34`. Its filesystem now exactly equals 13 tracked files; Git tree remains `1c8db5dff7c0136a6e5a22c6622f4a5409511f70`, proof SHA-256 remains `46ca87869bae0016639277832520eec48031532c5ab509a2cfca36f8fc3d12a2`, and complete 13-file manifest hash remains `680e70a717ffba1636aead431c0ff5edcfe3c7214506188f8794042fdc916c2e`. A safe actual current-version invocation rejects before Vite; raw `0.0.35` remains absent.
- Focused policy, unit, full browser/24-row matrix, dist build, 135-file dry pack (`9,863,031` packed / `11,180,665` unpacked; SHA-1 `e136b52fff57647e4e9cf480d524b612f573608a`), release pack policy, fingerprint, and diff checks passed. Fingerprint is now `4f4fd4fd066ee63eca685c2e321d43325d72c9179d30a3800ee103e67b0405f6` across 158 inputs because the release policy source is part of packaged assembly source.
- Bug `m92` closed PASS. `wom`, `k72.13`, and final audit `k72.15` remain in progress for one final certification; no successor or physical review is claimed.

## Final independent Task 6 readiness certification — PASS (2026-09-03)

- Certified exact clean pushed pre-certification heads assembly `a4ebb3f578a7042e7a0814e3113a620146b2c8ad` (implementation `f1da25c2e84533cec080507e1bf9d088d5876084`), renderer `aaebf80739164dcfc65463856a98cddda61f0ad4`, environment `543686003c36eb0ddface684925b373260d8f1d9`, and gameplay assets `0ed97676a0a816b797b12b9f8d19a9d281b9da03`. Two independent read-only audits plus parent inspection found no defect.
- Append-only policy PASS: exclusive non-recursive target `mkdirSync`, claim before fingerprint/build, `emptyOutDir:false`, no final-target deletion path, safe existing `0.0.34` rejection before Vite, normal-unit and release-pack regression coverage, and accurate README successor policy. Raw `0.0.34` is exactly 13 tracked/filesystem byte-identical files, tree `1c8db5dff7c0136a6e5a22c6622f4a5409511f70`, proof SHA-256 `46ca87869bae0016639277832520eec48031532c5ab509a2cfca36f8fc3d12a2`, canonical manifest aggregate `680e70a717ffba1636aead431c0ff5edcfe3c7214506188f8794042fdc916c2e`; ignored contamination is absent and raw `0.0.35` does not exist.
- Prior PASS remains valid: exhaustive current-filesystem scans found no rejected GLB/config/derived-PNG hash or current rejected path outside intentional denylist/history; exact photosphere hashes remain `ff142b3c…` / `d415e7de…` / `524c7a56…`; gameplay inventory/proof remain `1a5b66f5…` / `90dcbe52…`; renderer unit/full browser/39-file pack, gameplay strict/reproducibility, assembly unit/full 24-row browser/build/135-file dry pack/release policies, exact seven-GLB build payload, privacy/lifecycle/iframe/public-origin and acyclic graph gates all pass. Current fingerprint is `4f4fd4fd066ee63eca685c2e321d43325d72c9179d30a3800ee103e67b0405f6` over 158 inputs; current assembly dry pack is 135 files, 9,863,644 packed / 11,182,625 unpacked bytes, SHA-1 `232d2a937dc66b4896cc57a33ca4394912c42ace`.
- Final disposition: close `wom`, `k72.13`, and `k72.15` PASS; leave `k72.16` open and ready as the next Bead to build exact successor `0.0.35`. No successor, npm publication, GitHub Release, or Derrick physical-review claim was created.

## Independent immutable raw `0.0.35` QA — PASS (2026-09-03)

- QA began from clean exact pushed assembly commit `db50919802cfd89f8aed26ff7de18637c9eff1fc` and read the README, plan/release evidence, proof generator, fingerprint algorithm, append-only target policy, release-pack tooling, and `k72.16` graph. Package, lock root, and proof are exactly `0.0.35`; source fingerprint is `5f1c0f0efe8eb897b77c5cb694995552cfdf1e463b4b0c24db0f128b4ccc9884` over `158` inputs.
- Tracked raw `0.0.35` independently inventories as exactly `20` regular files / `13,878,153` bytes (`13,876,787` before the `1,366`-byte proof), all mode `100644`, Git tree `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, proof SHA-256 `22c41e8bf0630bb6b50523a96ab0e886b399a93c74a7d1e97bb2afe47a43c4ea`, and canonical complete aggregate `a111efba87a4b46502b04a34c3ff0ca102815f090a20fba7cf4ccf29d2d21951`. Proof is raw/unminified, base `/`, with three source maps, MediaPipe-only GPU-WebGL Pose Landmarker Lite float16 `/1/`, Tasks Vision `1.0.1`, standard thresholds, Fast/full/measured, 15 fps, no WASM, and no rejected runtime marker.
- Exact owned photosphere hashes match: config `d415e7de8cdc9c78cfc2d3261b9f50a0d9cb626fe8e368bec43de2c8e686fb42`, JPEG `ff142b3ce3d3509ab3cfafcfc6a8cc2d3b0ff737852072d3a7aea8075478eed5`, manifest `524c7a5623dfbafb65590a9b3b78dc894d4341165d564ac267d470f03acf7e80`. Exact gameplay GLBs match: circle `0cd90824251657825a9c84499a43a59ce7e39112e32f9903b1e1836c2466945e`, marker `be91826b05a9643a3923ad7d3af9d2e0e0c22bb29bac4bfa97a9758b43700bd3`, bomb `63d61feff050c284f2e3a228d345ea794c25bab48ab56cd4801d554c923def85`, arrow `2355b83eaa954354685ac92f91d72558002d7439aea906494a2f20056d8056ce`, shield `50eb6b6835ec6f2acfceb866d53a8f3f8b424d48259ac59c74a39f5bab731eee`, track `802088bad3cc9174e7a94ae769ec5cdbf28b3942c08dc2b369216b5aeb1cfee6`, wall `ed6511c1ce1c196c9b735e40c0b4bf96375cd2f9b9dd0723c5fe5c5209b5d27a`. Inventory/path/content policy found no rejected third-party environment, PLY, cubemap face, `.blend`, PNG/gameplay atlas, non-MediaPipe runtime asset, or external index URL.
- Two isolated ignored depth-matched temp Vite builds used the exact `build-release` options (`emptyOutDir:false`, `minify:false`, sourcemaps, exact output naming, `vite.config.js`, release mode) and independently synthesized the proof with the documented generator algorithm. A and B each produced `20` files / `13,878,153` bytes, fingerprint `5f1c0f0e…`, and proof `22c41e8b…`; recursive comparison proved `A == B == tracked raw 0.0.35` byte-for-byte without invoking or mutating the final target. Temp build outputs were removed. A preliminary outside-repository temp-path probe was correctly discarded because source-map relative paths encode output depth; the two authoritative reproductions used isolated ignored paths at the final target's depth.
- Actual `npm run build-release` against existing `0.0.35` rejected on the immutable-target guard before Vite. Complete before/after byte snapshots were identical for both releases. Raw `0.0.34` remains exactly `13` files, tree `1c8db5dff7c0136a6e5a22c6622f4a5409511f70`, proof `46ca87869bae0016639277832520eec48031532c5ab509a2cfca36f8fc3d12a2`, canonical aggregate `680e70a717ffba1636aead431c0ff5edcfe3c7214506188f8794042fdc916c2e`; raw `0.0.35` retained the exact identities above.
- Focused release-target and owned-environment policies, `npm test`, `npm run test:release-pack-policy`, and full `npm run test:browser` all passed. Browser coverage completed all `24` direct/real-iframe portrait/landscape requested-DPR1/3 Flow/Boxing Lanes/Boxing Grid rows plus compositor, cursor, lifecycle, privacy, audio, import/library, and zero-noise gates. A clean detached exact-commit dry pack normalized `285` tracked files to `0644` and produced `135` members, `9,864,406` packed / `11,186,519` unpacked bytes, SHA-1 `5d4381d677abff75586f2770d42b3fe1bd4526d3`, SHA-512 `Ln1UKRCaLZadc1vHLUXdLCBLcBDycubhDvbkSgCctdg+4lUY48hQ2QtAiryetuS+xFziRQ13E6Oi8k+dDRZr5A==`.
- Ephemeral local HTTP/Chromium raw smoke passed both direct and cross-origin iframe at `390×844`: connected exact-size canvas, owned photosphere ready/visible, all seven GLBs ready and requested with HTTP 200, Camera then Aero selection, menu close/re-show with Visuals controls intact, identical non-empty canvas PNG evidence (`64,765` bytes; SHA-256 `9f2ab54f10852028bfb6b3ff1c7cf5722d5ab4e3d489caa4368dd93385888d4b`), zero unexpected console/page/request failures, and zero network escape. Both temporary servers were stopped and all smoke/temp artifacts removed.
- README correctly identifies current raw `0.0.35`. Local and public `origin/main` were exact `db50919802cfd89f8aed26ff7de18637c9eff1fc` before QA metadata; no matching local/remote tag, GitHub Release, or npm `0.0.35` publication exists. QA disposition: close only `aerobeat-web-assembly-k72.16.1` PASS; retain parent `k72.16` in progress and `k72.16.2` open for independent final audit. This is automated QA evidence only and makes no physical-review claim.

## Final immutable raw `0.0.35` release audit — PASS (2026-09-03)

- Final audit began from exact clean pushed assembly `8918846b4cf41f4b453b8c8fa4d734afa209b3f0` and independently reviewed the release plan, raw proof, fingerprint and proof generators, append-only release-target tool/policy, release-pack policy surface, builder commit `db50919802cfd89f8aed26ff7de18637c9eff1fc`, and the complete `k72.16` / `.16.1` / `.16.2` evidence chain. The only post-builder changes were QA plan/Beads metadata. Exact clean pushed sibling heads remain renderer `aaebf80739164dcfc65463856a98cddda61f0ad4`, environment `543686003c36eb0ddface684925b373260d8f1d9`, and gameplay assets `0ed97676a0a816b797b12b9f8d19a9d281b9da03`.
- Package and lock root are exactly `0.0.35`; independent source-fingerprint readback is `5f1c0f0efe8eb897b77c5cb694995552cfdf1e463b4b0c24db0f128b4ccc9884` across `158` inputs. Raw `0.0.35` is exactly `20` tracked and filesystem regular files / `13,878,153` bytes, every working byte equals its Git blob, every Git mode is `100644` (the checkout presents the same non-executable files as owner-only `0600`), tree `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, proof SHA-256 `22c41e8bf0630bb6b50523a96ab0e886b399a93c74a7d1e97bb2afe47a43c4ea`, and reviewed canonical aggregate `a111efba87a4b46502b04a34c3ff0ca102815f090a20fba7cf4ccf29d2d21951`.
- Proof/schema and raw policy PASS: raw/unminified, base `/`, three parseable source maps, four JavaScript assets, no WASM, locked MediaPipe GPU-WebGL Pose Landmarker Lite float16 `/1/`, Tasks Vision `1.0.1`, `0.5 / 0.5 / 0.5`, Fast/full/measured, 15 fps, no forbidden backend/marker, no rejected environment/PLY/cubemap/`.blend`/atlas path, and no external runtime asset URL. Exact owned photosphere config/JPEG/manifest hashes remain `d415e7de…` / `ff142b3c…` / `524c7a56…`; exact seven GLBs remain circle `0cd90824…`, marker `be91826b…`, bomb `63d61fef…`, arrow `2355b83e…`, shield `50eb6b68…`, track `802088ba…`, and wall `ed6511c1…`.
- QA's two isolated depth-matched reproduction records were reviewed: both independently produced the same `20` files / `13,878,153` bytes, fingerprint `5f1c0f0e…`, proof `22c41e8b…`, and byte-for-byte equality with each other and tracked raw `0.0.35`; their temporary outputs were intentionally removed. Focused audit reruns passed release-target policy and owned-environment verification. A fresh actual `npm run build-release` rejected the existing `0.0.35` target before Vite and preserved a clean worktree. Raw `0.0.34` remains exactly `13` tracked/filesystem byte-identical non-executable files / `11,565,989` bytes, Git tree `1c8db5dff7c0136a6e5a22c6622f4a5409511f70`, proof `46ca87869bae0016639277832520eec48031532c5ab509a2cfca36f8fc3d12a2`, and reviewed canonical aggregate `680e70a717ffba1636aead431c0ff5edcfe3c7214506188f8794042fdc916c2e`.
- A fresh dry pack contained `135` members and zero `release/` paths. Local and remote `0.0.35` tags are absent, GitHub Releases list is empty, and npm returns `E404` for `@aerobeat/web-assembly@0.0.35`. README and plan truth identify `0.0.35`; no source, release byte, tag, npm publication, or GitHub Release was changed by this audit.
- The existing parent-managed server remains PID `1825864` with exact command `python3 -m http.server 5173 --bind 127.0.0.1 --directory release/raw/0.0.35`; its process cwd is this canonical assembly checkout. HTTP readbacks for index, proof, JPEG, and bomb GLB were byte-identical to tracked raw files. Fresh direct Chromium at `390×844` rendered the connected exact-size `390×844` canvas, fetched the photosphere and all seven GLBs with HTTP 200, had no request failures, page errors, or unexpected console warning/error under the repository's documented GL-driver filter, and produced a non-empty rendered capture. The immutable physical-review URL remains `http://127.0.0.1:5173/`.
- Final disposition: `aerobeat-web-assembly-k72.16.2` and parent `k72.16` are closed PASS after recording this evidence. `k72.17` remains open and ready for Derrick's physical review. This audit does **not** claim Derrick approval or physical-review PASS.

## Derrick physical review of raw `0.0.35` — NEEDS VISUAL REVISIONS (2026-09-03)

- Derrick reviewed the immutable release at the existing raw URL and requested: make arrows opaque because they are hard to see; increase glass-floor opacity; correct the mismatch between visually good red colliders and rectangular visible grid cells so all grid cells are square; add a chevron to collapse/open the top-left controls; and add photosphere selection/alignment controls for rotation and scale with per-environment JSON save/load.
- Derrick also requested photosphere conversion of the seven remaining purchased splat candidates and a dropdown to test all eight. Weak conversions should remain available for comparison rather than being represented as guaranteed winners. Photospheres are preferred over Gaussian splat runtime for the current 2D-device use case because the viewpoint does not move through the source and the sphere/texture path is cheaper.
- Raw `0.0.35` remains immutable and approved only as automated release evidence, not as physical visual approval. `k72.17` is in progress and now depends on the next audited release. Exact next raw successor is `0.0.36`; no current raw file will be overwritten.
- Revision graph: asset `0.0.3` coder `k72.21` → QA `k72.22` → audit `k72.23`; square-grid debug/fix `k72.24` → QA `k72.25`; seven additional owned photospheres `k72.26` → QA `k72.27`; catalog/collapsible alignment controls `k72.28` depends on all three lanes → full matrix `k72.29` → final audit `k72.30` → immutable `0.0.36` `k72.31` → release QA/audit `k72.32` → physical gate `k72.17`.
- Controls remain private Visual Test tooling: no host/iframe/public snapshot extension. Each photosphere owns one bounded JSON config with identity plus position/rotation/scale-compatible transform semantics for future splat parity. Runtime Gaussian splat integration is explicitly deferred; current work remains offline splat-to-photosphere derivation plus low-geometry sphere textures.
- Parallel coder launches were verified in the direct-child registry: gameplay asset coder `8ebeb864-2ff0-4036-b662-1faa16a3cb3b` (`k72.21`), environment photosphere coder `f69102b8-f44b-40f5-aecd-223ee7980151` (`k72.26`), and square-grid diagnostician/coder `26ff156c-3d4e-4c1f-841b-8a0cbb2789c0` (`k72.24`) all reported `running`. Each owns a disjoint repository/lane; only the grid coder may edit assembly implementation and is instructed not to edit this active plan. Architecture researcher `03edae4e-bd8b-4e61-840c-df98e59704ab` was later verified `running` on design Bead `k72.33`; it is read-only and `k72.28` now depends on its bounded implementation contract.

### Photosphere catalog/control contract for `k72.28`

- Renderer accepts a bounded assembly-owned equirectangular descriptor generically rather than hard-coding one JPEG identity: lowercase asset id, package-local same-origin URL, JPEG MIME, bounded positive bytes, exact SHA-256, RGB `4096×2048`, projection, center-forward `−Z`, world-up `+Y`, and strict transform config. Assembly owns the exact eight-entry hash catalog and Vite emissions; renderer remains payload-free.
- One JSON config belongs to exactly one environment id. Schema fields are version/id/projection plus `transform.position`, `transform.rotationDegrees`, and `transform.scale`; vectors are three finite numbers, scale is positive/bounded, and load rejects extra keys or mismatched selected id. The approved physical-revision instruction supersedes the earlier schema-only position note: Photosphere UI exposes bounded position X/Y/Z, yaw/pitch/roll, and uniform sphere-radius scale controls.
- The photosphere node stays camera-anchored at the approved gameplay camera origin plus configured local position. Rotation applies pitch/X, yaw/Y, roll/Z. Uniform scale changes the bounded sphere radius (minimum keeps the camera inside); the UI must truthfully avoid promising zoom when a centered inside-sphere scale has no angular effect.
- Selection and transforms are per-instance private Visual Test state. Save is an explicit-user-action JSON download; load uses an explicit local file picker and strict parse/validation. Neither file bytes, selection, transform, URL, hash, nor catalog extends host events, iframe messages, public snapshots, localStorage, or network fetch. Switching replaces one verified texture generation-safely; no eight-texture GPU cache is allowed on mobile.
- A 44px-accessible chevron collapses/reopens the complete top-left Visual Test control surface without disconnecting renderer/CV/audio or changing gameplay state. Default is open; collapse state is per connected instance and does not alter environment configuration.
- Architecture research `k72.33` completed PASS with no code changes. The full Bead comment maps the renderer owner/facade/decision/validators and assembly catalog/index/Vite/sync/component/shell/privacy validators. It confirms strict `aerobeat/environment_asset_config` v1 JSON, trusted explicit-user-action files bounded to 16 KiB, one resident texture, stale-generation abort/disposal, gradient fallback on replacement failure, camera-translation anchoring without camera-rotation inheritance, transform-before-descriptor ordering, per-connection per-id memory, computed Camera mode, and reconnect/context/destroy/multi-instance tests. It explicitly preserves the truth that centered sphere scale changes radius but not angular zoom. Bead `k72.33` is closed and pushed; implementation `k72.28` remains gated by the three active asset/grid/environment lanes.
- Square-grid diagnosis is complete before implementation in `.plans/debug/2026-09-03-square-grid-collider-mismatch.md`: all 16 raw/source, direct/iframe, portrait/landscape, DPR1/3 cases reproduce the same `1.5×1.05` cell (`1.4286` ratio), while the approved wall GLB is near-square `1.8×1.9`. Root cause is the legacy anisotropic renderer transform—column pitch `1.6`, row pitch `1.2`, matching cursor mapping `6.4×3.6`—not CSS, DPR, iframe, or camera projection. The scoped implementation replaces those literals with a single equal-unit 4×3 transform and derives square inset cells/cursors from it while preserving cell IDs, scoring/timing, camera, wall asset and interval truth. Renderer commit `e5130e01edd511911e39e27126e856c67ae56ece` and assembly evidence/gate commit `ec274b3058ba3612b586c092d06747b1192609f3` are clean and public. The dedicated eight-context direct/real-iframe portrait/landscape DPR1/3 matrix passed projected/framebuffer square error `<=1 px`; wall stays `1.8×1.9×1`. Two overlong unrelated full-browser attempts were interrupted and their process groups reconciled; the scoped suite passed without absorbing the environment source pin owned by `k72.28`. Independent QA `c7665c52-d7ea-4c67-b133-d44cedc135e0` independently reproduced the root cause and passed/closed `k72.25`: all 16 before cases retained the `1.429` ratio, the fresh eight-context after matrix had projected neutral error `0.0350–0.0758 px`, blocked error `0.0495–0.1070 px`, framebuffer boxes `180×180`, `83×84`, or `83×83`, direct/iframe hashes matched, cursor corners/centers aligned, wall/interval/scoring/camera semantics stayed unchanged, and scoped renderer/assembly/build/pack/release gates passed. The only full-suite stop is the expected environment source pin owned by `k72.28`; no process remained. `k72.24` stays in progress for aggregate integration audit.
- Gameplay asset coder `8ebeb864-2ff0-4036-b662-1faa16a3cb3b` completed immutable full-set release `0.0.3` at public pushed commit `2addf3c3368b43979d4709a0eb12b02baa5c6128`; raw tree `aa37bf534cc592a4057127876d567eadc3496f49`, proof SHA-256 `287adc43a0456782044f0fd7601efd7b5087342972d9da4525923598754b1efc`, inventory SHA-256 `69b88d38113a56061dfc0ea5e92ec51a0b181fcade6a99e1dcc5df1baecdde03`, set SHA-256 `a1ca1d4acccf3e1e53e11d6e93c5cf8fcfdcba73eeaf7eb6347eb74f10589288`. Arrow is OPAQUE/alpha 1/depth-write with tint base and white rim; track alpha is `0.52` versus `0.20`; five other GLBs/sources and all predecessor release/review trees are byte-identical. Minimal repository GLB reconstruction removed an invalid optional NumPy review dependency; strict validation, clean imports, negative guards and two clean reproducibility builds passed. Independent QA `524ee2b7-5564-4620-8983-9f2b2d6d67f9` was verified `running` on `k72.22`; coder `k72.21` stays in progress until audit `k72.23`.
- Photosphere coder `f69102b8-f44b-40f5-aecd-223ee7980151` completed the exact owned eight-entry catalog at clean public commit `e962fe2a8a48b7e2019d1a55c337abeb6ffd24d4`; image-tree identity `0c6fc3ea4f2c5f2c2e9ea7b63d53d7ee2eb028cb`, catalog SHA-256 `bf68f00eb4d346a98958a4b962c2b37351e1ca21c0f96bcb101e008c681c7e93`, review-manifest SHA-256 `91d0575669a46dfcf5435c057574bb40653c0675ef566f4dc8ca0614ebd1cd98`. All eight are RGB 4096×2048 q92/4:4:4, use strict per-id v1 JSON, and pass hash/source/AABB/orientation/seam/pole/Godot/GUT checks. Six are strong/strong-comparison; Snow Mountain and Igloo remain truthfully labeled comparison-with-artifacts. Protected luminous JPEG/YAML/manifest/review bytes remain unchanged. Independent QA `305a9287-5690-42ed-a346-6bcc2fe46303` passed and closed `k72.27`: exact public commit/tree/catalog/review hashes, eight source identities and 2.2m-point/AABB/toolchain records, JPEG q92/4:4:4 properties, strict configs, protected luminous bytes, orientation, all metrics, two isolated 65-file finalizations with aggregate `110f92a1509a2d9a5b30c903e8839653375fbe7d82d23ae3692976d7f5a96711`, Godot/GUT 34/34/295, leak scans, and native visual crops all passed. It confirmed the two labeled artifact-bearing comparisons remain selectable but truthful. Coder `k72.26` stays in progress for aggregate integration/audit.
- Gameplay asset QA `k72.22` reproduced and passed every 0.0.3 content, immutability, two-build, clean-import, negative, and visual/contrast check, but correctly withheld PASS on tooling bug `xkl`: Blender 4.0.2 can print an uncaught `Traceback` and exit 0, while the strict validator/repro runner trusted return code without requiring success markers. An adversarial fake Blender therefore received a false clean-import claim. `xkl` blocks `k72.22`; follow-up returned to original asset coder `8ebeb864-2ff0-4036-b662-1faa16a3cb3b`. Tooling-only repair `8b8b40593b9deb54d32654e39fd7c1c1c4a9dc1a` is clean and public: shared traceback/error rejection, exact identity-bound source/GLB and generation/validation markers, full postcondition inventories, and adversarial helper/validator/repro tests. Strict/finalized validation, two-build reproducibility, dedicated contract tests, real/fake Blender rc0 failure cases, compile and diff checks passed. All source/manifests/sets/release 0.0.1–0.0.3/review trees are byte-identical to pre-repair commit `2addf3c`. Original QA agent `524ee2b7-5564-4620-8983-9f2b2d6d67f9` independently rechecked the complete diff and closed `xkl` plus `k72.22` PASS: strict/finalized validation, two-build reproducibility, all 14 exact identity smoke operations, real Blender rc0 traceback rejection, fake traceback/missing/wrong/duplicate marker cases through helper/validator/repro, complete inventories, and unchanged content trees passed. Original auditor `f0c77448-7738-49af-b3bd-f2b671304c83` was interrupted after prolonged non-terminating synthesis but had already posted/closed the audit in the ledger. Bounded replacement auditor independently reconciled the clean repo and evidence, added a second authoritative PASS, and confirmed `k72.23` plus `k72.21` closed: exact public commit and raw tree, immutable predecessors/content, seven separate roles, arrow/track contracts, rights/provenance/budgets/reviews, repaired subprocess contract, strict/finalized validation, two builds, real/fake adversaries, and no publication all passed. Asset `0.0.3` is cleared for downstream pinning.
- Cross-repo integration coder `f3c0dddc-6119-440a-8695-07f944e2fd89` was verified `running` on now-unblocked `k72.28` from assembly `9a7a19b`, renderer `e5130e0`, environment `e962fe2`, and gameplay asset `8b8b405`. Scope is exact asset `0.0.3` pinning, the frozen eight-photo catalog, generic renderer transform/resource lifecycle, private strict save/load controls, accessible per-instance chevron, camera visibility restore, privacy-negative tests, and bounded cross-context validation. Raw `0.0.35` remains immutable and `0.0.36` must not be created in this lane.

## Physical-revision integration coder result — READY FOR `k72.29` QA (2026-09-03)

- Renderer landed first through final clean/pushed commit `80564a4ea2c206cf36f62572e03f2af5bd8086e1`. It pins exact gameplay source `8b8b40593b9deb54d32654e39fd7c1c1c4a9dc1a`, immutable raw `0.0.3` tree `aa37bf534cc592a4057127876d567eadc3496f49`, 17-file inventory hash `69b88d38113a56061dfc0ea5e92ec51a0b181fcade6a99e1dcc5df1baecdde03`, and proof hash `287adc43a0456782044f0fd7601efd7b5087342972d9da4525923598754b1efc`. Exact opaque/tintable arrow and track-alpha-`0.52` material contracts are validated.
- The generic payload-free environment owner now exposes the private `setEnvironmentTransform` seam with exact position/XYZ-Euler/uniform-scale validation, six-decimal canonicalization, camera-inside bounds, accessor/proxy rejection, and atomic failure. Camera translation anchors the sphere without rotation inheritance; radius-30/16×32/1,024-triangle inward unlit emissive/front-cull/depth-off geometry, generation/abort/disposal, one resident decoded/GPU texture, context restoration, and gradient fallback remain intact. Independent final renderer audit passed.
- Assembly now owns the exact ordered eight-entry catalog from clean environment `e962fe2a8a48b7e2019d1a55c337abeb6ffd24d4`, emitting only eight JPEGs, eight strict JSON configs, and eight rights/source manifests (`24` files / `16,013,891` bytes). Luminious Ice Cave remains default; Snow Mountain with Lake and Igloo Toon remain selectable and receive dropdown-only source-artifact labels.
- Visual Test adds per-connected-instance private controls for environment selection, position X/Y/Z, pitch/yaw/roll, truthful `Sphere radius scale`, retry, deterministic strict JSON save/load, and an accessible at-least-44px collapse chevron. Each selection retains one config in connection-local memory; transform is applied before asset replacement; Camera hides the resident environment and Aero restores it without changing the preference or config.
- Strict config normalization/load rejects unknown/missing keys, accessors, proxies, wrong id/projection/version/schema, non-finite/out-of-range/outside-sphere values, malformed input, and files over 16 KiB without partial config mutation. No environment selection/config enters storage, telemetry, snapshots, composed events, host/iframe messages, or public package exports.
- Fresh validation passed: gameplay strict release validation; all-eight photosphere continuity/catalog validation; renderer `npm test`, package inventory, and browser direct/real-iframe portrait/landscape DPR 1/3 matrix; assembly `npm test`, exact environment sync/package/fingerprint/Vite validator, Chromium privacy/accessibility checks, production build, dry pack, and independent eight-context square-grid/pixel matrix. All managed jobs settled and no Chromium/Vite validator process remained. Known hanging unrelated full product-shell matrices were not rerun.
- Debug records preserve the transient half-synced gameplay browser timeout and the independently found/repaired proxy-admission gap under `.plans/debug/`. Raw `0.0.35` remains byte/tree immutable at `bd69d3bd309660125d1a5ac3da6d07896c49bb96`; raw `0.0.36` remains absent. `k72.28` intentionally remains in progress for `k72.29` independent QA; no tag, npm publication, GitHub Release, asset/environment source edit, or network acquisition occurred.

## `8m2` replacement coder repair result — READY FOR FRESH INDEPENDENT AUDIT (2026-09-03)

- Reconciled the predecessor candidate without deleting correct integration work. Strict known-ID grammar and eight-entry membership now gate normalization and deterministic path-safe filenames; catalog entries retain exactly `label`, `descriptor`, `defaultConfig`, and `files`, while the two artifact-bearing comparison IDs live in a separate frozen assembly-owned list.
- Environment loading state now tracks the renderer's current environment promise and settles from renderer diagnostics only for the matching graph, connection generation, load generation, and selected ID. Camera-hidden copy has precedence; Retry is disabled except in renderer error state and re-applies transform before a fresh descriptor load.
- Executable hostile config/control coverage now runs trusted save and exact-16-KiB load, oversize/fatal-UTF-8/malformed/wrong-ID/wrong-projection/range atomic rejects, stale selection/session/graph/connection/hidden/reconnect rejection, no storage/events/messages/snapshot leakage, accessible 44px controls/focus/collapse/internal scrolling, and selection/failure/retry behavior in both direct and genuine cross-origin iframe contexts. The eight layout rows cover portrait/landscape and requested DPR 1/3. A newly exposed landscape transport overlap was diagnosed and repaired by bounding the internally scrolling body to `58vh`; all eight rows then passed.
- Replaced the invalid `package.json#files` whitelist with narrow `.npmignore` exclusions for `.plans`, `.beads`, release/build output, repository-only docs/fixtures/tests, editable sources, and review payloads. Assembly now carries an exact byte copy of all 17 required `assets/gameplay/0.0.3/**` members from clean renderer `80564a4`; dry-pack validation hard-codes and compares every gameplay member, all 24 environment members, allowed roots, and forbidden evidence absence.
- Final PASS evidence: renderer `npm run check:assets` (17 exact gameplay files, inventory `69b88d38…`, proof `287adc43…`, 39-file renderer pack); assembly environment sync/validator (8 entries, 24 environment files, 17 gameplay files, 102 packed files); `npm test`; `npm run build`; environment controls direct/iframe matrix; square-grid direct/iframe portrait/landscape DPR matrix; exact dry pack (102 files, 24 environment, 17 gameplay, zero forbidden, packed `16,125,779`, unpacked `16,964,607`, SHA-1 `75396b040f159b4ce5879f0c96bad49081f09886`); and `git diff --check`.
- Raw `0.0.35` remains untouched at tree `bd69d3bd309660125d1a5ac3da6d07896c49bb96`; `0.0.36` remains absent. Assembly remains intentionally dirty/uncommitted/unpushed and `8m2` remains `in_progress` for fresh independent audit before `k72.28` landing.

## `18s` context-restore synchronization coder result — FUNCTIONAL PASS; HOST CADENCE GATE RED (2026-09-03)

- Reconciled and preserved the complete inherited dirty `8m2`/`qv7` candidate. The narrow repair observes private `renderer.environmentLoadPromise` identity inside the existing caller-owned visible display loop and on visible resume. Attach, selection, and Retry immediately record that same identity, so the display observer deduplicates rather than double-tracking. No renderer source, public event/payload/snapshot/message, storage, telemetry, timer, or independent polling path was added.
- Every settlement is bound to the same graph, connected lifecycle/generation, session generation, selected environment ID, observed promise identity/ID, and assembly load generation. Hidden/session-stale settlement requests visible/current reconciliation rather than mutating stale UI. Camera-compositor hiding remains copy precedence only: internal error truth and exact Retry gating still update. Observation resets across explicit reset, teardown, and reconnect.
- Replaced the focused test's renderer/assembly dual-state mutation with actual renderer canvas `webglcontextlost`/`webglcontextrestored` dispatch and deferred real owner fetches in direct and genuine cross-origin iframe documents. Executable cases pass restore loading → error with exact `Environment unavailable. Retry.`, error-only Retry → ready, promise-identity deduplication, clear/new selection/newer Retry-generation stale rejection, Camera-hidden precedence, hidden restore visible-resume reconciliation, and hidden disconnect/reconnect new-graph isolation. Existing hostile config, trusted exact-16-KiB chooser/save, atomic rejection, storage/event/message/snapshot privacy, 44px/focus/collapse/scroll/layout matrices remain intact.
- Authoritative assembly renderer pins now resolve to exact clean landed commit `1626fa89235c3d56d98004f89103c1389dc9cf30`, tree `3df64e595ca45893e72cc5620361e5dec728fb26`; exact immutable gameplay `0.0.3` tree remains `aa37bf534cc592a4057127876d567eadc3496f49`. Search found no stale `80564a4ea2c206cf36f62572e03f2af5bd8086e1` outside historical plan/Bead evidence.
- PASS commands: `npm run test:environment-controls-browser`; `npm run sync:environment-assets -- --check` (8 entries/24 files); focused environment assembly validator (24 environment + 17 gameplay, 102 package members); component and Playwright privacy/noise validators; assembly `npm test`; `npm run build`; `npm run test:release-pack-policy`; renderer `npm run check:assets`, `npm test`, and full `npm run test:browser`; exact npm dry pack; and final whitespace/source checks. Build emitted only the known PlayCanvas `node:worker_threads` externalization and chunk-size warnings.
- Exact assembly dry pack: `102` files, `24` environment, `17` gameplay under only `0.0.3`, `0` older gameplay, `0` `.plans`, `0` `.beads`; packed `16,128,585`, unpacked `16,980,029`; SHA-1 `25e7d8d6dd05f114db4fb7cab708a41c35109b29`; integrity `sha512-zGAljTij4yiT8/xThKT0f5XKPv8xn1+fexH+slYNBfc5oN12eE/1nmLn0shov+lCv7Fu6ClRJAKBNYPRqaWKdg==`.
- Product-shell code paths pass, but unchanged sustained-cadence gates remain host-red. A capped isolated shell run reached 18/24 rows before the 600-second executor timeout. Its uncapped managed rerun reached iframe portrait DPR3 and failed only the unchanged audio timeline cadence (`displayRateFps=15`, `timelineDeltaP90Ms=78.125`, required `<66`). Full `npm run test:browser` passed console/audio/mobile/music/library predecessors and the repaired direct DPR1 shell rows, then failed only unchanged direct portrait DPR3 sustained cadence (`displayRateFps=14.9`, `37` renders, `timelineDeltaP90Ms=78.125`). Immediate host evidence showed unrelated `llama-server` at `241%` CPU and `WebKitWebProcess` at `98.5%` CPU. No cadence threshold or product behavior was weakened.
- Source status remains intentionally uncommitted: assembly `HEAD == origin/main == 65fa4ff8ecf3ee88366739a615459ca5836163a8` plus the protected cumulative dirty candidate; renderer `HEAD == origin/main == 1626fa89235c3d56d98004f89103c1389dc9cf30` with no tracked dirt. No raw release/tag/publication, renderer edit, Git commit/push, or Bead closure occurred. Leave `18s` and `8m2` open for fresh QA/audit and rerun full cadence on an unsaturated host.

## `y1n` stopped-display-loop restore notification coder result — PASS (2026-09-03)

- Fresh audit showed the prior identity observer still depended on an active display frame or visibility transition. At visible `menuOpen=true`, `frameTimer=0`, an actual canvas context restore changed renderer promise/state loading→error while assembly remained ready/blank/Retry-disabled. Dedicated root-cause record: `.plans/debug/2026-09-03-stopped-display-loop-environment-restore.md`.
- Added one private assembly-owned `webglcontextrestored` listener to the stable canvas per connected lifecycle and remove the same bound function on teardown. Its microtask captures and verifies graph, connected generation, lifecycle, and canvas before observing the renderer's synchronously replaced promise. Visible stopped-loop restores use existing identity-deduplicated tracking; hidden restores only mark reconciliation and are observed on visible resume. No timer, renderer edit, public event/payload/message/snapshot, storage, telemetry, or independent polling was added.
- Browser coverage contains no manual `observeEnvironmentLoad()` call. Direct and genuine cross-origin iframe paths keep `frameTimer=0`, dispatch actual context loss/restore events, and pass automatic loading→error/exact Retry, Retry/restore→ready, one-generation notification, clear/new selection/newer Retry stale settlement, Camera-hidden precedence, hidden resume, and queued old-event disconnect/reconnect isolation. Existing eight layout contexts, hostile config/file, privacy, and accessibility matrices remain intact. Static validation requires exactly one add/remove call site and the microtask/current/hidden guards.
- PASS: focused environment browser; syntax; focused environment/config/package validator; full `npm test`; production build; `npm run sync:environment-assets -- --check`; release-pack policy; component/import/privacy/noise checks; and explicit non-cadence browser gates for audio mix, mobile menu, music preview, batch library, camera compositor pixels, gameplay cursors, and square-grid direct/real-iframe DPR matrices. Build produced only known PlayCanvas worker externalization and chunk-size warnings. Per instruction, externally blocked qv7/product-shell cadence was not rerun.
- Exact dry pack remains `102` files: `24` environment, `17` gameplay only under `0.0.3`, zero older gameplay/`.plans`/`.beads`; packed `16,129,100`, unpacked `16,982,686`; SHA-1 `b3542896a20237f9f1457fb40eea91a5121041ea`; integrity `sha512-KQvG0TII19dnJQ4+0nLGbTAlDdUzJIupM+4XAmT5wIQkmmObF5GycPbulb2MaG9DtHjo0Kd221TPUZ7FJPIwXQ==`.
- Assembly remains intentionally cumulative-dirty at `HEAD == origin/main == 65fa4ff8ecf3ee88366739a615459ca5836163a8`; renderer remains clean/unchanged at `1626fa89235c3d56d98004f89103c1389dc9cf30`, tree `3df64e595ca45893e72cc5620361e5dec728fb26`. No Git commit/push, closure, release/tag/publication, renderer edit, or threshold change. Leave `y1n`, `18s`, `8m2`, and `k72.28` open for fresh QA/audit.

## Final cumulative assembly audit and landing — PASS (2026-09-03)

- Fresh landing-owner audit independently read the README, complete plan, all six target Beads and comments, seven debug records, complete tracked/untracked candidate, current source/tests, and exact sibling source trees. No unresolved requirement defect, race, or false-positive assertion remained. The audit confirmed eight ordered owned photospheres/default/labels/24 hashes; strict known-ID/path-safe/accessor/proxy/exact-shape/finite/range/inside-sphere atomic config validation; trusted private save/load and error-only Retry; accessible collapse/internal scrolling; Camera precedence; active/stopped/hidden/context-restore/reconnect generation isolation; privacy; semantic shell inventory/pointers/cleanup; square-grid/cursor pixels; opaque arrows; 0.52 depth-write-off track; exact 17-file gameplay package; narrow package exclusions; fingerprint coverage; and exact provenance pins.
- Normal unsaturated cadence was rerun without threshold changes. `npm run test:shell-matrix` passed all 24 direct/real-iframe portrait/landscape DPR 1/3 Flow/Lanes/Grid rows. Immediate full `npm run test:browser` passed its complete predecessor chain, repeated shell matrix, camera compositor, gameplay cursors, and eight-context square-grid pixel gate. No validator, Chromium, Vite, llama, or WebKit process remained.
- Fresh assembly gates passed: environment controls direct/real-iframe eight-row hostile/lifecycle/privacy matrix; environment sync verify; 24-environment/17-gameplay inventory/fingerprint/Vite validator; `npm test`; production build; release target and release pack policy; exact dry pack; syntax; and `git diff --check`. Dry pack remained exactly 102 files with 24 environment and 17 gameplay `0.0.3` members, no older gameplay/plan/Beads payload, packed 16,129,100 bytes, unpacked 16,982,686 bytes, SHA-1 `b3542896a20237f9f1457fb40eea91a5121041ea`, integrity `sha512-KQvG0TII19dnJQ4+0nLGbTAlDdUzJIupM+4XAmT5wIQkmmObF5GycPbulb2MaG9DtHjo0Kd221TPUZ7FJPIwXQ==`.
- Cross-repo proof gates passed from clean public sources: renderer `1626fa89235c3d56d98004f89103c1389dc9cf30` / tree `3df64e595ca45893e72cc5620361e5dec728fb26` full `npm test` and browser suite; gameplay `8b8b40593b9deb54d32654e39fd7c1c1c4a9dc1a` strict validation, two-build reproducibility, and adversarial subprocess contract for immutable tree `aa37bf534cc592a4057127876d567eadc3496f49`; environment `e962fe2a8a48b7e2019d1a55c337abeb6ffd24d4` eight-entry photosphere continuity proof. Raw assembly `0.0.35` stayed exactly 20 files/tree `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, proof SHA-256 `22c41e8bf0630bb6b50523a96ab0e886b399a93c74a7d1e97bb2afe47a43c4ea`; raw `0.0.36` remained absent.
- This plan-bearing cumulative commit lands only intended assembly source, scripts, docs, owned runtime assets, debug reports, `.npmignore`, and the inherited ledger interaction export. It does not create a raw release, tag, GitHub Release, npm publication, replacement server, or physical-device approval claim. Post-landing cross-mode QA `k72.29` is complete. Remaining work follows the authoritative graph: final release-readiness audit `k72.30` → build immutable raw `0.0.36` under `k72.31` → independent immutable-release QA/audit `k72.32` → Derrick's physical secure-context camera/calibration/playability review under `k72.17`. Publication remains prohibited and is not part of this graph.

## Release-readiness alignment repair (2026-09-03)

- Final release-readiness audit found no technical, asset, runtime, browser, package, rights, provenance, or immutability defect. It correctly withheld release authorization because implementation Beads `k72.24` and `k72.26` still reflected their earlier downstream-QA hold, and the prior remaining-work sentence misnamed the live graph.
- Both deferred implementation gates are now satisfied: square-grid QA `k72.25`, photosphere-catalog QA `k72.27`, aggregate integration/audit `k72.28`, and post-landing cross-mode QA `k72.29` are closed PASS. Therefore `k72.24` and `k72.26` are closed with explicit evidence-based reasons rather than left artificially in progress.
- The sole next authorization gate remains `k72.30`. Only its fresh PASS may unblock `k72.31`; raw `0.0.36` remains absent until then. The exact successor sequence is `k72.30` → `k72.31` → `k72.32` → `k72.17`, with no npm publication, GitHub Release, or tag.

## Immutable raw `0.0.36` builder result — PASS (2026-09-03)

- Builder began from exact clean public assembly `main == origin/main == ee9ae40e1561025de58827cbd054f87bd4541a5c`, with clean exact sibling pins renderer `1626fa89235c3d56d98004f89103c1389dc9cf30`, gameplay `8b8b40593b9deb54d32654e39fd7c1c1c4a9dc1a`, and environment `e962fe2a8a48b7e2019d1a55c337abeb6ffd24d4`. Before any release mutation, raw `0.0.36` was absent and raw `0.0.35` was snapshotted as exactly 20 tracked/filesystem byte-identical files / `13,878,153` bytes, tree `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, proof SHA-256 `22c41e8bf0630bb6b50523a96ab0e886b399a93c74a7d1e97bb2afe47a43c4ea`, and reviewed complete-manifest aggregate `a111efba87a4b46502b04a34c3ff0ca102815f090a20fba7cf4ccf29d2d21951`.
- The canonical `npm run version:patch` ran exactly once. `package.json`, package-lock top level, package-lock root package, and `index.html` proof meta moved exactly `0.0.35 → 0.0.36`; no `0.0.37` exists. Immediately before build, the three sibling pins were rechecked clean because linked working bytes enter the fingerprint. With `AEROBEAT_BASE_PATH=/` explicit, the authoritative fingerprint reproduced exactly as expected: `34e0e2b1365d8a37303df622250dd005f3a149b1a78b35ce13a42974f58ca261` over `196` inputs. Canonical `npm run build-release` then claimed the absent target and created builder commit `c17c48537e0cd41486ea312778cc897c56bd0da8`.
- Raw `0.0.36` is exactly 41 regular files / `27,760,611` bytes (`27,759,245` before the `1,366`-byte proof), all Git mode `100644`, tree `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, proof SHA-256 `3d18dd99afe99fcac389bccee760073baf83cc44c66cdec6d0ac5d933d142daf`, and complete-manifest aggregate `575fb3515aeca10f5a88ea06db4b2c2003b4506d2c70e520e6f8533a9ea97595` using the established globally sorted `sha256␠␠repo-relative-path` convention. Category totals are proof 1 / 1,366 B; HTML 1 / 527 B; CSS 1 / 1,303 B; JavaScript 4 / 4,360,890 B; source maps 3 / 7,353,726 B; environments 24 / 16,013,891 B; gameplay GLBs 7 / 28,908 B.
- Exact raw inventory (`path | bytes | SHA-256`):
  - `aerobeat-release-proof.json` | 1,366 | `3d18dd99afe99fcac389bccee760073baf83cc44c66cdec6d0ac5d933d142daf`
  - `assets/conversion-worker-BHYLiFBU.js` | 94,564 | `d390de7405a8c661e9c31e03a2ee88e434d4cf9b4fcc048950d9047097337834`
  - `assets/conversion-worker-BHYLiFBU.js.map` | 154,106 | `7c1a3759c509d5fa0be95687f1474bce088387b5894c51482acf2dde1f85cff1`
  - `assets/environments/alpine-river-valley-photosphere/1.0.0/alpine-river-valley-photosphere.config.json` | 334 | `c1cda19a958cee99c4d021baf9455ed95e0d0fd9cc420bfe1857ee5b7da1f5e6`
  - `assets/environments/alpine-river-valley-photosphere/1.0.0/alpine-river-valley-photosphere.jpg` | 2,664,010 | `7a529a6e0c1bee343633273d672a22f346f8ffc7406bf6a40812ae5435f3260b`
  - `assets/environments/alpine-river-valley-photosphere/1.0.0/manifest.json` | 4,147 | `5f01074a8eebfd2246587b68dc3a162a77d431c4350700a5df38ba67cb00d201`
  - `assets/environments/icebergs-on-sea-shore-photosphere/1.0.0/icebergs-on-sea-shore-photosphere.config.json` | 336 | `d016f6025b203711187572bd336486a153a679f5d15013767c7ec9801dde01f2`
  - `assets/environments/icebergs-on-sea-shore-photosphere/1.0.0/icebergs-on-sea-shore-photosphere.jpg` | 1,689,089 | `aa91137af632f15d03d1e900006cb490bd1229c761b47aebef94ae300678fe12`
  - `assets/environments/icebergs-on-sea-shore-photosphere/1.0.0/manifest.json` | 4,148 | `f62821ae9dc2cf48ac41592472f6e11ff3e4fab55c6c9ea697be2e7fd997b9ab`
  - `assets/environments/iceland-waterfall-photosphere/1.0.0/iceland-waterfall-photosphere.config.json` | 332 | `c129f38beea688e3c43399a72ace098befb621655fb43aaed9da34a2bec8bdd5`
  - `assets/environments/iceland-waterfall-photosphere/1.0.0/iceland-waterfall-photosphere.jpg` | 1,983,883 | `282f69ba1cad321074a8bdb84dd79d747d3c14a3df7408146837da1b08713c09`
  - `assets/environments/iceland-waterfall-photosphere/1.0.0/manifest.json` | 4,131 | `137da54aaae3162ae1cc3a3a895ad3d91dc9bcfc8482cf1392c0b10bf8767692`
  - `assets/environments/igloo-toon-photosphere/1.0.0/igloo-toon-photosphere.config.json` | 325 | `481329192d693584da295139d800647c326016c5c2ff5f2c29efa6c6a9ebc271`
  - `assets/environments/igloo-toon-photosphere/1.0.0/igloo-toon-photosphere.jpg` | 1,624,217 | `c8deae4e235ba529c093ce9ec28aed9d0088d94c379dafe7a23b9c82aac06477`
  - `assets/environments/igloo-toon-photosphere/1.0.0/manifest.json` | 4,074 | `d66a0b5f49590dfe63c6a27454b8b21194b6b93be9ca86edcaf197b4495018a1`
  - `assets/environments/luminious-ice-cave-photosphere/1.0.0/luminious-ice-cave-photosphere.config.json` | 333 | `57729755f3ba686e60314cd69c1921305bab1a19037effabedb59322c4e67443`
  - `assets/environments/luminious-ice-cave-photosphere/1.0.0/luminious-ice-cave-photosphere.jpg` | 2,210,289 | `ff142b3ce3d3509ab3cfafcfc6a8cc2d3b0ff737852072d3a7aea8075478eed5`
  - `assets/environments/luminious-ice-cave-photosphere/1.0.0/manifest.json` | 4,869 | `524c7a5623dfbafb65590a9b3b78dc894d4341165d564ac267d470f03acf7e80`
  - `assets/environments/salt-lake-2-photosphere/1.0.0/manifest.json` | 4,076 | `36c3c0f7ceb0fcd5e881a615157f30563ebe0b4720c47f2b86cf66c52cc88c48`
  - `assets/environments/salt-lake-2-photosphere/1.0.0/salt-lake-2-photosphere.config.json` | 326 | `f651188606f49506bfe9c627bf06718fa8c616f5dec30a1a55065fbfd0c6600f`
  - `assets/environments/salt-lake-2-photosphere/1.0.0/salt-lake-2-photosphere.jpg` | 1,974,340 | `beebe837d370302e201b4e0dbf6ac28010afc35dc9ec115b01256032737ed474`
  - `assets/environments/salt-lake-photosphere/1.0.0/manifest.json` | 4,079 | `f955189d3002f484940fa9ad6aa8f656400667eab8e26f88277ee6e455508121`
  - `assets/environments/salt-lake-photosphere/1.0.0/salt-lake-photosphere.config.json` | 324 | `4f602515f85343c8e44ebc0271b5ccadd6a8848d917bb311cc742deea04af5c7`
  - `assets/environments/salt-lake-photosphere/1.0.0/salt-lake-photosphere.jpg` | 2,010,461 | `2768567c95424d33a0b1727f1c50a48e3e063902d021132642ea7b6f5851185c`
  - `assets/environments/snow-mountain-with-lake-photosphere/1.0.0/manifest.json` | 4,187 | `1e39c718c7033aaac2caff8f58d5e1d63d809d73020f3a1c2babc66057db4af2`
  - `assets/environments/snow-mountain-with-lake-photosphere/1.0.0/snow-mountain-with-lake-photosphere.config.json` | 338 | `5c0c8f0da565e92c8372e6f92b0fdbb05e34c6608776b3292edb497b94d6b5e5`
  - `assets/environments/snow-mountain-with-lake-photosphere/1.0.0/snow-mountain-with-lake-photosphere.jpg` | 1,821,243 | `f501bec5b23357dc06be0fe8df8cb8ca39e901ca2b3230c66019fb2eaa08029e`
  - `assets/gameplay/0.0.3/any-note/circle-v1.glb` | 5,616 | `0cd90824251657825a9c84499a43a59ce7e39112e32f9903b1e1836c2466945e`
  - `assets/gameplay/0.0.3/athlete-marker/sphere-v1.glb` | 3,036 | `be91826b05a9643a3923ad7d3af9d2e0e0c22bb29bac4bfa97a9758b43700bd3`
  - `assets/gameplay/0.0.3/bomb/urchin-v1.glb` | 8,364 | `63d61feff050c284f2e3a228d345ea794c25bab48ab56cd4801d554c923def85`
  - `assets/gameplay/0.0.3/directional-arrow/outline-v1.glb` | 3,272 | `c5ae744fbb151b101c2b1e732fdcaf54ac8549c746c10c1df5a13dba7e4ef869`
  - `assets/gameplay/0.0.3/guard/shield-v1.glb` | 2,848 | `50eb6b6835ec6f2acfceb866d53a8f3f8b424d48259ac59c74a39f5bab731eee`
  - `assets/gameplay/0.0.3/track/blue-glass-v1.glb` | 2,480 | `46cb72ed47a235e9bf40305bac2355b02ca47aa6b39278503cd6fc1b32cef987`
  - `assets/gameplay/0.0.3/wall/red-glass-v1.glb` | 3,292 | `ed6511c1ce1c196c9b735e40c0b4bf96375cd2f9b9dd0723c5fe5c5209b5d27a`
  - `assets/index.css` | 1,303 | `86644d0307214793c0866611fa4f043a27ab9f8f6e0f3a1ce62dab208724e4dc`
  - `assets/index.js` | 4,062,189 | `abadb33ee92bd21e0a62aba37381857ed853a07f430a0510eb01f9ae72a61651`
  - `assets/index.js.map` | 6,821,553 | `710a5eb04e8ad1bbf98d54b5c119b27881c8d4a949adc6270012b2663ab15b03`
  - `assets/mediapipe-worker.js` | 6,582 | `10f0114b7cba802da8bd76606048c3de2b67eca7c3ef328226f5d1dba97bc2b2`
  - `assets/vision_bundle-CPgxPL03.js` | 197,555 | `ffaab300483935de2d13357687d37eedf5d9905cd446d4c50848ed46636d033f`
  - `assets/vision_bundle-CPgxPL03.js.map` | 378,067 | `fba60cbc5aab4c159eab5c4134467acf391c7978940e6d56e36cfbb49583669c`
  - `index.html` | 527 | `c29727032d9ae3d4410ec0bc47ac374a8718cfa88c7d6e787beed52fc79d4f9e`
- Proof/schema and raw policy passed: exact generator key set, raw/unminified, base `/`, three valid source maps, four JS assets, zero WASM, locked MediaPipe GPU-WebGL Pose Landmarker Lite float16 `/1/`, Tasks Vision `1.0.1`, `0.5 / 0.5 / 0.5`, Fast/full/measured, 15 fps, sole MediaPipe vendor/backend, required markers present, and all forbidden runtime asset/marker checks absent. The raw inventory has no rejected third-party environment, PLY, cubemap, `.blend`, PNG/gameplay atlas, old gameplay release, or external runtime asset URL.
- Exact source representation passed. The environment source, assembly assets, and raw release match all eight ordered photospheres / exact 24 config-JPEG-manifest files / `16,013,891` bytes. Gameplay source, renderer package, and assembly package match all 17 immutable `0.0.3` runtime-package members / `43,704` bytes, inventory `69b88d38113a56061dfc0ea5e92ec51a0b181fcade6a99e1dcc5df1baecdde03`, proof `287adc43a0456782044f0fd7601efd7b5087342972d9da4525923598754b1efc`, and set `a1ca1d4acccf3e1e53e11d6e93c5cf8fcfdcba73eeaf7eb6347eb74f10589288`; raw correctly emits only their seven GLBs byte-for-byte and excludes the ten package-only manifest/inventory/proof/set members.
- Two independent depth-matched detached worktrees at exact committed candidate `c17c485` linked only the already-installed local `node_modules`, removed only their temporary checked-out `0.0.36` copies, and invoked canonical `AEROBEAT_BASE_PATH=/ npm run build-release`. Both recomputed the same 196-input fingerprint and reproduced all 41 members, all `27,760,611` bytes, proof, tree-equivalent inventory, and aggregate byte-for-byte against primary and each other. Both temporary worktrees and outputs were removed. A subsequent actual canonical invocation against primary existing `0.0.36` failed before Vite with `EEXIST` / `already exists and is immutable`; complete predecessor and successor snapshots remained unchanged.
- Validation passed `npm test`, `npm run build`, release-target policy, complete release-pack-policy self-test, environment source/package validation, and canonical detached pack verification at `c17c485`: 401 tracked files normalized to `0644`; 102 archive members / no PAX / all `0644`; archive SHA-256 `d99496df5ec0971807a2a1335b0bc68ce22a160669155ec20ad176f111e759e3`; decompressed tar SHA-256 `27eb897132dc1ea5fa870c8ef95bf97eddd7d8577dcd304938eb7b2d5968c94a`; manifest SHA-256 `e799d67ecfd80e80aaab3a45740ddcef2a4892e028ef476dfc4dc680af712005`; internally derived metadata SHA-256 `f624568ec738ddd7967975b77661fd5fd6e5f0caba9ce4587f403cc33894a067`. Temporary pack output was removed.
- A temporary managed `127.0.0.1:5198` server, separate from the existing physical-review server, passed a normal headless Chromium smoke at `390×844`: exact connected/fill canvas, snapshot version/fingerprint, 11 local requests, zero network escape, zero unexpected errors under the documented GL-driver filter, and nonempty 69,850-byte capture SHA-256 `6784801b4109bb7b5d24b0ee8df8b8879a2c7b6cd45f85c67a45742d72b6abda`. The temporary server was stopped. The existing review server was not inspected, replaced, or touched.
- The first full-browser attempt passed console/audio then hit the known host-sensitive mobile calibration transition timeout at line 261. Diagnosis found no product/source delta; an immediate isolated mobile validator passed. The clean full `npm run test:browser` rerun then passed its complete console/audio/mobile/music/library chain, all 24 direct/real-iframe portrait/landscape requested-DPR1/3 Flow/Lanes/Grid rows, compositor, cursors, and eight-context square-grid pixel matrix. No threshold or source was changed.
- Read-only QA child `5d459801-6275-4665-a35d-0fb8b902e5ff` and final auditor `67e2f824-a38a-46ba-b5d2-4433fcc472a4` independently reproduced every release identity, proof, inventory, source-equality, and prohibited-action check. Their only pre-finalization holds were the then-running full-browser rerun and expected unpushed documentation state; both are resolved by the recorded clean rerun and final evidence commit/push. The auditor explicitly accepted the direct-user audit-first server deferral.
- Audit-first server deferral intentionally supersedes the older `k72.31` wording: even after builder push/closure, the existing review URL must remain assigned to its audited `0.0.35` state until dependent `k72.32` independently audits `0.0.36` and deliberately performs the replacement. This builder did not inspect or touch the current review server and does not claim that server switch or Derrick physical approval. No tag, GitHub Release, npm publication, network acquisition, renderer/gameplay/environment edit, history rewrite, or replacement server occurred.

## Immutable raw `0.0.36` QA, audit, and served review — PASS (2026-09-03)

- Independent `k72.32` phase 1 reproduced raw `0.0.36` twice from clean pushed `8d5590068995df5a7ba05b45ce78b0963a47ea95` and byte-compared all 41 files / `27,760,611` bytes. It confirmed tree `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, proof SHA-256 `3d18dd99afe99fcac389bccee760073baf83cc44c66cdec6d0ac5d933d142daf`, aggregate `575fb3515aeca10f5a88ea06db4b2c2003b4506d2c70e520e6f8533a9ea97595`, and fingerprint `34e0e2b1365d8a37303df622250dd005f3a149b1a78b35ce13a42974f58ca261` over 196 inputs. Append-only refusal, source/release/package policies, direct and genuine-iframe raw runtime matrices, all gameplay modes, eight photospheres, controls/config/lifecycle/privacy, and all predecessor immutability checks passed.
- After phase-1 authorization, the parent deliberately started managed review-server job `bash-39` as PID `2445724`, exact command `python3 -m http.server 5173 --bind 127.0.0.1 --directory release/raw/0.0.36`, from the canonical assembly repository cwd. Parent and phase-2 audit independently fetched all 41 paths from `http://127.0.0.1:5173/`; every response was HTTP 200 and byte-identical to immutable raw `0.0.36`.
- Phase 2 passed all eight direct/genuine-iframe portrait/landscape requested-DPR1/3 Flow/Lanes/Grid contexts against the exact review URL, plus the eight-environment selector, transform/collapse/config, lifecycle, accessibility, privacy, cadence, and zero-noise checks. Raw `0.0.35` remains exact at tree `bd69d3bd309660125d1a5ac3da6d07896c49bb96` and proof `22c41e8bf0630bb6b50523a96ab0e886b399a93c74a7d1e97bb2afe47a43c4ea`.
- `k72.32` is closed PASS and the immutable successor is ready for Derrick's physical review. `k72.17` remains in progress; no physical approval is claimed. No tag, GitHub Release, npm publication, or history rewrite occurred.

## Derrick physical review of raw `0.0.36` — FAIL; `i4u` diagnosis complete (2026-09-03)

- Derrick's remote Tailscale HTTP review failed: DDR and other BeatSaver downloads show exact error `Cannot read properties of undefined (reading 'digest')`. Physical gate `k72.17` remains FAIL/in progress; bug `aerobeat-web-assembly-i4u` owns recovery. Raw `0.0.36` remains immutable and the managed `100.113.165.57:5173` server remains unchanged as failed physical evidence.
- Diagnostic Chromium reproduced the exact UI action path on `http://100.113.165.57:5173/`: `isSecureContext=false`, `globalThis.crypto` exists, and `crypto.subtle` is undefined. The archive request succeeded (`200`, `2,309,957` bytes), then raw source-map evidence resolved the cause to BeatSaver `archive.js:79` SHA-1. A temporary localhost control serving identical bytes reported secure context/WebCrypto available and successfully authored three packages, loaded content/audio, all seven gameplay GLBs, and the environment; that temporary server was stopped.
- Scope is broader than the first song error. Content authoring (including its Worker), runtime package/assets, encoded audio, renderer GLBs, and environment JPEGs all depend on `SubtleCrypto.digest`; renderer gameplay assets and the photosphere already fail verification on the insecure origin before download. Camera remains separately and correctly unavailable on non-loopback HTTP; supported non-camera song/Test/asset behavior must not require a secure context.
- Full diagnosis and verification design are preserved in `.plans/debug/2026-09-03-insecure-context-song-download-digest.md`. Implementation `i4u.1` → independent insecure/secure QA `i4u.2` → final audit/immutable-successor lane `i4u.3` are explicitly linked. Integrity must remain fail-closed through an audited bundled pure-JS SHA-1/SHA-256 fallback (with no hash skipping, external runtime URL, WASM, or runtime network acquisition) across every owning repo.
- Package/lock inspection found no existing SHA library. `aerobeat-web-gameplay` has a deterministic pure-JS text-only SHA-256 implementation that is useful prior art but lacks SHA-1, binary/incremental support, and correct cross-layer ownership. Recommended architecture is one approved low-level first-party hash owner consumed by BeatSaver, authoring, content, audio, renderer, and gameplay, with assembly owning end-to-end QA/release rather than the algorithm.
- Release policy was checked: `scripts/bump-patch-version.js` increments the current semantic patch exactly once, so the policy-derived expected successor to `0.0.36` is `0.0.37`. It is not created or reserved by diagnosis. Only implementation, independent QA, and final audit PASS may authorize that successor build and a deliberate review-server replacement; `0.0.36` must remain byte-identical.

## `b9e` BeatSaver explicit metadata-version fallback diagnosis (2026-09-03)

- Fresh public-inspector reproduction on exact clean vendor candidate `a654d91934186d4ea2c00923c9d8d0700d29737b` confirmed that explicit `version: "5.0.0"` and `"1.0.0"`, wrong-type/empty/malformed declarations, permissive numeric prefixes, and conflicting `version`/`_version` pairs are accepted as v2/v3/v4 through shape fallback. The same explicit v5 archive succeeds through provider download and local import because both paths share `inspectBeatSaverArchive()`.
- Full diagnosis and exact matrix are preserved in `.plans/debug/2026-09-03-beatsaver-explicit-version-fallback.md`. `buildSourceManifest()` currently erases present-invalid values through `optionalString()`, while unchanged-since-`938c6f55` `detectFormatMajor()` uses permissive `parseInt` then falls into legacy shape inference. README policy has always promised unsupported metadata rejection; existing tests cover only valid declared v2/v3/v4 and ZIP-security failures.
- Required minimal repair is to validate every own `version`/`_version` declaration as a complete supported version, reject any malformed/unsupported declaration or cross-major dual-key conflict with stable `unsupported`, and invoke the existing v2/v3/v4 shape heuristic only when both declaration keys are truly absent. Preserve supported key aliases, same-major dual declarations, hybrid legacy shape tolerance, exact provider hash semantics, and documented versionless compatibility; do not delete heuristics or add broad shape-coherence rejection.
- JSON parsing means inherited/accessor/proxy object identity cannot reach the detector: the public boundary receives bytes and native `JSON.parse()` creates ordinary own data properties. Tests must lock serialization/parse reality (`__proto__` does not mutate the prototype; serialized accessor/proxy values become ordinary declarations) rather than inventing an unreachable direct-object API.
- Diagnosis changed only this plan and the persistent debug report. No vendor/authoring/shared product, test, package, release, server, commit, push, or closure action occurred. `b9e` remains in progress for the parent/coder repair and full `i4u.1.2` regression matrix.

## `i4u.1.5` assembly shared-hash integration — implementation and focused QA PASS (2026-09-04)

- Assembly coder claimed `aerobeat-web-assembly-i4u.1.5` from exact clean public `main == origin/main == e37cc9fd28e7d061190829f7f0966cb8295d5826`, tree `ff52a74f73eec4bc74f13cf409343c8b5dcff24b`. Read the root README, this active plan, every comment on `i4u`, `i4u.1`, `.1.5`, `i4u.2`, `i4u.3`, and `k72.17`, both new debugging reports, and the seven relevant package READMEs/manifests. Research children `c054a7eb-2c0e-429a-8644-b2b8350ce67e` and `7b924b7c-7553-4833-a014-b87f4ab8802c` were verified running, completed read-only, and returned pin/harness audits; neither edited, served, committed, or touched the managed review process.
- Exact clean public consumer provenance is now direct validator/fingerprint/release-proof/README authority: shared hash `be7249b0bdfffcab568b760c1b582bfe2a0c1e92` / tree `b423c6742c07f56dde196d9f60f2e23c51ad913c`; vendor `4d2479df0d4b12305cc8190dbe918995abae5d03` / `40c8055acab241614272fb922a51c1d092e6dd08`; authoring `08b330d9f519113793eb5041d942aa1e8e2095dc` / `5b8e64f42f1f2d9e341c90fe6fc5688646051fa0`; content `501bddac3fcf2aa6332690dc6ac16ad73e8140e2` / `89e57f58598763cd6ef11fb49b227c7691cbb4ca`; audio `2bd5c6bc96d001f9755b6d8b2a79c57cd8e196f1` / `4bb2a110ddeab77559d1f7ffcf5d3e348a93f027`; renderer `8d2554b6a9cd08636afd189e292b19a8bd75a1ad` / `b2d9dabe7aa602e161e0c599ee267ff28f07c711`; gameplay `e7e83edacd20a6691b297e3e220708f3034e4f34` / `3da9eeafed18a230dd2baa1996c93443af7dd217`. All seven were tracked-clean, exact `HEAD == origin/main`, and exact SSH public origins before use.
- `package.json` / lock now directly own `@aerobeat/web-hash` through the committed sibling file link; Vite aliases that exact root source for main and module Worker bundles and excludes it from dependency prebundling. Recursive source fingerprinting still owns all three exact shared package source inputs and now additionally salts the exact seven commit/tree records, so a same-tree/different-commit provenance change cannot be hidden by a lockfile. Future release proof generation records the same dependency provenance and refuses emitted WASM before proof creation.
- New production source-map policy built two scripts with five maps and observed three exact `aerobeat-web-hash/src/index.js` attributions across main/Worker graphs. Every direct SubtleCrypto digest occurrence belonged to that source; consumer/assembly production sources had none. Exact shared source retained SHA-1/SHA-256 incremental fallback plus native auto path and had no fetch, external URL, dynamic import, WebAssembly, `.wasm`, dependency, or hash-skip route. The first gate overreached by scanning unrelated locked MediaPipe WebAssembly tokens; persistent diagnosis `.plans/debug/2026-09-03-hash-bundle-wasm-scan-overreach.md` and discovered Bead `slu` preceded the scoped repair. The corrected focused gate passed and `slu` closed.
- New production-style validator built to a temporary directory, bound separate child/parent servers to free ports on `0.0.0.0`, selected genuine RFC6598 Tailscale identity `100.113.165.57`, and used no Chromium trust override. Exact identities passed: insecure direct child and insecure same-host/different-port iframe child had `isSecureContext=false` and `crypto.subtle===undefined` in captured Window state and a pre-conversion module Worker; localhost direct and `127.0.0.1` parent → `localhost` child iframe controls had `isSecureContext=true` and callable SubtleCrypto. Every parent/child iframe origin differed exactly. All servers were temporary and stopped; PID `2459798` / immutable physical-fail server was never inspected, stopped, rebound, replaced, or changed.
- Each of four rows exercised production UI/service graph with the uncommitted exact local BeatSaver 4858 fixture (`7,552,654` bytes, SHA-256 `4273f0305518aa79ae4a7b58ccb07e86704ba50dbe679d0f0e29c52cb7b6beed`): Latest, search, selected Download on direct rows, local `Blob` ZIP on iframe rows, raw SHA-1 `6eb806bbc3935f28d8a8f396cad5030f08e1e25d`, provider SHA-1 `431ffaa53a1e45ffab6c81a895e456f6aad1e038`, three real authoring Workers, atomic IndexedDB collection, reload through a fresh graph, exact ExpertPlus content/assets/audio decode, trusted Test click to real playing state, zero camera requests, seven GLBs, all eight owned JPEG environments with one resident, Flow/Lanes/Grid actual renderer calls, deterministic AEROPKG1 export, revoked URLs, collection/package/shared-asset deletion, Worker termination, reconnect teardown, and zero network escape/unexpected noise. Insecure camera truth stayed `camera=false` / `camera_unavailable`; no camera success was claimed.
- Genuine 4858 uncovered an unrelated exact seam: Hard/Expert Flow obstacles derived from legacy negative durations contain `end < start` (Hard event 93 is `83.75 → 83.5`). Content correctly rejects `event_interval_invalid` after all three packages atomically persist; the validator asserts that fail-closed boundary, then explicitly selects interval-valid ExpertPlus for downstream audio/Test proof. Persistent report `.plans/debug/2026-09-03-real-4858-negative-obstacle-intervals.md` and discovered Beads `env` / follow-up `ncg` preceded the test recovery. `env` closed after four-row PASS; `ncg` stays open for a future authoring-domain normalization decision without changing the exact audited i4u pins.
- Focused v4 integration now adds explicit `5.0.0` and `4.0.0junk` metadata rows with stable `unsupported` rejection, preserves genuinely versionless v4 inference, retains exact v2/v3/v4 admission, and retains provider one-byte tamper rejection with no package commit. Package/chart/asset/audio/GLB/JPEG one-byte fail-closed behavior remains owned and rerun through the seven official consumer suites; assembly source-map and four-origin integration add the exact production composition proof rather than copying consumer algorithms/tests.
- Assembly focused PASS: `npm run test:hash-provenance`; four-row `npm run test:insecure-hash-integration`; `npm test`; `npm run build`; complete `npm run test:browser` direct/iframe portrait/landscape DPR1/3 shell/camera/grid matrix; `npm run test:environment-controls-browser`; `npm run test:release-target-policy`; `npm run test:release-pack-policy`; and dry pack at unchanged `@aerobeat/web-assembly@0.0.36`, 107 files, packed `16,144,328` bytes, unpacked `17,038,652`, SHA-1 `2562c4fca29ba5fbe8be3d32b9bbbddd8cbc659d`, integrity `sha512-02p4E9/6loM4D+BN+2iLGkZQ1gLyy9m1dA8NF7DoER3aVUtR6Iwir+0YYpwzvbI4V000hFXkO9xsdFi2mAcJLQ==`.
- Independent candidate QA `50cef25c-90a5-42a9-b8cf-d86c1742dbb8` and final code audit `1b2255b7-7ae0-4dbb-85af-88d80bc1f86b` were verified running against the complete dirty candidate with no edit/commit/ledger/server authority; their final disposition is recorded in the landing evidence below.
- Independent QA `50cef25c-90a5-42a9-b8cf-d86c1742dbb8` returned PASS on exact pins/fingerprint/bundle ownership, genuine insecure iframe/local and secure direct/remote production rows, all-twelve raw snapshot, diff hygiene, unchanged `0.0.36`, and absence of tags/releases/npm publication; its follow-up also passed the actual cancellation row/pin-drift assertion.
- Independent auditor `1b2255b7-7ae0-4dbb-85af-88d80bc1f86b` initially returned FAIL because release proof did not itself reject dirty sibling bytes, routing allowed arbitrary local ports, maps were double-counted rather than graph-bound, physical shared-asset deletion was not observed, live public refs were not read, and composition mismatch rows were incomplete. Persistent report `.plans/debug/2026-09-03-i4u-assembly-audit-gaps.md` and claimed discovered Bead `4b6` preceded repair. Final code now rejects dirty proof inputs, reads live public refs, permits exact ephemeral origins only, deduplicates three physical maps and requires exact main + conversion Worker attribution, observes all three IndexedDB stores, and executes package/chart/audio/GLB/JPEG one-byte failure rows plus actual cancellation. Fresh independent re-audit disposition follows in landing evidence.
- Seven clean exact-pinned official suites PASS, including every package's genuine insecure gate: hash `npm test`, browser Window/module Worker secure/insecure matrix, performance, build, and exact pack; vendor `npm test` plus browser secure/insecure download/local/provider/raw/version/tamper matrix; authoring `npm test` (security plus real 4858/3D44B) and browser Window/module Worker/IndexedDB matrix; content `npm test` plus secure/insecure package/chart/asset/audio tamper matrix; audio `npm test` plus secure/insecure default-hash/decode/lifecycle matrix; renderer `npm test` plus browser all-seven-GLB/all-eight-JPEG/context/tamper matrix; gameplay `npm test` plus browser lifecycle matrix. All seven remained clean `main...origin/main` afterward.
- Before and after implementation, the new immutable snapshot gate compared every byte and tracked tree for all twelve raw predecessors `0.0.24`, `0.0.26` through `0.0.36` against clean public baseline `e37cc9fd28e7d061190829f7f0966cb8295d5826`. Raw `0.0.36` stayed tree `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, 41 files / `27,760,611` bytes, proof SHA-256 `3d18dd99afe99fcac389bccee760073baf83cc44c66cdec6d0ac5d933d142daf`; `0.0.35` and `0.0.34` retained their prior exact trees/counts/bytes/proofs. Final audit/push evidence follows below; no version bump, raw successor, publication, tag, GitHub Release, network acquisition, history rewrite, server change, or physical approval occurred.

## `i4u.1.5` replacement integration coder reconciliation — LANDING CANDIDATE PASS (2026-09-04)

- Replacement coder resumed the preserved cumulative candidate after the DSH/router restart, re-read the complete README, all 552 prior plan lines, every requested Bead comment, all five named debug reports, and the complete changed/new implementation before editing. No good predecessor work was discarded and no product contract was weakened. The earlier audit repairs remain intact: release fingerprint/proof require clean exact sibling commit/tree bytes; live public main is checked by focused provenance; routing permits only exact ephemeral child/parent origins; source maps bind exact main plus conversion Worker; package/chart/audio/GLB/JPEG mutation and packages/collections/assets cleanup/cancellation are executable; reversed Hard/Expert intervals remain fail-closed with ExpertPlus selected only for downstream proof.
- Reverified every audited sibling against exact local HEAD, local `origin/main`, live public `refs/heads/main`, commit/tree, SSH origin, and empty porcelain. Exact pins remain shared `be7249b0`/`b423c674`, vendor `4d2479df`/`40c8055a`, authoring `08b330d9`/`5b8e64f4`, content `501bddac`/`89e57f58`, audio `2bd5c6bc`/`4bb2a110`, renderer `8d2554b6`/`b2d9dabe`, and gameplay `e7e83eda`/`3da9eeaf`. `npm ls --all` passed with one linked shared hash owner through assembly and all six consumers; strict BeatSaver version declaration policy and duplicate provider ordering remain consumer-owned.
- Fresh focused production integration passed all four exact rows in `51.05 s`: insecure direct/remote and genuine iframe/local first asserted child Window and module Worker `isSecureContext=false` plus absent `crypto.subtle`; secure localhost direct/remote and `127.0.0.1` parent → `localhost` child iframe first asserted secure Window/Worker SubtleCrypto. Exact 4858 archive SHA-256 `4273f030…`, raw SHA-1 `6eb806bb…`, provider SHA-1 `431ffaa5…`, three real conversion Workers, atomic IDB persistence/reload, valid ExpertPlus content/audio/Test, seven GLBs, all eight JPEG environments, Flow/Lanes/Grid, deterministic export, URL revocation, deletion, cancellation, worker termination, and zero network escape/noise passed. Package/chart/audio mismatch codes remained `export_package_hash_mismatch` / `chart_hash_mismatch` / `audio_declaration_mismatch`; one-byte GLB/JPEG responses reached renderer error states. Insecure camera remained unavailable and no camera request occurred.
- Fresh assembly gates passed without threshold changes: `npm test` in `17.19 s` (including immutable 12-release snapshot, release target, exact 24 environment + 17 gameplay inventories, fingerprint `fb68f470674d051606576a41cb3762e46e134fec3815ed1a573f41554bad7d3a` over `199` inputs, and production ownership `4` scripts / `3` physical maps / exact main + conversion Worker); `npm run build` in `0.64 s`; complete `npm run test:browser` in `14:19.83`; dedicated `npm run test:shell-matrix` in `11:29.70`; environment controls in `13.75 s`; and release target/pack policy in `33.75 s`. Only the established Vite PlayCanvas worker externalization/chunk-size warnings appeared during build.
- Fresh exact-pinned sibling gates all passed and left every repository clean: shared hash unit/browser/performance/build/pack (`128 MiB` SHA-1 policy vector completed in `1,952 ms`; pack 5 files / `7,178` packed bytes); vendor unit/browser strict 16-accept/51-reject version matrix; authoring unit/security/real-4858/3D44B plus Window/module-Worker/IDB browser matrix; content unit/browser; audio unit/browser; renderer unit/browser secure+insecure seven-GLB/eight-JPEG mutation/lifecycle; gameplay unit/browser. No sibling byte or ref changed.
- Exact dry pack remains unchanged `@aerobeat/web-assembly@0.0.36`: `107` files, `16,144,328` packed bytes, `17,038,652` unpacked bytes, SHA-1 `2562c4fca29ba5fbe8be3d32b9bbbddd8cbc659d`, integrity `sha512-02p4E9/6loM4D+BN+2iLGkZQ1gLyy9m1dA8NF7DoER3aVUtR6Iwir+0YYpwzvbI4V000hFXkO9xsdFi2mAcJLQ==`. An actual `npm run build-release` safely failed with `EEXIST` at existing immutable `release/raw/0.0.36` before Vite; before/after candidate status matched and the complete twelve-release snapshot passed again.
- Raw `0.0.36` remains exact tree `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, `41` files / `27,760,611` bytes, proof `3d18dd99afe99fcac389bccee760073baf83cc44c66cdec6d0ac5d933d142daf`; raw `0.0.35` and `0.0.34` remain exact predecessor trees `bd69d3bd309660125d1a5ac3da6d07896c49bb96` and `1c8db5dff7c0136a6e5a22c6622f4a5409511f70`. No `0.0.37`, tag, GitHub Release, npm publication, server, network acquisition, history rewrite, or physical claim exists. `aerobeat-web-assembly-ncg` remains the separate open normalization lane. Landing intentionally leaves `i4u.1.5` and repair `4b6` open for fresh independent QA/audit; later `i4u.2`/`i4u.3` own independent QA and release authorization.

## `i4u.2` independent secure/insecure hashing QA — PASS (2026-09-04)

- QA claimed `aerobeat-web-assembly-i4u.2` and began from exact clean pushed implementation `HEAD == origin/main ==` live public `refs/heads/main == 0486e93acc7254b43d4ae7afe8288090b584c033`, tree `00cfa7924b3565b8d922a5a890ffb280f635b977`. The implementation diff passed `git diff --check`; no production source or raw release byte was edited by QA.
- Independently verified all seven clean local/origin/live-public sibling pins and trees: hash `be7249b0` / `b423c674`, vendor `4d2479df` / `40c8055a`, authoring `08b330d9` / `5b8e64f4`, content `501bddac` / `89e57f58`, audio `2bd5c6b` / `4bb2a110`, renderer `8d2554b` / `b2d9dabe`, and gameplay `e7e83ed` / `3da9eeaf`. `npm ls --all` resolved one linked shared hash owner through assembly and all migrated consumers.
- The complete shared-hash package gate passed 19 SHA-1/SHA-256 FIPS/NIST, million-`a`, binary, padding, slice, partition, randomized differential, incremental/finalization/reset, tamper, hostile-input, mutation-snapshot, native/fallback-parity, malformed-native-fallback, and bounded-diagnostic tests. Its real Chromium matrix passed secure localhost and genuine insecure `100.113.165.57` Window plus module Worker contexts; the 128 MiB incremental SHA-1 policy vector completed with zero buffered tail growth, and build/exact five-file pack passed. Production source contains no fetch, dynamic import, external URL, WebAssembly/`.wasm`, dependency, secure-context assumption, or hash-skip path.
- Assembly `npm run test:hash-provenance` reproduced fingerprint `fb68f470674d051606576a41cb3762e46e134fec3815ed1a573f41554bad7d3a` over 199 inputs and production ownership of exactly four emitted scripts / three deduplicated source maps. Exact shared source attribution is present in main `assets/index-C0dcxzw_.js` and module Worker `assets/conversion-worker-ugoIXsD9.js`; every production-source direct SubtleCrypto digest belongs only to `aerobeat-web-hash/src/index.js`, and emitted WASM is rejected.
- Focused production integration passed all four exact rows: insecure non-loopback direct/remote and genuine cross-origin iframe/local first proved Window and pre-conversion module Worker `isSecureContext=false` with absent `crypto.subtle`; secure localhost direct/remote and `127.0.0.1` parent → `localhost` child iframe first proved secure Window/Worker WebCrypto. Parent and child iframe origins differed, no trust override was used, and insecure camera remained explicitly unavailable with zero camera requests.
- The real 4858 fixture reproduced archive SHA-256 `4273f0305518aa79ae4a7b58ccb07e86704ba50dbe679d0f0e29c52cb7b6beed`, raw SHA-1 `6eb806bbc3935f28d8a8f396cad5030f08e1e25d`, and provider SHA-1 `431ffaa53a1e45ffab6c81a895e456f6aad1e038`. Latest/search/download or local Blob import, three real authoring Workers, atomic IndexedDB persistence, fresh-graph reload, valid ExpertPlus selection, content/assets/audio decode, trusted unscored Test playback, seven GLBs, all eight environments with one resident texture, Flow/Lanes/Grid, deterministic export, URL revocation, all three store deletion, cancellation, Worker termination, and zero network escape/unexpected noise passed. Hard/Expert reversed legacy obstacle intervals rejected fail-closed with exact `event_interval_invalid` before valid ExpertPlus downstream proof.
- Executable one-byte tampering remained fail-closed: package `export_package_hash_mismatch`, chart `chart_hash_mismatch`, shared audio declaration `audio_declaration_mismatch`, and renderer GLB/JPEG error states. Strict v4 integration also retained unsupported explicit-version rejection and provider tamper rejection with no commit.
- Full assembly gates passed: `npm test`; production `npm run build`; complete `npm run test:browser`; dedicated environment-controls direct/real-iframe portrait/landscape DPR1/3 matrix; release-target and release-pack policy; and exact dry pack. Browser coverage passed product shell, compositor, gameplay cursors, square-grid pixels, audio, mobile controls, preview/library/lifecycle/privacy, all 24 Flow/Lanes/Grid shell rows, and zero unexpected noise. Dry pack remained version `0.0.36`, exactly 107 files / `16,144,328` packed / `17,038,652` unpacked bytes, SHA-1 `2562c4fca29ba5fbe8be3d32b9bbbddd8cbc659d`, with no raw release, plan, or Beads payload.
- Release/immutability checks passed: an actual `npm run build-release` refused the existing immutable `0.0.36` before Vite and preserved status; raw `0.0.36` remains exactly 41 files / `27,760,611` bytes, tree `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, proof SHA-256 `3d18dd99afe99fcac389bccee760073baf83cc44c66cdec6d0ac5d933d142daf`, and byte-unchanged from pre-implementation `e37cc9f`. Raw `0.0.37` is absent. There is no local/remote `0.0.36` tag, GitHub Release, or npm publication.
- QA disposition is PASS. Leave `i4u.2`, implementation Beads, and parent recovery Beads open/in progress for auditor-owned closure. No managed physical-review server was inspected or changed, no successor was created, and no remote physical camera/calibration approval is claimed.

## `i4u.3` final independent pre-release source audit — PASS; raw `0.0.37` build authorized (2026-09-04)

- Final audit began from and repeatedly fetched exact clean QA-evidence `HEAD == origin/main ==` live public `refs/heads/main == 3b09943936d7cfdc6fb3811d149d605efe95a79b`, tree `2505094019ceed07cdae3be3d3156f5a57466675`. Its direct parent implementation is `0486e93acc7254b43d4ae7afe8288090b584c033`, tree `00cfa7924b3565b8d922a5a890ffb280f635b977`; the implementation diff is the documented assembly/runtime-validator integration and the QA-only diff changes exactly this plan by 13 lines. Both diffs pass `git diff --check`.
- Independently reverified all seven local HEAD, local `origin/main`, live public main, tree, SSH origin, and clean worktree pins exactly: hash `be7249b0bdfffcab568b760c1b582bfe2a0c1e92` / `b423c6742c07f56dde196d9f60f2e23c51ad913c`; vendor `4d2479df0d4b12305cc8190dbe918995abae5d03` / `40c8055acab241614272fb922a51c1d092e6dd08`; authoring `08b330d9f519113793eb5041d942aa1e8e2095dc` / `5b8e64f42f1f2d9e341c90fe6fc5688646051fa0`; content `501bddac3fcf2aa6332690dc6ac16ad73e8140e2` / `89e57f58598763cd6ef11fb49b227c7691cbb4ca`; audio `2bd5c6bc96d001f9755b6d8b2a79c57cd8e196f1` / `4bb2a110ddeab77559d1f7ffcf5d3e348a93f027`; renderer `8d2554b6a9cd08636afd189e292b19a8bd75a1ad` / `b2d9dabe7aa602e161e0c599ee267ff28f07c711`; gameplay `e7e83edacd20a6691b297e3e220708f3034e4f34` / `3da9eeafed18a230dd2baa1996c93443af7dd217`. `npm ls --all` resolves the one linked shared owner through assembly and all six consumers.
- Source review and fresh shared-package gates confirm deterministic incremental pure-JS SHA-1/SHA-256 with a bounded 64-byte block, exact WebCrypto auto fast path, input snapshotting, malformed/rejected-native fallback, and backend forcing used only by tests. Production sources contain no direct SubtleCrypto digest outside the shared owner and no runtime fetch, external dynamic import, WASM, Node production dependency, or hash-skip route. Shared `npm test`, browser secure/insecure Window+module-Worker matrix, performance, build, and exact pack passed: 19 algorithm/adversarial/parity/lifecycle tests, 128 MiB SHA-1 in 1,929 ms with zero buffered-tail growth, and five files / 7,178 packed bytes.
- Fresh assembly `npm test` and `npm run test:hash-provenance` passed fingerprint `fb68f470674d051606576a41cb3762e46e134fec3815ed1a573f41554bad7d3a` over 199 inputs and exact production ownership of four scripts / three deduplicated physical maps, including main `assets/index-C0dcxzw_.js` and conversion Worker `assets/conversion-worker-ugoIXsD9.js`. A temporary untracked sibling sentinel independently proved release provenance rejects dirty input bytes; cleanup restored the sibling to exact clean state.
- Fresh `npm run test:insecure-hash-integration` passed all four production rows: genuine non-loopback insecure direct/remote and iframe/local proved Window and pre-conversion Worker `isSecureContext=false` with absent `crypto.subtle`; secure loopback direct/remote and genuine cross-origin iframe/local proved WebCrypto. Exact-origin routing admitted only each ephemeral child and parent origin and emitted no network escape/noise. Insecure camera remained unavailable with zero camera requests.
- Real 4858 reproduced exact archive SHA-256 `4273f0305518aa79ae4a7b58ccb07e86704ba50dbe679d0f0e29c52cb7b6beed`, raw SHA-1 `6eb806bbc3935f28d8a8f396cad5030f08e1e25d`, and provider SHA-1 `431ffaa53a1e45ffab6c81a895e456f6aad1e038`. Three real conversion Workers, atomic IndexedDB persistence/reload, valid ExpertPlus content/audio/Test, seven GLBs, eight environments, Flow/Lanes/Grid, deterministic export, deletion, cancellation, and teardown passed. Malformed Hard and Expert remain fail-closed with stable `event_interval_invalid`; ExpertPlus remains valid end-to-end. Separate open `aerobeat-web-assembly-ncg` owns eventual legacy-duration normalization without weakening this boundary.
- Tamper rows passed exact package `export_package_hash_mismatch`, chart `chart_hash_mismatch`, audio `audio_declaration_mismatch`, and renderer GLB/JPEG error states. Both delete and cancellation paths directly observed IndexedDB `packages`, `collections`, and `assets` counts at zero. Fresh vendor `npm test` passed strict metadata admission across 16 accepted and 51 rejected inspector/acquisition/local-import rows; assembly retained explicit `5.0.0` and `4.0.0junk` rejection plus versionless-v4 acceptance.
- Fresh release target and pack-policy gates passed. Exact dry pack remains `@aerobeat/web-assembly@0.0.36`, 107 files, `16,144,328` packed / `17,038,652` unpacked bytes, SHA-1 `2562c4fca29ba5fbe8be3d32b9bbbddd8cbc659d`, integrity `sha512-02p4E9/6loM4D+BN+2iLGkZQ1gLyy9m1dA8NF7DoER3aVUtR6Iwir+0YYpwzvbI4V000hFXkO9xsdFi2mAcJLQ==`. Actual guarded `npm run build-release` refused existing immutable `0.0.36` with `EEXIST` before Vite and preserved status/tree/proof.
- All twelve raw predecessors passed the independent byte snapshot. Raw `0.0.36` remains exactly tree `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, 41 files / `27,760,611` bytes, proof SHA-256 `3d18dd99afe99fcac389bccee760073baf83cc44c66cdec6d0ac5d933d142daf`. Raw `0.0.37` remains absent. No matching local/remote tag, GitHub Release, or npm publication exists. The auditor did not start, stop, bind, replace, serve, or otherwise alter a physical-review server; no listener was observed on port 5173 during the final read-only check, so served-byte and physical review remain explicitly outstanding.
- Research child `61f83035-97fc-49c7-8e1a-060390c46147` was verified running and returned an independent read-only PASS for repair `4b6`, fixture/version/release evidence, and candidate mapping. Child `6c76ee4e-2d68-42a2-90c3-61aa80b185c4` was verified running for a redundant hashing review, was stopped after the parent independently completed the required gates, and returned PASS with no blocker; parent evidence, not either child alone, owns disposition.
- Nonblocking release-lane cautions: backend forcing remains a public shared-package test hook, but production source has no forced call and all ordinary consumers use auto/incremental behavior; source-map ownership relies on complete `sourcesContent`, cross-checked here against emitted calls and exact source provenance; the immutable snapshot prints a differently scoped aggregate than README's separately documented complete-manifest aggregate, while the required raw tree/file/byte/proof identities reproduce exactly. Reconcile aggregate naming/scope during successor certification without changing predecessor bytes.
- Final disposition: `i4u.2`, `i4u.1.5`, repair `4b6`, and implementation parent `i4u.1` close PASS. Umbrella bug `i4u` and audit/release lane `i4u.3` remain open/in progress because raw successor construction, independent release reproduction, served-byte audit, and Derrick physical review remain.
- **Explicit authorization:** the next release lane is authorized to apply the repository patch policy exactly once (`0.0.36 → 0.0.37`) and build exactly one append-only immutable raw `0.0.37` from the audited pushed source. This authorization does not permit changing any `0.0.36` byte, creating any other version, publishing npm/GitHub Release/tag artifacts, replacing the physical-review server before independent reproduction/served-byte acceptance, or claiming physical approval.

## Immutable raw `0.0.37` builder result — PASS (2026-09-04)

- Builder claimed `aerobeat-web-assembly-i4u.3` and began from exact clean public `HEAD == origin/main ==` live public `refs/heads/main == c889751eee7ae120c97ef26437e5437ca2d82143`, tree `8d959f6b158cc798923bc4405928edb716f1b683`. Raw `0.0.37` was absent. Raw `0.0.36` was unchanged at tree `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, 41 files / `27,760,611` bytes, proof SHA-256 `3d18dd99afe99fcac389bccee760073baf83cc44c66cdec6d0ac5d933d142daf`; raw `0.0.35` remained tree `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, 20 files / `13,878,153` bytes, proof `22c41e8bf0630bb6b50523a96ab0e886b399a93c74a7d1e97bb2afe47a43c4ea`.
- Canonical `npm run version:patch` ran exactly once and changed package, lock top-level/root package, and release-proof HTML metadata exactly `0.0.36 → 0.0.37`; no other version was created. The newly applicable exact-version provenance assertion was advanced from the implementation-lane hold (`0.0.36`) to the authorized release successor (`0.0.37`) after the first truthful `npm test` exposed it; this preserves a fail-closed exact-version gate rather than weakening or bypassing it.
- Pre-build PASS gates on the corrected bumped source: `npm test`; `npm run test:hash-provenance`; `npm run test:release-pack-policy`; `npm run build`; exact sibling-clean/provenance checks; absent-target release policy; and package dry run. Fingerprint before release was `c09983b7420cae205eb72c61af2eeba005b1a51b5a9492e5a9e08e7cf4bb5698` over 199 inputs. With `AEROBEAT_BASE_PATH=/`, canonical `npm run build-release` ran exactly once and successfully claimed/created the absent target; it was not rerun and no output byte was mutated afterward.
- Raw `release/raw/0.0.37` is exactly 41 regular files / `27,820,403` bytes (`27,817,604` before the 2,799-byte proof), staged immutable tree `6d2b8c4e39d3677f28e48ad076bc6259abcd47b9`, proof SHA-256 `9415f1ee7f9ddc687b4756be84e5a2bec9dfa9521eb5ec8f6c6ddc5b9ee286f9`, and globally line-sorted complete-manifest aggregate `5994c06810b832fb8987e66269cb199a434a8bd5a4f575212ee6d33d4ff7bd50` over `sha256␠␠repo-relative-path` rows. Category totals are proof 1 / 2,799 B; HTML 1 / 527 B; CSS 1 / 1,303 B; JavaScript 4 / 4,381,552 B; source maps 3 / 7,391,423 B; environments 24 / 16,013,891 B; gameplay GLBs 7 / 28,908 B.
- Proof/bundle validation passed exact raw/unminified/base `/` policy, four scripts, three deduplicated physical source maps, two shared-source attributions owned by `assets/index.js` and `assets/conversion-worker-DWPeGCGS.js`, and zero emitted WASM. Runtime scripts are conversion Worker, main index, MediaPipe Worker, and pinned vision bundle. Locked MediaPipe-only backend/model/version/threshold/cadence markers and all forbidden runtime asset/marker rejections remain intact.
- Final package dry-run identity is `@aerobeat/web-assembly@0.0.37`, 107 entries, `16,144,515` packed / `17,039,061` unpacked bytes, SHA-1 `036978384085f966a47d11d1940bdb4eec44e536`, integrity `sha512-wEtZ7PxXbvWDEg2ARg2WURZZ3g4BCBBBSO3nyR+0AaiDyE94cHZnDIEHHUBJwyVvcswbagncHIqrWiHhSIyHhg==`. Raw release storage is intentionally Git-tracked with forced add under the repository's ignored-release policy; it is not an npm package member.
- Post-creation immutable validation passed the canonical 12-release predecessor snapshot plus exact successor proof/inventory/tree/bundle checks. The snapshot validator's `aggregate` is path-ordered over repository-relative rows (`0.0.35` `2813c920…`, `0.0.36` `7ae94c2c…`, `0.0.37` `6d76f01c…`), while README's `complete-manifest aggregate` is globally line-sorted over the same complete rows (`0.0.35` `a111efba…`, `0.0.36` `575fb351…`, `0.0.37` `5994c068…`). These are deliberately different orderings/names, not byte disagreement; predecessor bytes were not changed and no check was weakened.
- `i4u.3` and umbrella `i4u` remain open/in progress. Independent clean-checkout release reproduction, served-byte audit, and Derrick's physical secure-context/insecure-context review remain required. This builder did not serve `0.0.37`, start or alter a listener, publish npm, create a tag/GitHub Release, acquire assets, change runtime splats, rewrite history, or claim physical approval.

## Independent immutable raw `0.0.37` reproduction and release audit — PASS (2026-09-04)

- Audit began from exact clean public builder commit `238743bdbfb015ac6f4fa304865af121d94b2eaa`, commit tree `57b0a2420a241e7f235b557115d84653541555fc`; local `HEAD`, local `origin/main`, and live public `refs/heads/main` matched. The range from authorized source `c889751eee7ae120c97ef26437e5437ca2d82143` is exactly one commit: six narrow metadata/evidence files plus exactly one new 41-file tracked directory, `release/raw/0.0.37`. No predecessor release path changed. Package, lock top-level/root package, source proof meta, raw proof meta, and the truthful fail-closed provenance assertion move exactly once from `0.0.36` to `0.0.37`; README records the successor and canonical complete-manifest identity, while the prior builder evidence and this audit explicitly define its ordering.
- Full `git diff --check c889751..238743b` reports exactly 173 trailing-whitespace rows, all in generated immutable `release/raw/0.0.37/assets/index.js`; the same generated PlayCanvas shader payload has exactly 173 such rows in raw `0.0.35`, `0.0.36`, and `0.0.37`. The source/metadata diff excluding the immutable generated raw directory passes with exit `0`. This is deterministic bundled shader text, not a source hygiene regression, and no raw byte was changed to suppress it.
- Independent raw computation PASS: exactly `41` regular files / `27,820,403` bytes, including a `2,799`-byte proof and `27,817,604` pre-proof bytes; all files match committed Git blobs and tree `6d2b8c4e39d3677f28e48ad076bc6259abcd47b9`. Proof SHA-256 is `9415f1ee7f9ddc687b4756be84e5a2bec9dfa9521eb5ec8f6c6ddc5b9ee286f9`; fingerprint is `c09983b7420cae205eb72c61af2eeba005b1a51b5a9492e5a9e08e7cf4bb5698` across `199` inputs. Category totals reproduce exactly: proof `1 / 2,799 B`; HTML `1 / 527 B`; CSS `1 / 1,303 B`; JavaScript `4 / 4,381,552 B`; maps `3 / 7,391,423 B`; environments `24 / 16,013,891 B`; gameplay GLBs `7 / 28,908 B`; other `0`.
- Proof internals PASS: raw/unminified, base `/`, exact package/version, seven exact dependency commit/tree records, locked MediaPipe-only GPU-WebGL Pose Landmarker Lite float16 `/1/`, Tasks Vision `1.0.1`, thresholds `0.5 / 0.5 / 0.5`, standard/fast/full/measured, `15 fps`, four runtime scripts, three deduplicated physical maps, and zero runtime WASM. Exact shared-hash attribution is only main `assets/index.js` plus conversion Worker `assets/conversion-worker-DWPeGCGS.js`; source-map validation finds no direct production SubtleCrypto digest outside `aerobeat-web-hash/src/index.js` and no shared-source fetch, external URL, dynamic import, WASM, or hash-skip path.
- Aggregate reconciliation PASS over the same 41 exact `(SHA-256, repository-relative path)` rows: snapshot/path order hashes to `6d76f01ce5cb2d39e967289421ec4b52e8589c87d2b43100a828d76dae422985`; globally lexicographically line-sorted complete-manifest order hashes to `5994c06810b832fb8987e66269cb199a434a8bd5a4f575212ee6d33d4ff7bd50`. Predecessors similarly retain path/global pairs `0.0.35` `2813c920…` / `a111efba…` and `0.0.36` `7ae94c2c…` / `575fb351…`. The different names and row order alone explain the values; file membership, paths, and bytes are identical.
- Fully isolated reproduction PASS under exact Node `22.22.3` / npm `10.9.8`: created disposable no-hardlink local clones for assembly plus all fourteen recursively linked siblings, detached assembly at `238743b` and the seven provenance owners at their exact pins, copied installed dependency trees into the disposable sibling topology, and verified fingerprint `c09983b… / 199`. A canonical 41-row manifest was preserved before deleting only the disposable clone's tracked target. One canonical `AEROBEAT_BASE_PATH=/ npm run build-release` recreated the target at the exact in-repo depth. Every reproduced path, byte length, SHA-256, and byte payload matched canonical raw `0.0.37`; the reproduced proof, total, fingerprint, and complete-manifest aggregate match the identities above. The rebuilt clone returned Git-clean because it reproduced all tracked bytes exactly, and the complete disposable lab was removed. An earlier non-authoritative `/tmp` worktree probe intentionally demonstrated that symlinking canonical `node_modules` across a different sibling topology changes source maps/chunk hashes; it was discarded cleanly and was not used as release evidence.
- Validation PASS: `npm test`; `npm run build`; `npm run test:release-pack-policy`; full `npm run test:browser`; direct `npm pack --dry-run --json --ignore-scripts`; shared-hash `npm test`, performance, build, pack, and secure/insecure Window/module-Worker browser matrix; exact seven local/origin/live-public pin/tree/clean checks; `npm ls --all`; and an actual guarded canonical `AEROBEAT_BASE_PATH=/ npm run build-release` refusal. The refusal failed with `EEXIST` before Vite and a complete before/after manifest proved no canonical byte changed. Full browser coverage passed genuine insecure direct/iframe rows with absent SubtleCrypto, secure controls, exact 4858 SHA-1/SHA-256 identities, three real Workers, persistence/reload, package/chart/audio/GLB/JPEG tamper rejection, lifecycle/cleanup, all 24 Flow/Lanes/Grid shell rows, environments/camera/cursors/square-grid matrices, and zero unexpected noise.
- Package identity independently reproduces exactly `@aerobeat/web-assembly@0.0.37`, `107` entries, `16,144,515` packed / `17,039,061` unpacked bytes, SHA-1 `036978384085f966a47d11d1940bdb4eec44e536`, integrity `sha512-wEtZ7PxXbvWDEg2ARg2WURZZ3g4BCBBBSO3nyR+0AaiDyE94cHZnDIEHHUBJwyVvcswbagncHIqrWiHhSIyHhg==`; raw release storage is absent from the package. Raw `0.0.36` remains exactly tree `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, `41` files / `27,760,611` bytes, proof `3d18dd99afe99fcac389bccee760073baf83cc44c66cdec6d0ac5d933d142daf`; raw `0.0.35` remains tree `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, `20` files / `13,878,153` bytes, proof `22c41e8bf0630bb6b50523a96ab0e886b399a93c74a7d1e97bb2afe47a43c4ea`. The canonical predecessor snapshot passes all twelve releases.
- Publication/server boundary PASS: no matching local or remote tag, GitHub Release list is empty, npm returns expected `E404` for `@aerobeat/web-assembly@0.0.37`, and no listener exists on port `5173`. This audit did not start, stop, bind, replace, serve, or otherwise alter a physical-review server; did not publish, rewrite history, acquire assets, or change splats; and makes no physical-review or camera approval claim.
- Final disposition: close only `aerobeat-web-assembly-i4u.3` PASS. Leave umbrella `aerobeat-web-assembly-i4u` and physical review `aerobeat-web-assembly-k72.17` in progress. **The exact audited bytes at `release/raw/0.0.37`—and only those bytes with tree `6d2b8c4e39d3677f28e48ad076bc6259abcd47b9` and proof SHA-256 `9415f1ee7f9ddc687b4756be84e5a2bec9dfa9521eb5ec8f6c6ddc5b9ee286f9`—are authorized to be served for renewed physical review.** Serving/replacing the review process remains a separate operator action; physical approval remains pending.

## Independent physical-review served-byte audit of raw `0.0.37` — PASS (2026-09-04)

- Audit began from a clean checkout with local `HEAD`, fetched `origin/main`, and live public `refs/heads/main` all exactly `9217cdba19841b70d95a5caaeb13cfaec8a8af67`. The tracked raw directory independently resolved to tree `6d2b8c4e39d3677f28e48ad076bc6259abcd47b9`, exactly `41` files / `27,820,403` bytes, proof SHA-256 `9415f1ee7f9ddc687b4756be84e5a2bec9dfa9521eb5ec8f6c6ddc5b9ee286f9`, and globally line-sorted complete-manifest aggregate `5994c06810b832fb8987e66269cb199a434a8bd5a4f575212ee6d33d4ff7bd50` without trusting the handoff values.
- Port `5173` had exactly one listening socket, inode `22938075`, owned only by PID `2751790` / fd `3`, bound to `0.0.0.0:5173`. Its executable resolved to Python `3.14.2`; cwd was exactly this owning repository; argv was exactly `python3 -m http.server 5173 --bind 0.0.0.0 --directory release/raw/0.0.37`; cwd plus the relative directory resolved exactly to this repository's canonical raw `0.0.37`. No second listener/server existed, and the process was not stopped, restarted, replaced, or mutated.
- A canonical per-file manifest was computed for every regular file and every path was fetched from `http://100.113.165.57:5173/` with identity encoding. All `41/41` responses were `200` and matched the canonical path set, byte length, SHA-256, and payload exactly; totals and both release aggregates above reproduced. `/` returned the exact `527`-byte `index.html` with `text/html`, not a directory listing. Representative JavaScript, CSS, source map, JSON, JPEG, and GLB responses returned usable types `text/javascript`, `text/css`, `application/octet-stream`, `application/json`, `image/jpeg`, and `model/gltf-binary` respectively. Plain, singly encoded, mixed encoded, and double-encoded parent traversals, root-level source/package probes, predecessor `0.0.36` paths, and nonexistent root/asset paths returned `404`; an in-root normalized `assets/../aerobeat-release-proof.json` returned only the exact canonical proof.
- Headless Chromium loaded the actual Tailscale URL, not localhost, with HTTP `200`, title `AeroBeat Web Assembly`, and proof meta `0.0.37`. Window and a real module Worker both reported `isSecureContext === false`, `crypto` object present, `crypto.subtle === undefined`, and callable `getRandomValues`; public capabilities truthfully reported `secureContext:false`, `camera:false`, `camera_unavailable`, IndexedDB and Worker support. The renderer reached ready state with all seven canonical gameplay GLBs, no fallback, and the default Luminious Ice Cave JPEG environment ready. Boot produced zero page errors, failed requests, warning/error console noise, or external runtime URLs; all observed boot/runtime asset responses were same-origin successes.
- Actual insecure-origin fallback hashing/import PASS used the existing exact local 4858 archive (`7,552,654` bytes; SHA-256 `4273f0305518aa79ae4a7b58ccb07e86704ba50dbe679d0f0e29c52cb7b6beed`) through a test-only Playwright same-origin route without modifying the managed server. With Window/Worker SubtleCrypto absent, the served production vendor path reproduced raw SHA-1 `6eb806bbc3935f28d8a8f396cad5030f08e1e25d` and provider SHA-1 `431ffaa53a1e45ffab6c81a895e456f6aad1e038`; three real conversion Workers authored and atomically persisted three packages/one collection, then terminated. The known independent Hard/Expert negative-obstacle defect remained fail-closed as `event_interval_invalid`; selecting interval-valid ExpertPlus reached content/audio `ready`, retained exact lineage, and exposed five variants. This exercises the bundled shared fallback rather than the unavailable former `crypto.subtle.digest` path; the former crash did not recur and integrity was not skipped or weakened.
- Live external BeatSaver was available and also passed from the actual insecure Tailscale page: exact map/version `53F26` / `addd9d6f8e7340ad6f5633947136d8475a7a99b5` fetched only the expected BeatSaver API and CDN URLs, authored/persisted one package, reached content `ready` with exact source/lineage hash and five variants, emitted no failures/noise, and was deleted from the ephemeral browser context afterward.
- Post-audit all-file fetch and canonical manifest recheck remained byte-identical; PID/listener/cwd/argv were unchanged; the repository remained clean at the initial commit before this evidence edit. No matching local/remote tag exists, GitHub Release list is empty, and npm returned expected `E404` for `@aerobeat/web-assembly@0.0.37`. No release/raw/predecessor byte, server state, asset, splat, publication, or history was changed. This served-byte subaudit leaves both `aerobeat-web-assembly-k72.17` and umbrella `aerobeat-web-assembly-i4u` in progress: Derrick's physical review and approval are still required.

## Derrick physical review of raw `0.0.37` — REJECTED; diagnosis only (2026-09-04)

- Derrick supplied four physical-review screenshots and exact follow-up requirements. Directional arrows still render visually clear/translucent rather than opaque; he explicitly requires a root-cause report before any arrow fix is implemented. Diagnosis Bead: `aerobeat-web-assembly-10c`.
- The review was incorrectly exposed over insecure `http://` even though earlier AeroBeat physical review used a secure Tailscale route that allowed camera permission across connected tailnet machines. Research must recover and verify the prior secure serving topology before replacing the current reference server. Bead: `aerobeat-web-assembly-0mx`.
- Pitch/yaw/roll/scale transforms apply live, but their numeric labels do not update until Reset or Play; the degree symbol wraps onto a second row. Remove the texts `At centered position, sphere radius scale does not change angular view or zoom.` and `Environment transform applied.` Set the default environment to `Alpine River Valley`, and use `alpine-river-valley-photosphere.environment-config.v1.json` as that environment's config because it contains the updated rotation. Diagnosis Bead: `aerobeat-web-assembly-sl3`.
- Raw `0.0.37` remains immutable. This phase is diagnosis/research only: do not implement an arrow fix or authorize/build a successor until Derrick receives the requested findings. `aerobeat-web-assembly-k72.17` and umbrella `aerobeat-web-assembly-i4u` remain in progress.

## Raw `0.0.37` physical-defect diagnosis — COMPLETE; no implementation (2026-09-04)

- **Arrow is not alpha blended.** Exact raw/source GLB JSON and live PlayCanvas materials are opacity `1`, `BLEND_NONE (3)`, `depthWrite=true`, and `depthTest=true`. A controlled raw-bundle black/white oracle found `4,212` invariant interior pixels and center RGBA `46,54,66,255`; only `277` antialiased edge pixels varied with the background. The intentional resolved-note removal fade remains a separate bounded `80 ms` behavior.
- **Arrow root cause is compound grid occlusion plus wrong-side geometry and low-contrast tint.** A refined exact-raw Chromium probe placed the pending opaque arrow at `z=-1.5`, behind camera-nearer transparent grid cells at `z=0`: enabling the grid altered all `1,381/1,381` sampled arrow pixels with mean RGB shift `114.08` (`[46,54,66,255] → [86,92,102,255]`) while alpha stayed `255`. At crossing `z=0`, `2,147/2,364` arrow pixels remained unchanged because the front cap moved ahead of most grid geometry. Separately, asset release `0.0.3` retained the exact `1,100`-byte geometry BIN from `0.0.2`; the attempted fix only made already-implicit OPAQUE metadata explicit and renamed cyan to `tint_base`. `rear_and_sides()` and `extrude(inset)` generate two coplanar rear caps at runtime-facing `z=+0.09` (`5+5` overlapping triangles), while the clean white-rimmed face is authored for `-Z`. The runtime camera sits at `+Z`; review generation places its camera at `-Z`. Near crossing, `tintMix=0.6667` shifts the remaining runtime-facing surface toward pale white. Existing checks prove metadata/material alpha and nominal draw order but not grid-on/grid-off target occlusion, structural non-overlap, runtime-facing contrast, or per-arrow pixels. Bead `aerobeat-web-assembly-10c` contains both debugging records.
- **Recommended arrow correction:** prevent transparent cells/grid from compositing in front of approaching targets through corrected depth placement or explicit target/grid layering, and source-first re-author `directional-arrow/outline-v1` with closed, styled, non-coplanar opaque geometry on both camera-accessible faces—or at minimum correct the `+Z` runtime-facing face—while retaining base alpha `1`, OPAQUE blending, and depth writes. Prefer an asset correction over a renderer `Y=180°` flip, which risks left/right direction mirroring. Add a GLB structural assertion forbidding coplanar caps, grid-on/grid-off target pixel checks, `+Z/-Z` bright-environment captures, per-arrow black/white alpha invariance, contrast thresholds, every direction/mode/free-fly case, and explicit removal-progress tests.
- **Transform labels:** `applyEnvironmentControls()` updates native values, private config, and renderer synchronously, then calls `renderEnvironmentControls(false)`, which deliberately skips `<output>` projection. Reset/Play later call the default `true` path, creating the delayed-label symptom. The degree glyph is a fourth direct child auto-placed into row two of a three-column grid. Correct with immediate output projection and one no-wrap value/unit wrapper with explicit accessible input/output association. Remove the two requested strings while explicitly clearing stale success status.
- **Alpine authority:** Derrick's supplied `/home/derrick/Downloads/alpine-river-valley-photosphere.environment-config.v1.json` is strict deterministic v1 data, `336` bytes, SHA-256 `69a62f73bc1193b0e9414c2698a269e24ed06c309616dc345a3f5cf80966f977`; its sole semantic difference from the `334`-byte current config (`c1cda19a…`) is Alpine yaw `0 → 180`. Promote it source-first through `aerobeat-environment-community` generator/validator/manifest/catalog aggregate, then repin/sync assembly and make per-entry packaged transforms runtime authority. Set Alpine as fresh/reconnect default without reordering the eight-entry catalog or overwriting a connection-local user selection; do not add browser persistence. Decouple environment classification from default identity.
- **Secure review:** the historical Tailscale Serve route never disappeared. Tailnet-only `https://derrick-alienware-aurora-r13.tail613fcb.ts.net:8443/` already proxies `/` to `http://127.0.0.1:5173`, serves exact raw `0.0.37` bytes, has a valid MagicDNS certificate, reports `isSecureContext=true`, exposes callable `getUserMedia`, and leaves camera permission at `prompt`. The mistake was binding the backend broadly at `0.0.0.0`; implementation should leave Tailscale Serve and unrelated `:8444` untouched, replace only managed job `bash-22` with the same absolute immutable raw root bound to `127.0.0.1`, then re-audit all `41` files, HTTPS/certificate/camera state, one listener/process, tailnet-only status, and direct `:5173` refusal. Bead `aerobeat-web-assembly-0mx` contains the exact switch and rollback procedure.
- No source, asset, bundle, raw release, server, or Tailscale state was changed during diagnosis. Current renderer browser validation passed, demonstrating the test gap rather than disproving the physical defect. This report satisfies Derrick's required diagnose-and-report-before-fix gate; implementation and a new immutable successor remain pending.
- Pending mandatory landing chain: coder `aerobeat-web-assembly-k72.34` → independent QA `aerobeat-web-assembly-k72.35` → independent auditor `aerobeat-web-assembly-k72.36`. Secure serving restoration remains `aerobeat-web-assembly-0mx`; physical approval remains `aerobeat-web-assembly-k72.17`.

## Secure Tailscale physical-review serving restored (2026-09-04)

- Collected and stopped managed broad-bind job `bash-22` via DSH job control; it settled `killed` by `SIGTERM`. Started retained managed job `bash-23` with exact argv `python3 -m http.server 5173 --bind 127.0.0.1 --directory /home/derrick/.dsh/projects/aerobeat/aerobeat-web-assembly/release/raw/0.0.37`; sole listener PID `2962908` owns only `127.0.0.1:5173`, with this repo as cwd and the immutable raw root selected absolutely.
- Existing Tailscale Serve state was left untouched: tailnet-only HTTPS `:8443` proxies to loopback `:5173`; separate tailnet-only DSH `:8444` remains unchanged; no Funnel/public route was enabled. Local loopback and HTTPS return `200`/HTTP2 while direct `http://100.113.165.57:5173/` now refuses connection (`curl` exit `7`).
- Post-switch HTTPS parity PASS: all `41/41` canonical files matched payload bytes exactly, totaling `27,820,403`; proof remains SHA-256 `9415f1ee7f9ddc687b4756be84e5a2bec9dfa9521eb5ec8f6c6ddc5b9ee286f9`. Headless Chromium loaded the MagicDNS HTTPS route with `isSecureContext=true`, `navigator.mediaDevices.getUserMedia` callable, camera permission state `prompt`, and title `AeroBeat Web Assembly`. Raw `0.0.37`, its server root, and Tailscale routes were not mutated.

## Raw `0.0.37` physical-review correction coder — PASS / QA HANDOFF PENDING (2026-09-04)

- Primary coder claimed `aerobeat-web-assembly-k72.34` and verified all four canonical repositories were clean on `main...origin/main` before edits. Owning assembly baseline is exact requested commit `efedd970894d0a1947b0ded3ca3429febeebb6ea`, tree `05d08627efc07ff52b8949df941864e817c83083`; gameplay asset baseline is `8b8b40593b9deb54d32654e39fd7c1c1c4a9dc1a`; environment baseline is `e962fe2a8a48b7e2019d1a55c337abeb6ffd24d4`; renderer baseline is `8d2554b6a9cd08636afd189e292b19a8bd75a1ad`.
- Bounded coder helpers were launched and verified as direct running children: gameplay source/release `0.0.4` child `6a322ce3-46fe-402c-b240-a85573dd7a71`; Alpine canonical-config child `eedd444b-2a6b-4469-bff9-a21c9e937952`; assembly controls/default/config-authority child `83c3907a-a59f-4f56-861d-1120e34d6d5e`. They operate only as coder helpers under already-claimed `k72.34`, have no QA/audit/Bead-closure authority, and may not build assembly raw `0.0.38` or touch server/Tailscale state.
- Implementation follows the diagnosed causal fixes rather than opacity metadata: source-first robust two-face arrow geometry and structural/runtime-facing evidence; explicit renderer ordering that prevents camera-nearer transparent grid cells from compositing over approaching targets while retaining target OPAQUE/depth-write truth and square-grid/red-cell semantics; packaged per-entry environment transforms; immediate live labels and accessible no-wrap units; Alpine fresh default with connection-local lifecycle retention; exact requested text removals and stale-status clearing.
- Environment source landed clean/pushed at `c8fedde5a940c93b6e4d9fa35d5eba43ca3e6e23`, tree `8a0582d8004f33e74980bf3d7342dcf576d96849`. Canonical Alpine config is the exact Derrick-owned `336` bytes / SHA-256 `69a62f73bc1193b0e9414c2698a269e24ed06c309616dc345a3f5cf80966f977`; Alpine manifest is `4,147` bytes / `10be1ad8a8847239df7ad75aa8e4ad010972b5f218b441e028cafc32bfbfe8fc`; ordered eight-entry catalog is `18,656` bytes / `007c4bf63ac9cfccc062986ffa10f28f1bd438f0cabebbf387dee6c77b27cb9d`; aggregate review manifest is `4,767` bytes / `45eb37da7adad86b1ef05a22d0a4fa5a200dfe2dc5a61dd45fe30e17224224d4`. Finalizer and independent validator now enforce per-ID yaw (`180` only for Alpine), deterministic regeneration reproduced the same diff, full Godot GUT passed `34/34` tests / `295` assertions, and all JPEG bytes/order/rights/classifications/review records remained unchanged.
- Gameplay asset successor landed clean/pushed at `32e0fc71c55f999a1fb16abf73dcb768b8294b3a`, tree `c26f21e60b447793a0821c109a219599ac0516e5`; raw `0.0.4` tree `be36bbd03647bfb4654e0be1ed8b3f6446ced4ec`, exact `17` files / `44,741` bytes / aggregate `79a2dcae789a5595352657e68a8d5e2891495bb71bb223efd85339520e1e3136`; inventory `efecf985fd1bc1024c9ffcb64faf92b76f3492df4f8ffa10e53277d5bac18698`, proof `c1916a14d90aef230747185ed823c17bcae0e91229929595599f1bd3aee6e97b`, set `412092d4e9b8ee8069865ec95b9649929027b8e703e8a71a8e8ab5953089a0e3`. The `3,832`-byte arrow GLB is `1a1ffd53d02e07da8ba098e940d3a53d0041d1e865fe9a9682b19c721bccf513`, `136` triangles, unchanged `0.78×0.78×0.18` dimensions/pivot/bounds, closed two-manifold with non-overlapping cap planes at white `±0.09`, charcoal `±0.088`, tint `±0.086`, and styled opaque/depth-writing `+Z/-Z` faces. Six non-arrow GLBs are byte-identical to `0.0.3`; review tree `8342d83194d8375886f371d6d57c6fcdda677a6f` includes inspected bright-background face renders and contrast `18.612077`/`14.082577` over threshold `7`. Strict Blender validation, two-build release reproducibility, subprocess failure contract, and finalization all passed; all raw/review `0.0.1`–`0.0.3` trees remained unchanged.
- Derrick then approved correction of the physically observed wall/grid mismatch. Independent diagnosis on `aerobeat-web-assembly-ro9` proved a cross-repository contract defect: canonical cells are `0.94×0.94` on equal `1.0` pitch, while the unit-X/Y wall is `1.80×1.90`, making it `1.9149×` wider and `2.0213×` taller and causing adjacent instances to overlap by `0.80`/`0.90` world units. Static blocked overlays and all other cell-scoped assets are correctly bounded; wall Z remains intentional interval truth. Durable diagnosis is `.plans/debug/2026-09-04-wall-grid-size-contract-mismatch.md`; raw/review `0.0.4` is immutable and must be superseded by source-first gameplay `0.0.5` with an exact `0.94×0.94×1.00` wall.
- Initial wall coder `a1c0e5b2-23ac-4d86-8c13-f0ba6f36d6aa` failed without a closing report after modifying only `tools/generate.py` and `tools/validate.py`; parent reconciled those partial effects with Python compilation and `git diff --check` PASS, requested interruption, and launched the one permitted idempotent standalone recovery coder `d1dfeb0a-890b-48d5-a569-b20a27f1e99b`. The recovery child also ended with a transport/report failure, but only after landing clean/pushed gameplay `0.0.5` commit `2bd4712f00dd65a758aa064d0e709131f8af8c64`, tree `b2336c6ebe8a90a49ee5beb3024e79bfaa078613`; parent reconciled the completed effects instead of retrying again. Raw `0.0.5` tree is `000653eace4b93f3c5d2eef11bd5c8255008b3de`, exact `17` files / `45,819` bytes / aggregate `24f6bb3b86657716ed03958a32dee5c9db3904aa980cb0a839aacac0590cc860`; inventory/proof/set SHA-256 are `4984cca24b8121bc6657153304726f1f7ef05d878ca5220f3c3e2b6f2457a102` / `4aac2274a9803a05e9ff533c02958cf1c5def66e0af1bf2fae3cc4479319f350` / `b5d50f1dff440b687c017f96b2bc0f06f5afdcd5e2a00522511f3803b4ec7ed2`; review tree is `a1781ce69ba81d660e4ffb24ae8b47d3873c63bb`. Parent independently confirmed wall measured AABB `[-0.47,-0.47,-0.5]` to `[0.47,0.47,0.5]`, exact `0.94×0.94×1.00` dimensions, wall GLB SHA-256 `1227bfbb7d5379b33f1468c1a0d7fffad07c9390654b54033f079ba602a84a37`, all six non-wall GLBs byte-identical to `0.0.4`, and clean `main...origin/main`. Parent managed job `bash-27` independently passed strict validation for all seven assets, release inventory/SHA-256/rights/provenance/tools/dependencies/review hashes, clean Blender imports, and subprocess adversarial contracts (`VALIDATE_OK release=0.0.5 assets=7 release_files=17 smoke=1`; `CONTRACT_TEST_OK adversarial=4 validator_fake_blender=3 reproducibility_fake_blender=3`). Assembly raw `0.0.38` remains forbidden until gameplay `0.0.5` is pinned, QA'd, and audited.
- Downstream renderer coder `9409adae-de21-4cc9-a852-97076009b434` was launched and verified running, then landed clean/pushed renderer commit `4153e963ce840b50b8f95e2be0679ed88ab8dded`, tree `e77db3502dcfc39e7bd146df149dbab090b5178b`. Renderer now contains sole exact gameplay `0.0.5`; structural and Chromium runtime AABBs prove each wall is `0.94×0.94`, equals neutral/blocked visible cells, retains exact canonical centers and `0.06` gaps on `1.0` pitch, scales only Z to interval truth, and preserves authored red obstacle semantics. Full renderer `npm test`, `npm run test:browser`, dry pack (`39` files / `72,928` packed / `243,583` unpacked), and diff hygiene passed. The prior arrow/no-clear evidence remained exact: arrow SHA-256 `1a1ffd53…`, `988/988` grid-off/on target pixels unchanged, physical opaque marker/target depth, all directions/modes/faces, and `0/40/79/80 ms` fade behavior.
- Assembly candidate now pins renderer `4153e963…` / `e77db350…`, packages exact gameplay `0.0.5` plus the exact environment payload, and enforces the wall footprint/pitch/gap/X-Y/Z contract without creating raw `0.0.38`. Focused environment/package validation passed `24` environment + `17` gameplay files and a `107`-file pack. The controls browser matrix initially exposed two validator-only assumptions: it attempted to read intentionally private renderer transform state from `describe()` and expected six-decimal values beyond the native range input's `0.1` step. The corrected oracle observes intercepted renderer calls and the browser-sanitized control value; direct/real-cross-origin iframe portrait/landscape DPR `1/3`, immediate labels, no-wrap unit/accessibility, fresh/reconnect Alpine yaw `180`, ordinary selection retention, privacy, exact `16 KiB` load, atomic hostile rejection, and stale-generation cases all pass. Full `npm test`, final ordinary `npm run build`, release-pack policy, and final dry pack (`107` files / `16,148,904` packed / `17,054,659` unpacked; SHA-1 `c0c3bd43841375f39a399750988bf00f6d3a09b3`) pass. The complete final `npm run test:browser` passes every console, insecure/secure hash, audio, mobile, preview, library, direct/iframe product-shell, camera-compositor, cursor, and square-grid stage; exact wall runtime size is `0.9399999976×0.9399999976` with preserved interval Z `1.2000000477` across portrait/landscape DPR `1/3`. Two earlier full-suite runs diagnosed stale Luminious-default request counters and the legacy `1.8×1.9` wall oracle; both were updated to Alpine and the approved `0.94×0.94` contract. A separate recovery-countdown timeout reproduced only under the full sequential load, passed standalone unchanged, and passed the final suite with a liveness-only `12 s` ceiling that does not weaken any state or visual assertion. Raw `0.0.37` remains exact tree `6d2b8c4e39d3677f28e48ad076bc6259abcd47b9`; raw `0.0.38` is absent.
- Assembly implementation committed as `48bc9504fed114a21ee160db3211653a4c8a832c`, tree `ff5c90c3da97534b7f61a35ec1160799d6fa7eb3`. Comprehensive coder evidence was recorded on `aerobeat-web-assembly-k72.34`; only that assigned coder Bead was closed, with `k72.35`, `k72.36`, `k72.17`, `i4u`, and `ro9` left open for their owners and independent chain. `bd dolt push` completed. Final plan/evidence commit and upstream verification follow this implementation commit.

## Assembly wall-contract recovery after premature coder closure (2026-09-04)

- Direct parent inspection disproved original coder child `b2711487`'s completion report at pushed assembly HEAD `3eb45d1e3e28ff4427a8b379ae40d77d02f69ed4`: this plan and README described the new `0.94 × 0.94` wall, but `docs/handcrafted-3d-visual-spec-v1.md` still retained the superseded `1.80 × 1.90` cross-section and oversized X/Y AABB, while `scripts/validate-square-grid-alignment.js` still lacked an explicit adjacent-wall non-overlap/gap regression. Bead `aerobeat-web-assembly-k72.34` was therefore reopened to `in_progress` and its existing Derrick assignment was confirmed before recovery edits. The current worker exposes DSH session ID `09e71f01-083e-4215-8c09-7052a1b6b94c`; no separate durable child ID is visible inside this delegated context.
- Corrected the specification to the finalized gameplay `0.0.5` source contract: exact `0.94 × 0.94 × 1.00`, source AABB `[-0.47,-0.47,-0.5]` to `[0.47,0.47,0.5]` before interval-authoritative Z scaling, runtime X/Y scale exactly `1`, variable full-interval Z with unchanged endpoints, translucent red rendering, and exact non-overlapping `0.06` X/Y gaps on canonical `1.0` pitch. No cell identity/center, collision, camera, scoring, or timeline semantic changed.
- Strengthened the existing direct/real-cross-origin-iframe portrait/landscape DPR `1/3` square-grid validator without weakening its `0.94 × 0.94`, neutral-cell equality, unit X/Y, or fixture `scale.z = 1.2` assertions. For both X and Y it now constructs the neighboring footprint exactly one canonical pitch away, requires non-overlap, and requires gap `0.06` within the existing runtime-float tolerance. The focused matrix passed all eight rows with measured wall `0.9399999976158142 × 0.9399999976158142 × 1.2000000476837158` and X/Y gaps `0.06000000238418579`.
- Recovery validation passed: `node scripts/validate-square-grid-alignment.js`; full `npm test`; and `git diff --check`. Raw `0.0.35`, `0.0.36`, and `0.0.37` remain unchanged at trees `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, and `6d2b8c4e39d3677f28e48ad076bc6259abcd47b9`; raw `0.0.38` remains absent. This recovery makes no QA, audit, release, serving, Tailscale, or physical-approval claim and does not touch those Beads.

## Raw `0.0.37` physical-review correction independent QA — PASS (2026-09-04)

- Claimed `aerobeat-web-assembly-k72.35` and independently started from four clean, aligned `main == origin/main` checkouts. Git and remote read-back matched gameplay `2bd4712f00dd65a758aa064d0e709131f8af8c64` / tree `b2336c6ebe8a90a49ee5beb3024e79bfaa078613`, environment `c8fedde5a940c93b6e4d9fa35d5eba43ca3e6e23` / tree `8a0582d8004f33e74980bf3d7342dcf576d96849`, renderer `4153e963ce840b50b8f95e2be0679ed88ab8dded` / tree `e77db3502dcfc39e7bd146df149dbab090b5178b`, and assembly baseline `1783e57e2167032bc45b3043cecdbeee60b99a2b`; assembly contains implementation `48bc9504`, recovery `c97f1c79`, and accurate coder closure `1783e57e` in order.
- Gameplay source/release QA passed `python3 tools/validate.py --root . --release 0.0.5`, `python3 tools/reproducibility.py --root . --release 0.0.5`, and `python3 tools/test_subprocess_contract.py --root .`: seven assets and clean Blender imports passed, all `17` release files reproduced byte-identically across two fresh builds, and all `4` adversarial plus `3+3` fake-Blender subprocess cases failed closed. Independent GLB parsing reproduced arrow `3,832` bytes / SHA-256 `1a1ffd53d02e07da8ba098e940d3a53d0041d1e865fe9a9682b19c721bccf513`, three OPAQUE RGBA-1 materials, identity node/no consumer Y flip, and wall `3,692` bytes / SHA-256 `1227bfbb7d5379b33f1468c1a0d7fffad07c9390654b54033f079ba602a84a37`. Strict geometry reported arrow `136` triangles with closed robust `±Z` faces/no coplanar caps and wall AABB `[-0.47,-0.47,-0.5]…[0.47,0.47,0.5]`; `0.0.4` raw/review trees remained unchanged and all six non-wall GLBs are byte-identical from `0.0.4` to `0.0.5`.
- Environment QA passed `python3 .testbed/tools/photosphere/validate_photosphere.py --repo-root .` and the full headless GUT command: exact eight-entry order remained Luminious Ice Cave; Icebergs on Sea Shore; Snow Mountain with Lake; Iceland Waterfall; Igloo Toon; Salt Lake; Salt Lake 2; Alpine River Valley, all continuity metrics passed, and GUT passed `34/34` tests / `295` assertions. Alpine config independently reproduced exact `336` bytes / SHA-256 `69a62f73bc1193b0e9414c2698a269e24ed06c309616dc345a3f5cf80966f977` with yaw `180`; source→assembly config and gameplay source→renderer→assembly arrow bytes compare identical.
- Renderer `npm test`, `npm run test:browser`, `npm pack --dry-run --json`, and `git diff --check` passed. The pack is exactly `39` files / `72,928` packed / `243,583` unpacked bytes, SHA-1 `5d00172952761fa2d81a452d3aac14a90b7a21c4`. Actual Chromium evidence preserved all `988/988` target pixels with grid off/on, no grid/target depth clear, physical marker/target depth, opaque/depth-writing unresolved targets, all directions in Flow/Grid/Lanes plus free-fly, and exact resolved-note samples at `0/40/79/80 ms`. Environment hashing, insecure-context fallback, strict descriptor/transform rejection, one-resident replacement, stale generations, context restore, disposal, and zero browser/WebGL noise passed.
- Assembly passed `npm run verify:environment-assets`, hash provenance/insecure integration, `npm run check`, full `npm test`, v4 integration, live Flow obstacles, environment-controls Chromium, release-pack-policy self-test, ordinary `npm run build`, full `npm run test:browser`, dry pack, and `git diff --check`. Environment-controls passed all eight direct/real-cross-origin iframe portrait/landscape DPR `1/3` rows with immediate pitch/yaw/roll/scale outputs, associated no-wrap degree units, Alpine fresh/reconnect default, valid connection-local selection retention, exact-16-KiB trusted load, atomic hostile rejection, stale generation/lifecycle rejection, and privacy. Current `src`, `docs`, and `dist` contain neither requested removed string. The full browser gate passed insecure/secure hashing, audio, strict BeatSaver/provider/import grammar and atomic persistence, mobile menu accessibility, preview/library, eight-row product shell, camera compositor, cursors, and square-grid stages with no unexpected console/page noise.
- Square-grid actual-pixel QA passed all eight direct/iframe portrait/landscape DPR `1/3` rows. Wall world size was `0.9399999976158142 × 0.9399999976158142 × 1.2000000476837158`, unit X/Y and authoritative interval Z were retained, X/Y neighbor gaps were `0.06000000238418579`, direct/iframe pixel hashes matched, canonical centers/cell IDs and red collision semantics remained exact, and the assembly specification/validator now agree on source `0.94 × 0.94 × 1.00` and AABB `±0.47 X/Y, ±0.5 Z`.
- Ordinary assembly build and dry pack passed at exactly `107` files / `16,149,200` packed / `17,055,724` unpacked bytes, SHA-1 `42f1efeaaa2299b9a12709447e23750f75824217`. Raw `0.0.35`, `0.0.36`, and `0.0.37` remain byte-tree unchanged from pre-correction `efedd97` at `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, and `6d2b8c4e39d3677f28e48ad076bc6259abcd47b9`; raw `0.0.38` is absent. QA changed only this plan and Bead metadata, did not build a release, alter serving/Tailscale, publish/tag, mutate immutable predecessors, or claim Derrick physical approval. Disposition: PASS and close only `k72.35`; independent audit `k72.36` and physical gate `k72.17` remain for their owners.

## Physical-correction independent source audit — PASS (2026-09-04)

- Audit Bead `k72.36` was claimed before audit work. The reported QA child launch was `4e2773ff-37c5-4057-8e7a-e0dd760a218e`; the independently visible audit child/session identity is `856668d8-a1ba-492d-95e5-502578f3a272`. The complete coder → recovery → recovery closure → QA evidence ancestry is `48bc9504fed114a21ee160db3211653a4c8a832c` → `c97f1c79c25121677a03d587c2ca3a9627a93829` → `1783e57e2167032bc45b3043cecdbeee60b99a2b` → `3f9fef4815b836481caea7cb2c2bb6bedb89b586`; full textual/binary diffs, commit metadata, plan evidence, and Bead comments/dependencies/closures were inspected rather than accepted from the QA report.
- Fresh SSH fetch/read-back proved clean aligned `main == origin/main`: gameplay `2bd4712f00dd65a758aa064d0e709131f8af8c64` / tree `b2336c6ebe8a90a49ee5beb3024e79bfaa078613`; environment `c8fedde5a940c93b6e4d9fa35d5eba43ca3e6e23` / tree `8a0582d8004f33e74980bf3d7342dcf576d96849`; renderer `4153e963ce840b50b8f95e2be0679ed88ab8dded` / tree `e77db3502dcfc39e7bd146df149dbab090b5178b`; assembly QA `3f9fef4815b836481caea7cb2c2bb6bedb89b586` / tree `aaddfe8a726678b6a2315152c8fb3f3a4973fa1e`. All remotes are SSH. Local tags are empty, remote tag refs are empty, all four GitHub Release lists are empty, and npm returned expected `E404` for `@aerobeat/web-assembly@0.0.38`.
- Independent gameplay reruns passed `python3 tools/validate.py --root . --release 0.0.5`, `python3 tools/test_subprocess_contract.py --root .`, and `git diff --check`: seven assets, exact 17-file/45,819-byte raw release, clean Blender imports, adversarial subprocess contracts, opaque RGBA-1 arrow with closed robust `±Z` and no coplanar caps, and wall AABB `±0.47` X/Y / `±0.5` Z. Independent GLB/hash/inventory comparison proved arrow SHA-256 `1a1ffd53d02e07da8ba098e940d3a53d0041d1e865fe9a9682b19c721bccf513`, wall SHA-256 `1227bfbb7d5379b33f1468c1a0d7fffad07c9390654b54033f079ba602a84a37`, exact source→renderer→assembly bytes, exact inventory/proof hashes, unchanged raw/review `0.0.4`, and byte-identical six non-wall GLBs into `0.0.5`.
- Independent environment validation passed the strict eight-entry catalog/rights/hash/continuity validator. The exact source and assembly Alpine config bytes are `336` / SHA-256 `69a62f73bc1193b0e9414c2698a269e24ed06c309616dc345a3f5cf80966f977`, yaw `180`, byte-identical; the source and assembly keep the required eight-item order and fresh/reconnect Alpine default while ordinary live per-instance selections/configs retain during a connection.
- Independent renderer `npm test`, full `npm run test:browser`, dry pack, and diff hygiene passed. This reproduced exact 17-file gameplay `0.0.5` provenance, no Y flip, opaque/depth-writing targets, `World opaque → Grid → Targets → World transparent` composition, physical target/cursor depth, no depth clear, and exact bounded fade samples at `0/40/79/80 ms`; environment strict descriptor/transform, one-resident texture, hash fallback/tamper, lifecycle/context/disposal, and direct/iframe secure/insecure pixel contracts passed. Renderer pack remained `39` files / `72,928` packed / `243,583` unpacked bytes / SHA-1 `5d00172952761fa2d81a452d3aac14a90b7a21c4`.
- Independent assembly passed environment sync verification, `npm test`, environment-controls browser, camera compositor pixels, square-grid pixels, release-pack policy, ordinary build, dry pack, mobile shell, full product-shell matrix, insecure/secure production hash/import integration, and diff hygiene. It reproduced immediate pitch/yaw/roll/scale outputs, associated nowrap degree units, exact 16 KiB fatal-UTF-8 strict-JSON atomic rejection/stale lifecycle/privacy, exact canonical cells/red collision semantics, unit wall X/Y with `0.06000000238418579` adjacent gaps on 1.0 pitch, and interval-authoritative Z/endpoints. Both requested removed strings are absent from current `src`, `docs`, and rebuilt `dist`. One first standalone mobile run timed out at the known recovery-countdown liveness wait; an instrumented no-source-change reproduction immediately passed the complete script, while the independent product-shell matrix passed all direct/iframe portrait/landscape DPR 1/3 rows and the earlier QA full browser run also passed. No product finding was produced by this transient host/test timing event.
- Assembly dry pack reproduced exactly `107` files / `16,149,200` packed / `17,055,724` unpacked bytes / SHA-1 `42f1efeaaa2299b9a12709447e23750f75824217`, including exactly 17 gameplay and 24 environment files and no raw-release storage. `npm ls --all` showed only declared linked runtime packages and platform optional misses; no dependency manifests changed in assembly and renderer added only its validator script entry, with no new dependency or network/runtime path.
- Raw `0.0.35`, `0.0.36`, and `0.0.37` remain unchanged from `efedd97` at trees `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, and `6d2b8c4e39d3677f28e48ad076bc6259abcd47b9`; raw `0.0.38` remains absent. This audit did not build a release, change servers/Tailscale, publish, tag, mutate an immutable predecessor, or claim physical approval.
- **Disposition:** PASS. The audited source tree is authorized **only** for the next append-only immutable raw `0.0.38` construction lane. This is not release QA, serving approval, publication approval, or Derrick physical approval; `k72.17` remains in progress for the later physical review after independently audited raw construction.

## Raw `0.0.38` release-lane decomposition (2026-09-04)

- Created build Bead `k72.37` for one append-only immutable raw `0.0.38` construction from the audited source tree. It depends on closed source audit `k72.36` and is the only ready release-lane Bead.
- Created independent release QA/audit Bead `k72.38`, depending on `k72.37`. Physical review Bead `k72.17` now additionally depends on `k72.38`, so secure serving and Derrick review cannot bypass the immutable-release audit.
- An initial scripted dependency declaration reversed the two new blocking edges. The orchestration parent immediately removed both wrong edges, added the intended `k72.36 → k72.37 → k72.38 → k72.17` chain, verified `bd dep cycles --json` returned `[]`, verified only `k72.37` is ready, and pushed the corrected Dolt state before delegating construction. No release bytes were created during decomposition.

## Immutable raw `0.0.38` builder result — PASS (2026-09-04)

- Builder Bead `k72.37` was claimed before repository mutation. The delegated builder identity visible in this lane is DSH session `9c46a769-025c-464c-a0bf-465e4b3592bb`; no separate child ID was exposed. Fresh `git fetch --prune origin` and read-back proved all four checkouts clean and aligned on `main == origin/main`: gameplay assets `2bd4712f00dd65a758aa064d0e709131f8af8c64` / tree `b2336c6ebe8a90a49ee5beb3024e79bfaa078613`, environment `c8fedde5a940c93b6e4d9fa35d5eba43ca3e6e23` / tree `8a0582d8004f33e74980bf3d7342dcf576d96849`, renderer `4153e963ce840b50b8f95e2be0679ed88ab8dded` / tree `e77db3502dcfc39e7bd146df149dbab090b5178b`, and assembly `bb2ea7d1c4e7cbfea737f69f3ac8251d5061ed1a` / tree `8fc3779f5e30df4376cd3a2434c11f12c77657ca`. Raw `0.0.38` was absent from both the filesystem and Git.
- Before construction, a complete working-byte-versus-Git snapshot reproduced predecessor identities: `0.0.35` tree `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, `20` files / `13,878,153` bytes, proof SHA-256 `22c41e8bf0630bb6b50523a96ab0e886b399a93c74a7d1e97bb2afe47a43c4ea`, complete-manifest aggregate `a111efba87a4b46502b04a34c3ff0ca102815f090a20fba7cf4ccf29d2d21951`; `0.0.36` tree `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, `41` files / `27,760,611` bytes, proof `3d18dd99afe99fcac389bccee760073baf83cc44c66cdec6d0ac5d933d142daf`, aggregate `575fb3515aeca10f5a88ea06db4b2c2003b4506d2c70e520e6f8533a9ea97595`; `0.0.37` tree `6d2b8c4e39d3677f28e48ad076bc6259abcd47b9`, `41` files / `27,820,403` bytes, proof `9415f1ee7f9ddc687b4756be84e5a2bec9dfa9521eb5ec8f6c6ddc5b9ee286f9`, aggregate `5994c06810b832fb8987e66269cb199a434a8bd5a4f575212ee6d33d4ff7bd50`. `npm run test:release-target-policy`, `npm run verify:environment-assets`, and gameplay `python3 tools/validate.py --root . --release 0.0.5` passed before construction.
- Canonical `npm run version:patch` ran exactly once and moved `package.json`, package-lock top-level/root, and `index.html` proof metadata exactly `0.0.37 → 0.0.38`. The fail-closed exact-version assertion in `scripts/validate-shared-hash-provenance.js` was advanced to the authorized `k72.37` successor. Immediately before build, package/index/lock coherence, clean sibling refs, target absence, dependency provenance, and fingerprint were rechecked. The authoritative source fingerprint was `03288323640ec1c52105dd26f9c20dd51c723c454a9ef5481603dc93d53893c3` over `199` inputs.
- Canonical `AEROBEAT_BASE_PATH=/ npm run build-release` ran exactly once against the permanent absent target and created raw `0.0.38`; it was never rerun against that target. The candidate contains exactly `41` regular files / `27,834,715` bytes (`27,831,916` before the `2,799`-byte proof), all staged mode `100644`, raw tree `9c4225c83b8697a6404190bddcbfcbee0a5d60f3`, proof SHA-256 `86f08597e4c17d0191d1ba7fb70225c8188c21c0cbc7ffa21b32cb2cef2b6041`, and globally line-sorted complete-manifest aggregate `87698cb3fee11308b45904fde4726c04ebed06863296c64151d03d89fc7e75aa`. Category totals are proof `1 / 2,799 B`; HTML `1 / 527 B`; CSS `1 / 1,303 B`; JavaScript `4 / 4,388,261 B`; source maps `3 / 7,398,064 B`; environments `24 / 16,013,893 B`; gameplay GLBs `7 / 29,868 B`.
- Proof read-back binds exact base `/`, MediaPipe-only runtime configuration, zero WASM, four scripts / three source maps, two shared-source attributions, and all seven exact dependency commit/tree provenance records. Forbidden asset patterns and runtime markers for TensorFlow, ONNX, MoveNet, ORT-WASM, predictive/predicted pose, responsive A/B, direct-256, and experimental Worker VideoFrame are absent. Candidate environment bytes match all `24` assembly-source files exactly; its seven runtime gameplay GLBs match their approved assembly payload bytes, while package policy separately confirms the complete exact `17`-file gameplay `0.0.5` source payload.
- One isolated depth-matched temporary reproduction under `.tmp/k72.37-repro/0.0.38` invoked Vite with the canonical release build options and independently synthesized the proof with the checked-in proof/fingerprint/bundle policies. All `41` paths, all `27,834,715` bytes, and proof SHA-256 matched the permanent candidate byte-for-byte. Only this owned temporary output was removed afterward; the permanent target was not touched.
- Post-build validation passed `npm test`, `npm run build`, `npm run test:release-pack-policy`, candidate proof/inventory/category/forbidden-marker checks, environment/gameplay byte comparison, `npm ls --all`, and post-build predecessor byte/tree snapshots. Package dry-run identity is exactly `@aerobeat/web-assembly@0.0.38`, `107` files, `16,149,201` packed / `17,055,725` unpacked bytes, SHA-1 `72489a8cb54a9f1a0628999a7f9ad0d2a8b8fdbf`, integrity `sha512-RSzngZchJap29BAXdhjl3vMvHzJmWbzh9smQm+oHxL/ilO4gHdELAX4AYb5NU4zihWzuWmp8SA3XXayP+b7zhQ==`; it includes exactly `17` gameplay and `24` environment files and zero `release/` members. Source/metadata diff hygiene passes; generated immutable `assets/index.js` retains the deterministic PlayCanvas shader trailing whitespace already documented for predecessors and was not mutated after construction.
- This builder did not serve or inspect the review URL, change a listener or Tailscale, publish npm, create a Git tag or GitHub Release, change external services, rebuild/delete/overwrite any predecessor, acquire assets, or claim independent release QA/audit or Derrick physical approval. Only `k72.37` closed on this builder PASS. Independent `k72.38` remains open for release QA/audit, and physical review `k72.17` remains in progress.
- The immutable version files, raw candidate, README, and plan evidence landed in release commit `f54d5ff94e831a6ac23fc05b489e1137aadffcdd` / commit tree `3f5f67090179787e3bbde3c497d08ec3695da741`; raw subtree remains `9c4225c83b8697a6404190bddcbfcbee0a5d60f3`. The generated Bead closure interaction then landed in `d7b5eaa8d2d3987b72f6277812d24c3727bd5ca6` / tree `8d2622de0c66fd86aafa01bbb2d554475c9726c3`. Both commits were pushed to assembly `origin/main`, and `bd dolt push` completed; the final plan-only evidence commit follows this recorded chain.

## Immutable raw `0.0.38` independent release QA/audit — PASS (2026-09-04)

- Audit Bead `k72.38` was claimed before audit work. The independent auditor/session identity is `6ce018a8-3d89-4a6d-a6fb-05b94a9ada0a`; read-only cross-check children `da7e025d-06b5-4ded-b77a-71f329499331` and `1c17d72b-0966-4db1-af70-951a532f3c39` independently reported no discrepancies. The pushed chain was inspected rather than trusting builder narration: raw payload `f54d5ff94e831a6ac23fc05b489e1137aadffcdd` → Bead closure evidence `d7b5eaa8d2d3987b72f6277812d24c3727bd5ca6` → final assembly `54e8c581af322a227b809492fe163953f9e8b403`, tree `824a91d83b37e35ed59f58aef10ebc3618c0daca`; `main == origin/main` after fresh fetch.
- Git/tree/byte audit reproduced append-only construction. Raw `0.0.38` was absent at `f54d5ff^`; predecessor trees remain exact: `0.0.35` `bd69d3bd309660125d1a5ac3da6d07896c49bb96`, `0.0.36` `ce125ba4a596f7d6cad84c9e3bf983c5ccf0ed77`, `0.0.37` `6d2b8c4e39d3677f28e48ad076bc6259abcd47b9`. Their counts/bytes/proof/complete-manifest identities reproduced. Raw `0.0.38` is subtree `9c4225c83b8697a6404190bddcbfcbee0a5d60f3`, all `100644`, exactly `41` files / `27,834,715` bytes, proof `2,799` bytes / SHA-256 `86f08597e4c17d0191d1ba7fb70225c8188c21c0cbc7ffa21b32cb2cef2b6041`, before-proof bytes `27,831,916`, complete-manifest aggregate `87698cb3fee11308b45904fde4726c04ebed06863296c64151d03d89fc7e75aa`, and source fingerprint `03288323640ec1c52105dd26f9c20dd51c723c454a9ef5481603dc93d53893c3` over `199` inputs.
- A fresh depth-matched isolated reproduction under an auditor-owned `.tmp/` directory invoked Vite directly with the canonical release options, synthesized proof from `release-fingerprint.js` and `production-hash-bundle-policy.js`, and never imported or invoked `claimAppendOnlyReleaseTarget`. All `41` relative paths and all `27,834,715` bytes compared byte-for-byte with permanent raw `0.0.38`; reproduced proof SHA-256 was exact. The temporary output was removed. An initial `/tmp` reproduction correctly exposed source-map path-depth sensitivity and therefore differed only in generated maps/proof byte totals; the corrected depth-matched procedure proves the required exact deterministic identity without touching any raw target.
- Exact assets/provenance passed. Gameplay source, renderer, and assembly contain the same complete `0.0.5` `17`-file / `45,819`-byte release; raw contains its seven exact GLBs. Independent validation confirmed seven Blender assets, clean imports, rights/provenance/tool hashes, opaque two-face arrow, and wall AABB `±0.47` X/Y and `±0.5` Z. Environment source and assembly validation confirmed eight owned entries in unchanged order and exactly `24` byte-identical raw files; Alpine River Valley is fresh/reconnect default with exact yaw `180`. All seven proof dependency commit/tree records, package/index/lock/proof version `0.0.38`, required MediaPipe markers, four JS/three maps/two shared-hash attributions, zero WASM, and all forbidden runtime asset/marker exclusions reproduced.
- High-fidelity assembly validation passed unchanged: `npm test`; `npm run verify:environment-assets`; hash provenance; secure/insecure production hash/import integration; v4 golden/import; live Flow obstacle integration; release target/pack policy; ordinary `npm run build`; focused environment-controls browser; camera-compositor pixels; square-grid pixels; and full managed-background `npm run test:browser` with exact exit `0`. The full browser log was `62,273` bytes / `46` lines / SHA-256 `002fc5ac3b034f54dc29790d3b2addf7d5fc73e36ab459c1ceca3ebe8a65802c` and was removed after collection. It passed every direct/real-iframe portrait/landscape DPR row, product shell, camera, cursor, grid, audio, library, mobile, secure/insecure hash and import stage with no product console/page noise.
- Focused cross-repo regressions passed: renderer `npm test` plus full `npm run test:browser` reproduced opaque/depth-writing arrows in all directions/modes, `World opaque → Grid → Targets → World transparent` with no depth clear, exact removal samples `0/40/79/80 ms`, secure/insecure asset hashes, photosphere lifecycle, and exact wall `0.94 × 0.94`, unit X/Y, `0.06` gaps at pitch `1.0`, interval-derived Z. BeatSaver `npm test` and browser passed strict explicit v2/v3/v4 declarations, `16` accepted / `51` rejected version rows, versionless fallback only when declaration keys are absent, ordered provider digest integrity, and native/fallback hashing. Gameplay and environment source validators passed. Exact raw-candidate HTTPS route-interception smoke used no server/listener and reached connected renderer state with seven gameplay assets, Alpine environment/yaw `180`, bounded privacy, and no product noise; one exact known Chromium `GPU stall due to ReadPixels` driver warning was classified under the repository's established host-noise filter, not suppressed by a test edit.
- Final detached-package audit used a clean detached `54e8c581` worktree outside protected checkouts, normalized all `494` tracked modes from Git, created dry and real packs with lifecycle scripts disabled, and passed the pinned Node `22.22.3` / npm `10.9.8` canonical gzip/USTAR verifier: `107` members, all `0644`, zero PAX, exactly `17` gameplay + `24` environment + zero `release/` members. Final pushed-Git package truth is `16,149,148` packed / `17,055,776` unpacked bytes, SHA-1 `a4d38f727b092b77dd1751c4401ce7d6e68646ee`, integrity `sha512-oYzL9nGXo9B5Ml9pMK4XF5QWnwVXUyHZDl5oMV5Q1fGX5wJIBxtV4fu0G/BXdKi6TgjdvvpmoNdsilUgAoi1Uw==`; verifier archive SHA-256 `24a5c62774d14259d7a4451f89939af8bb1710dd3813ab239eb58ff15a83db36`, manifest SHA-256 `8ad758367152fb7440c39a2766ff8d6710c15fa06274e150361634b436d64a98`. This truthfully supersedes the builder's pre-evidence dry-pack numbers: the committed README is exactly `51` bytes larger than its pre-payload parent, explaining the exact `+51` final unpacked-byte delta. All detached pack artifacts/worktree were removed.
- **Disposition:** PASS. Exact immutable raw `0.0.38` bytes identified above are authorized only for secure loopback-backed Tailscale physical-review serving. This does not authorize npm/publication, Git tags, GitHub Releases, other bytes, or Derrick physical approval; physical gate `k72.17` remains for Derrick.

## Raw `0.0.38` secure physical-review serving — ACTIVE (2026-09-04)

- Collected and stopped the prior managed loopback server job `bash-23`, which served immutable raw `0.0.37`, and started retained managed job `bash-49` with exact argv `python3 -m http.server 5173 --bind 127.0.0.1 --directory /home/derrick/.dsh/projects/aerobeat/aerobeat-web-assembly/release/raw/0.0.38`. The only backend listener is `127.0.0.1:5173`; no broad bind was introduced.
- Existing Tailscale Serve state was preserved: tailnet-only HTTPS `:8443` proxies `/` to loopback `127.0.0.1:5173`; unrelated tailnet-only DSH `:8444` still proxies to `127.0.0.1:3082`; no Funnel or public route was enabled.
- Exact-byte parity passed for all `41` files and all `27,834,715` bytes through both loopback HTTP and tailnet HTTPS against immutable raw `0.0.38`. The served proof reports `proofVersion: 0.0.38` and exact source fingerprint `03288323640ec1c52105dd26f9c20dd51c723c454a9ef5481603dc93d53893c3`.
- Headless Chromium loaded the actual HTTPS route with status `200`, title `AeroBeat Web Assembly`, `isSecureContext=true`, no console/page errors, a reachable renderer canvas through the composed tree, exact release proof fetch, and `Alpine River Valley` present in the composed UI. The first smoke probe's document-only text assertion could not see shadow-root content; a recursive composed-tree read confirmed Alpine without any source or server change.
- Physical gate `k72.17` remains in progress pending Derrick's explicit PASS or exact revision requests. Serving authorization remains review-only; npm publication, tags, GitHub Releases, alternate bytes, and any physical-approval claim remain forbidden.

## Raw `0.0.38` Derrick physical review — REJECTED (2026-09-04)

- Derrick tested live-camera gameplay over the secure review route and rejected raw `0.0.38`. Nose and wrist landmark spheres exhibit the same clear/low-visibility rendering symptom previously corrected for directional notes; this must be diagnosed from actual marker materials, depth, layering, environment contrast, and camera-facing geometry rather than described as alpha transparency without evidence.
- In BeatSaver map `3c9d` (`Dance Dance Revolution [DDRMIX]`), obstacles around `37 s` occupy a full column in BeatSaver ArcViewer but appear as only one cell in AeroBeat. Diagnosis must compare the downloaded source obstacle records, BeatSaver/Beat Saber width semantics, conversion output, cell identity mapping, and renderer geometry before changing collision or scoring behavior.
- Derrick requested that dynamic green/yellow/red success bands become tinted tiles applied directly over the lane surface, preserving data-driven counts while visually aligning with the lane grid rather than floating as a separate overlay.
- Directional-note white outlines and the white right-timing flash were physically confirmed good. Additional timing cues are a future requirement but are intentionally unspecified in this rejection and must not be invented during the bounded correction.
- Miss behavior must retain the passed note temporarily, gray its role color, and let it continue past the athlete instead of hiding immediately. `Miss` text should be larger and perform a short left-right-back-and-forth head-shake animation before disappearing. Hit behavior should use larger `Great` text with a brief bouncy entrance/exit motion.
- Beats and obstacles need a directly-below projected shadow/depth cue to improve non-VR depth perception. The correction must define placement, recipient types, transparency/depth ordering, lifecycle, and collision-free/non-scoring behavior before implementation.
- Diagnosis Bead `aerobeat-web-assembly-0wn` is claimed/in progress. It must produce a durable debug report before source edits and then decompose a mandatory coder → independent QA → independent audit → immutable successor build/audit → Derrick physical-review chain. Raw `0.0.38` remains immutable and may continue only as rejected physical evidence; it must never be rebuilt or modified.

## Raw `0.0.38` defect diagnosis — COMPLETE; obstacle semantic decision pending (2026-09-04)

- Read-only obstacle research child `bf4fab12-c249-4171-97d7-219a8eea1904` and visual-cue diagnosis child `e143527a-4a4b-479e-90ca-8f5976a661ba` were verified running after launch and completed without modifying repos, Beads, serving, or immutable releases. Parent reproduced the real map conversion independently and inspected ArcViewer commit `a7b2d984f91346ade9f25e523afb5ed67cb2cb84` before removing its temporary clone.
- Durable diagnosis: `.plans/debug/2026-09-04-raw-0.0.38-physical-review-defects.md`. Marker alpha is `1`/opaque with depth test/write; the physical failure is led by a small single-material sphere whose only material is force-tinted, lacks stable structural contrast, and ignores assembly's `sizeCssPx:18` request. Existing automation tested presence/basic depth rather than environment/live-camera area and edge contrast.
- Exact BeatSaver `3c9d` source at approximately `37.04 s` is a v2 `_type:1`, `_width:1` crouch/ceiling wall. ArcViewer preserves one horizontal lane with continuous `y=2,height=3`; AeroBeat normalizes `height=1`, clips to canonical mask `[1]`, discards continuous geometry, and renders one `0.94×0.94` square. Current Flow obstacles are validated but resolved as `ignored`, so there is no implemented obstacle collision to compare. Expanding collision to `[1,5,9]` would encode a full-height v2 type-0 wall, not source truth.
- Recommended obstacle architecture is separate source-faithful visual geometry from the bounded canonical intersection mask: render `x=1,y=2,width=1,height=3` while retaining mask `[1]` and current ignored/non-scoring behavior. Any decision to make the full canonical column collision-blocked is a separate gameplay-semantic change requiring Derrick approval.
- Remaining corrections are now specified: append-only marker asset successor with preserved white/dark structure and tintable core; tested apparent-size policy; variable-count lane-surface timing tiles with exact timing bounds; hit-path regression preservation; gray miss continuation through the existing bounded projection window; larger deterministic Miss shake and Great bounce; renderer-only directly-below shadows with no gameplay identity. Additional unspecified timing cues remain deferred.

## Derrick obstacle direction and successor decomposition — APPROVED (2026-09-04)

- Derrick selected source-faithful behavior both visually and for Flow gameplay, explicitly accepting that this requires code/contract changes. The implementation must preserve the source distinction between one-lane crouch/ceiling walls and full-height walls; it must not fake source fidelity by blindly converting type 1 to a full canonical column. A dedicated design lane must define continuous visual geometry, available-pose head collision/avoidance, temporal evaluation, and consequences before coding.
- Diagnosis `aerobeat-web-assembly-0wn` is complete. Execution chain:
  - Flow obstacle gameplay design `aerobeat-web-assembly-h51` → implementation `aerobeat-web-assembly-v5q` → independent QA `aerobeat-web-assembly-qfx` → audit `aerobeat-web-assembly-0hu`.
  - Marker asset coder `aerobeat-web-assembly-r7p` → independent QA `aerobeat-web-assembly-n6k` → audit `aerobeat-web-assembly-4qk`.
  - Renderer/assembly integration `aerobeat-web-assembly-gy3` depends on both audits, then independent QA `aerobeat-web-assembly-zzu` → final source audit `aerobeat-web-assembly-e64`.
  - Immutable assembly successor build `aerobeat-web-assembly-cgg` → independent release QA/audit `aerobeat-web-assembly-esu` → existing Derrick physical gate `aerobeat-web-assembly-k72.17`.
- Raw `0.0.38`, gameplay raw/review `0.0.1–0.0.5`, and all assembly predecessors remain immutable. No successor version directory may be created until the corresponding build lane is authorized by final source audit. No npm publication, tag, GitHub Release, or physical PASS is implied.
- Active execution was delegated and launch-verified: source-faithful Flow obstacle designer `133b12b5-3547-4bbb-9bd5-59d1c3d05c97` claimed `h51`; primary marker asset coder `74283114-0292-4c26-86ed-8df9e57507f2` claimed `r7p`. The design lane is read-only except Bead evidence; the asset coder owns only the gameplay asset repo and must not edit renderer/assembly or the plan.
- Initial `bd create --deps blocks:<id>` use established the chain edges in the opposite direction from intended. Parent detected this by inspecting each Bead's actual `dependencies`, removed all eleven reversed edges, added the intended prerequisite direction explicitly with `bd dep add <dependent> <prerequisite>`, verified `bd dep cycles --json` is `[]`, reread every direct blocker, and pushed corrected Dolt state. The durable order now exactly matches the chain listed above; neither active lane depends on its downstream QA.

## Source-faithful Flow obstacle gameplay design — PASS / IMPLEMENTATION AUTHORIZED (2026-09-04)

- Research/design child `133b12b5-3547-4bbb-9bd5-59d1c3d05c97` completed `h51` read-only, recorded the complete actionable design as Bead comment `01a06e16-4870-7677-87f0-67dce407f8b7`, closed only `h51` PASS, and pushed Dolt. Parent verified the closure, comment, cycle-free graph, and that `v5q` is now blocked only by the closed design prerequisite. The child edited no source, plan, server, or immutable release; its expected tracked closure event is included with this plan update.
- New source authority is exact `aerobeat/flow_obstacle_geometry` v1 `{schema,version,coordinateSpace:"beatsaber_lane_layer",x,y,width,height}`. New Flow/package/resolved-event v2 contracts replace ambiguous obstacle `cells` with continuous `geometry` plus a unique sorted derived `gridMask`; the mask is bounded UI/indexing/Boxing feasibility truth only and may never drive Flow rendering or collision. Legacy v1 remains list/export/delete compatible but is not silently reinterpreted: Play/Test requires atomic reimport and reports `flow_obstacle_reimport_required`; persistence migrates DB `4→5` by labeling legacy rows without rewriting stored package bytes or hashes.
- Normalization is fail-closed and source-specific: v2 type `0→y=0,height=5`, type `1→y=2,height=3`; v3 uses inline `b/d/x/y/w/h`; v4 resolves indexed `obstaclesData`; malformed indexes, ranges, duration, conflicting geometry, unsupported type, and nonzero/unsupported rotation reject atomically rather than clamp/default. The exact `3c9d` Hard golden is provider hash `5662f64a12c76a3dd11a5f6ee22611608cd06760`, BPM `150`, beat `92.5999984741211→92.6624984741211`, `37039.99938964844→37064.99938964844 ms`, geometry `{x:1,y:2,width:1,height:3}`, derived mask `[1]`. It renders one continuous wall centered `(-0.5,3)`, visual face `0.94×2.94`, interval Z `0.15`; it must never become one square or fake cells `[1,5,9]`.
- Collision uses only measured calibrated nose as the available head proxy; wrists, predicted pose, retained render geometry, and video pixels never collide. Nose maps to logical source space as `sx=4*nx-0.5`, `sy=2.5-3*ny`. Consecutive valid same-calibration measurements are analytically clipped through obstacle time and logical rectangle so the real `25 ms` wall remains detectable at `15 fps`; no interpolation crosses stale/lost/invalid/rollback/duplicate/calibration-change evidence or gaps above `150 ms`. Tracking uncertainty produces non-penalizing `unevaluated_tracking`, not a guessed collision or avoidance.
- Overlapping contacts aggregate into deterministic entry episodes. Aggregate clear→occupied resets combo exactly once, applies zero direct points, leaves note hit/miss/ignored arithmetic and max-combo history unchanged, and records bounded source obstacle outcomes (`contact`, `avoided`, `unevaluated_tracking`) without exposing pose coordinates/frame/calibration IDs publicly. Continuous overlap does not repeat consequence; leaving and re-entering creates another deterministic episode. Contact is not a note miss and emits no `Great`/`Miss`; a deterministic wall pulse and accessible `Obstacle hit` status are allowed. `visual_test` is visual-only. Energy/fail/multiplier-tier/audio-muffling constants are explicitly excluded rather than guessed.
- Default mode is visual plus head collision. Between-run identity-bound unranked/local-only accessibility may support `no_obstacles` and optional `obstacle_visual_only`; modifiers never mutate authored package bytes. Renderer transform is one continuous instance: `centerX=x+(width-1)/2-1.5`, `centerY=y+(height-1)/2`, visual width/height `width-0.06` / `height-0.06`, X/Y scale divided by source `0.94`, exact interval-derived Z with no minimum-depth distortion. Collision uses exact logical bounds while visuals retain the `0.03` outer inset. Shadows use the continuous footprint but remain renderer-only and non-colliding.
- The detailed Bead comment names exact implementation surfaces across contracts, authoring, content runtime, gameplay, assembly, renderer, and input proof, plus ten mandatory acceptance groups: source schemas; exact offline `3c9d`; geometry/mask separation; continuous temporal vectors; aggregate consequence; tracking/accessibility; direct/iframe responsive rendering and lifecycle; DB migration/reimport; privacy/bounds; and full repo browser/build gates. `v5q` is authorized to implement this design and commit/push source repos, but may not touch gameplay asset `0.0.6`, any immutable raw/review release, serving/Tailscale, publication, tags, GitHub Releases, or physical approval.

## Gameplay marker `0.0.6` — REJECTED; append-only `0.0.7` — CODER / QA / AUDIT PASS (2026-09-04)

- Marker coder `74283114-0292-4c26-86ed-8df9e57507f2` landed append-only gameplay asset `0.0.6` at pushed commit `425de25adeb7b7291d419d365f2ff7b824a9fd48`, tree `ab7b36d2f5da41a1d1a52bcd4ab0e9f8fc3687d8`. Raw subtree `53181edfdb560de2aeae01e9a05c212a9b93e438` is `17` files / `49,337` bytes; review subtree `f5652c80852153e746774579e4c1fb495ea360f3` is `28` files / `26,272,946` bytes. Marker GLB is `5,496` bytes / SHA-256 `bd7a1523eb62c6ba0cb0f1f19c69b8b2cecc7edc31d05b1cad55138ab86da145`; all six non-marker source/manifest/GLB identities match `0.0.5`, and all earlier raw/review trees remained unchanged. Coder strict/final validation, two-build reproducibility, subprocess tests, mutation guards, and parent strict Blender smoke validation passed. `r7p` was closed only to authorize independent QA; this was not an asset audit or physical PASS.
- Independent QA child `b45b16ac-75be-4faf-88dc-c139a7721610` completed `n6k` with FAIL and durable comment `01a06e27-6938-71d4-95de-f532e252b472`. Direct GLB mathematics proved all `168/168` marker triangles inward-wound: every `cross(b-a,c-a)·centroid` is negative (`-0.0001394881…` to `-0.0000533798…`), and every normalized geometric face normal disagrees with the explicit outward radial vertex normals (`-0.998864…` to `-0.991712…`). The editable `.blend` normals are also inward. This contradicts `doubleSided:false` plus `cull:back`: the exterior is back-facing under standard glTF CCW semantics.
- Existing attractive bright/dark evidence is not runtime-truthful because review/smoke reconstruction ignored embedded NORMAL orientation and did not enable backface culling. QA nevertheless inspected all twelve cardinal bright/dark renders and context views: their no-cull visual treatment was materially improved, with limited but accepted `+Z` bright charcoal coverage (`771` pixels / `2.06%` segmented area). That visual observation cannot override the culling failure. All official coder gates passing therefore exposed a missing validator/smoke contract, not a QA false positive.
- Raw/review `0.0.6` is immutable rejected evidence and must never be repaired in place. Parent closed `n6k` as completed FAIL, created repair coder `k72.39` and independent repair QA `k72.40`, added `k72.40 → k72.39` and final marker audit `4qk → k72.40` blocker edges, verified no dependency cycles, and pushed Dolt.
- Repair child `49ae28fc-0f1e-4775-ac2c-5b905464e5e4` completed append-only marker-only `0.0.7` at pushed asset commit `7dec076e243571144b7ead638d3e3f4780bcb9f4`, tree `62863270ed4455eee7132d9bb374522a46f72e30`. Raw subtree `846c41297230b5077ab1119880b729cc120e1098` is `17` files / `49,515` bytes / content digest `d7ed901aaff35295d25a1d79ca5caa243c3ade848b1a2dc22d664f4d1f3b8f28`; review subtree `8ca78c143d78743ff1dfce1b9fcadc5755a02530` is `28` files / `26,231,461` bytes / digest `91135c131745d17ca03a0a9e257c6adc747d65b96c9b9a7e3d5a4d1feacbc5dd`. Marker GLB SHA-256 is `b2316b8ec013e9d9087a0bd6d9e5dcef643a34132f9c51fc2526c68d317f7530`; source SHA-256 is `7120714531b85b50bf7213294aeb0f1ab32e083fc1190a5258be6d578dbdcd64`. Direct coder math found `168/168` positive winding values (`0.0000533798…` to `0.0001394881…`) and `504/504` positive face/NORMAL comparisons (`0.948575…` to `0.979447…`); exact bounds/topology/material partition remained unchanged, source and review use backface culling, and six non-marker assets byte-match `0.0.6`.
- Parent fetched/read back exact local/origin/live-remote commit/tree and raw/review subtrees, reproduced marker/source hashes and directory byte counts, inspected `+Z` bright and gameplay-context evidence natively, and ran independent strict Blender validation to `VALIDATE_OK release=0.0.7 assets=7 release_files=17 smoke=1`; coder gate `k72.39` closed PASS.
- Independent QA child `83aca3b9-054f-40a1-ade8-6f72ff526abe` independently parsed actual GLB accessors and Blender source, reproduced all `168/168` outward faces and `504/504` positive face/NORMAL comparisons, ran strict/final validation, fourteen Blender smokes, subprocess/adversarial mutation tests and two-build reproducibility, verified exact inventory/proof/set hashes plus predecessor immutability, and natively judged all cardinal/context evidence. It recorded durable evidence comment `01a06e48-2ff1-7679-a201-399bf1867388` and closed only `k72.40` PASS.
- Final independent auditor `faeaa8c9-bccf-44f0-8d6d-e6ea581e53bb` repeated the critical direct GLB/source math, backface-culling and adversarial checks, official gates, append-only Git/remote history, byte identities, provenance, and native visual review. It recorded durable evidence comment `01a06e51-95d5-7a40-81a6-ae0147f195f7` and closed only `4qk` PASS. Asset `0.0.7` is therefore authorized for renderer pinning; this does not claim runtime `sizeCssPx`, live photosphere visibility, serving, publication, immutable assembly-release approval, or Derrick physical approval. `gy3` remains additionally blocked by Flow obstacle audit `0hu`. No web runtime, serving, publication, or physical-review state changed in this marker chain.

## Source-faithful Flow obstacle gameplay implementation — CODER PASS (2026-09-04)

- Implementation Bead `aerobeat-web-assembly-v5q` landed the approved h51 design in dependency order. Pushed identities: contracts `7755e48f9733e916849896884a574a3b5935620b`; authoring `5da1ed92d39f6bf51e8bb0a7f53c0d818c9dab50` / tree `be43933e13d43730e02284d979eecefbad5f34ab`; content `c6d129e6bf4162ea1a7f1eb9f8e769dd78d50fc1` / tree `d57921ec6f86050fca20b58d4de2c0f1fe12ca8b`; gameplay `af595726e66b6340bf2890d3dd7b14d254f9ab13` / tree `434130b3fc7ef357684d5ad75138d41cd9d68848`; renderer base `e5ccf9b8a8ba35643433c0f4bed3d99b324c4947` plus bounded contact-pulse follow-up `3089f07c79e7ff55ba66813162e25e1ba8b1bcda` / tree `28e88ec65349c5724d1a5796c2daa7499895bac1`; UI `5cf346a4034ef8bdd91bd6762b9e3f691c9e4dbb` plus docs `e7adb0c`; assembly `5176303450a0d23585b4bdfd6483494e496c83e8` / tree `7c0890a98905f66730d83bb4786534002e304dd2`.
- Contracts, authoring, content, gameplay, renderer, UI, and assembly now carry source geometry v1 / Flow chart-package-event v2, strict v2/v3/v4 source normalization, DB 4→5 legacy labeling, atomic malformed rejection, exact geometry/mask validation and cap, measured calibrated nose-only analytical clipping, deterministic aggregate contact consequences, obstacle snapshots/outcomes, `no_obstacles` and `obstacle_visual_only`, one continuous interval wall, contact pulse, Flow v2 UI selection, and aggregate-only public obstacle telemetry. Legacy Flow v1 is not playable and is never silently granted collision.
- Exact offline BeatSaver `3c9d` Hard fixture gate passes through authoring → content → gameplay → assembly projection → renderer: raw SHA-256 `4db5b3393a389c7bcaba6d7a02aec57c10801bcfd74de91523b8e9cdad859b55`; beat `92.5999984741211→92.6624984741211`; `37039.99938964844→37064.99938964844 ms`; geometry `x1,y2,w1,h3`; mask `[1]`; one wall at `(-0.5,3)` with visual dimensions `0.94×2.94×0.15`.
- Mandatory gates passed: contracts and authoring/content/gameplay unit/browser/type/dry-pack suites; gameplay replay hash `sha256:71048f48035b3032e37c917d1d6c4e3eacab716d490f9392c1e489eb1e917ad1`; renderer type/model/browser/context suites and dry-pack; UI product contract/browser behavior apart from its pre-existing Vite allow-list asset noise; assembly `npm test`, `npm run build`, dry-pack, strict malformed direct/iframe integration, mobile menu, audio/preview/library, all eight direct/iframe portrait/landscape DPR matrices, camera compositor, gameplay cursors, and square-grid/continuous-wall pixel matrix. Task 11 source remains `sha256:64fbf421005e021e7ffa6ad88bcd23038f02f6686dc2c83ca38f84c5ead45d4`.
- Renderer ordinary `npm test` remains blocked solely because prohibited external repo `aerobeat-asset-gameplay` is at HEAD `7dec076e243571144b7ead638d3e3f4780bcb9f4` rather than the renderer's older pinned creator commit; all renderer gates not requiring that external sync passed. The asset repo, immutable raw/review/release trees, publication, tags, Releases, serving/Tailscale, and physical-review state were not touched.
- This is coder evidence only. Close `v5q` to unblock independent QA `qfx`; it does not confer QA/audit/release/physical approval.

## Source-faithful Flow obstacle gameplay — INITIAL QA FAIL / REPAIRS READY FOR RE-QA (2026-09-04)

- Independent QA `qfx` recorded FAIL comment `01a06e81-443f-74ce-86eb-6d0f1259b316` after broad cross-repo, exact `3c9d`, collision, direct/iframe, persistence, privacy, and package validation. It reproduced two P0 defects: repeated display frames severed sparse measured collision continuity (`stk`), and present object-valued v2/v3/v4 obstacle containers normalized to zero obstacles rather than rejecting (`ovd`). QA also required explicit persistence-provenance, dual-reimport, staggered/future-partition, and hostile public privacy proof (`dud`). `qfx` remained open and was linked to all three blockers; audit `0hu` remained gated.
- `ovd` closed CODER PASS at pushed authoring commit `e6ac536ee8a151bfe7041cf0341de56f66d79e41`: present non-array obstacle containers now fail atomically with bounded `obstacle_container_invalid`, absent valid optional arrays remain supported, malformed v2 precedence is strict, and direct/memory/Worker/IndexedDB gates prove zero package or DB commit. Full check/unit/security/real/browser/dry-pack gates passed.
- `stk` closed CODER/PARENT PASS at pushed gameplay commit `a429d140491ef73ebaa3856907a40daac280abaf`, tree `dcd1a23e10eb8622da473381a978a7bb0b01eeda`. Identical still-fresh repeated evidence is now a no-op that preserves the prior fresh endpoint and occupied aggregate episode; stale/lost/invalid/conflicting duplicate/rollback/calibration-change and gaps above `150 ms` still sever continuity. Deterministic tests cover one/five repeats around a `25 ms` wall, avoidance, staggered overlap, sustained occupancy, leave/re-entry, one combo reset, zero score delta, unchanged note truth, and non-penalizing uncertainty. Coder and parent gameplay suites passed.
- `dud` closed CODER/PARENT PASS across pushed authoring commit `06b024f4848af6377e83598fe54126252ff9aa6a`, tree `e0957b19883918876deafccdbde4ceae91199dff`, and assembly commit `a1eabb31e1da71f20dd1011ebfa69c4df275ed49`, tree `23d74dd82c5cc3c14443672b11f43e310f927c08`. Persistence now derives `source_geometry_v1` only from structurally proven Flow v2 source geometry; both exact reimport-required reasons use the stale-library clear/no-loop path; new deterministic assembly gates cover staggered overlap, paused future-variant obstacle partitioning, and hostile direct snapshot/event/genuine cross-origin iframe attempts. Public truth remains capped aggregate counts/accessibility only and excludes geometry, masks, lane coordinates, nose/tracking/calibration/frame/episode IDs, and outcome arrays.
- Parent verification passed authoring `npm test`, browser Worker/IndexedDB on localhost and non-loopback, and dry-pack (`18` files, `52,988` packed bytes, `232,996` unpacked bytes, SHA-1 `ea652cded561d65d722a57ba1b28e1e9001d83f4`); assembly focused outcome/library/privacy gates, full `npm test`, and production build passed with only existing PlayCanvas browser-externalization/chunk warnings. The hostile privacy script's interrupted draft omitted one `evaluate()` call terminator; diagnosis and the verified minimal syntax repair are preserved in `.plans/debug/2026-09-04-dud-flow-obstacle-migration-privacy.md`.
- All three blockers are closed, Beads has no dependency cycles, and `qfx` is unblocked for independent re-QA. This repair evidence does not confer QA/audit/release/serving/physical approval. Immutable raw `0.0.35–0.0.38`, gameplay assets `0.0.1–0.0.7`, publication, tags, Releases, and Tailscale state remain untouched.

## Source-faithful Flow obstacle gameplay — INDEPENDENT QA PASS / AUDIT READY (2026-09-04)

- Independent QA child `a4a5923b-a2d0-4966-a40c-998ef48325d5` revalidated the repaired pushed candidate, added exact PASS evidence to `qfx`, closed only `qfx` with reason `QA PASS`, and pushed Dolt. It edited no source, plan, asset, release, serving, publication, or physical-review state.
- Original `stk` reproduction now produces `contact` with and without repeated display frames while every invalid/stale/gap/calibration/conflicting-duplicate boundary still severs continuity; original `ovd` v2/v3/v4 probes now reject with `obstacle_container_invalid`. Persistence provenance, both reimport reasons, staggered overlap, paused future partitioning, and hostile direct snapshot/event/genuine cross-origin iframe privacy gates independently pass.
- Exact offline `3c9d` remains beat `92.5999984741211→92.6624984741211`, `37039.99938964844→37064.99938964844 ms`, geometry `{x:1,y:2,width:1,height:3}`, mask `[1]`, and one continuous renderer wall `0.94×2.94×0.15`; focused scoring/accessibility/renderer/mobile/browser regressions pass. All eight affected repositories were independently verified clean, `git diff --check` clean, and `HEAD == origin/main`.
- Audit Bead `0hu` is now unblocked. QA PASS does not confer final audit, runtime visual integration, immutable successor release, secure-serving, or physical approval.
