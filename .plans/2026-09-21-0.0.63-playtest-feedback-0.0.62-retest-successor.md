# 0.0.63 — Playtest feedback (0.0.62) + equipment interactivity successor

> **STATUS (2026-09-21): ACTIVE — GATE 1 SIGNED OFF (Derrick confirmed D1–D6; D7 no decision). Coding authorized: L-A (xc01+ixpj diagnosis→fix) and L-B (l7bk seam fix) launched in parallel. Predecessor: `2026-09-19-0.0.62-playtest-feedback-0.0.61-retest-successor.md` (COMPLETE — 0.0.62 shipped raw `0f8b3b2`, served on 5173 + tailnet, beads 0hou/r2lb/5y0q/xshm closed).**

## Intake (Derrick, 2026-09-21 playtest of 0.0.62)

> "Playtested both flow and boxing, I can confirm the models are load in place of the markers."

### I-1 — BUG: gameplay pauses when markers move outside the calibrated area
> "Whenever the markers move outside of the original calibrated area, the game pauses. That should not be the case, as our last update was to make lost tracking *not* pause the gameplay."

### I-2 — Behavior: markers must keep working outside the calibrated grid
> "We should also allow the markers to continue working when they move outside of the grid. Because during large flow swings we end up 'losing tracking' because your markers move outside of the calibrated grid, even though tracking has not been lost."

**W0 grounding (2026-09-21, parent):** cursor AND equipment records are both zeroed by the same gate — `tracking.gameplayPaused === true || safety.freshCalibrationRequired === true` (`gameplay-cursor-records.js:27`, `gameplay-equipment-records.js:62`, `index.js:1044`). Setters live in `aerobeat-web-input/src/body-grid-service.js` + `aerobeat-web-gameplay/src/session-coordinator.js`. One diagnosis covers I-1 + I-2.

**W0 COMPLETE (2026-09-21, parent-verified):**
- **I-2 root cause confirmed:** out-of-grid → `inGrid=false` → anchor `valid:false` + `x/y=null` → that role's cursor/equipment record omitted (`gameplay-cursor-records.js:38`, `gameplay-equipment-records.js:73`) — "losing tracking" with NO pause flag involved. Fix (input repo): anchor `valid`/`x`/`y` keyed to `signalValid` (= `scoringValid && confidence≥0.5`, the exact app staging gate) so off-grid anchors keep staging with finite raw coords; `cell`/`subcell`/anchorHistory stay gated on `inGrid` (scoring semantics unchanged — an off-grid point can never be in a cell); boxing collider already range-guards `0≤x,y≤1` (`boxing-collider-collision.js:200`) so no hit-path change.
- **I-1 root cause:** served 0.0.62 bundle IS the 0.0.60-F4 input (proof pin `fd00f14` = HEAD; `enterTrackingFreeze` + `anchorsFrozen` present in the served `assets/index.js`) — the diagnosis child's "stale pre-F4 build" hypothesis **rejected** on that evidence. New oracle `aerobeat-web-input/scripts/validate-midrun-confidence-freeze.js` (parent re-ran: PASS) proves a calibrated mid-run sustained confidence drop (≥3 fails, 750 ms) **FREEZES**: `anchorsFrozen=true`, `gameplayPaused=false`, `freshCalibrationRequired=false`, readiness `countdown`, calibrationId+bounds preserved — and the coordinator's `enforceSafety` (`session-coordinator.js:610,628-637`) stays `safetyReady`, so no `paused_tracking`. The ONLY in-play paths to a true pause (camera preview + "Recalibrate to continue") are: **source-identity change while calibrated** (`body-grid-service.js:479-488`: `sourceChangeId` / pose `sourceId` / `mirrored` / **aspect** drift → `trackingPaused=true` + `invalidateCalibration("source_changed")`), uncalibrated loss, manual reset, destroy. Open question for next playtest: confirm with Derrick what his pause looked like (camera preview = source-change path; dimmed frozen markers = F4 freeze, not a pause) before coding any source-stability change.

### I-3 — Saber glow reads disconnected from the blade (screenshot 1)
**W0 grounding (2026-09-21, parent):** the GLB is geometrically CONTINUOUS — `HILT_Y1 = BLADE_Y0 = 0.18` (`build-flow-saber-v2.py`). The seam is the r2b gain-1.0 additive glow capsule, which starts at **y=0.15 — inside the hilt** (hilt ends 0.18) — rendered as a bright white-wash band over bright Aero that visually severs dark hilt from blue blade. The original r2 spec was "blade section + tip halo only, hilt excluded" — the 0.15 start already violated it. Fix candidates: (a) glow start → `BLADE_Y0` (0.18); (b) gain falloff profile near the hilt end; (c) both. (a) recommended — one constant, restores the original spec. NOTE: any pixel change re-anchors the saber locked-look oracle (signed-off look changes with this fix — Derrick's screenshot is the new baseline).

### I-4 — Feature: Test-mode equipment config (foundation feature)
> "We need an easier way for me to configure these assets. Lets think through how we can expose the markers and the equipped models so they appear in the Test mode, that way we can introduce new options to modify the transform (scale and rotation) of the equipped models to the hands. The flow and boxing would have unique saved configs we can load the defaults in the build, modify, and save the config yaml so I can pass a finalized config to you to use as the new defaults for the lightsaber and gloves."

Design + implement: Test-mode surface showing markers + equipped models; per-mode (flow/boxing) transform config (scale, rotation relative to hand); load defaults from the build; modify + save YAML; pipeline to bake a finalized YAML into build defaults. **Foundation for I-7 and I-8.**

### I-5 — Saber cut aftermath: slice position + half separation
> "The sword cutting animation sort of works today, but when a beat is cut, it doesn't necessarily happen from the spot the sword cut, its always at the same halfway point, and the parts that corpse that appears may not line up with how rotation or seperation should work (the two corpse pieces should fall to the left and right farther, that way they don't rotate into each other and clip the two halves of the corpse.)"

Implement: (a) slice plane at the blade's actual cut position (not the fixed midpoint); (b) wider lateral separation so the two tumbling halves don't clip each other.

### I-6 — Design-first: velocity-based boxing corpse variations
> "The boxing corpse animation isn't bad, I'm curious to see if playtesters like the smack animations or if they are too similar (there's no amount of randomization / velocity thats taken into account). Perhaps we could create a few variations based on the incoming velocity of the boxing gloves and load the proper predefined animation for the corpse path."

Design a small set of predefined corpse-path variations keyed to incoming-velocity buckets; A/B with playtesters. **Design artifact first — implementation waits on the playtest signal.**

### I-7 — Feature: glove rotation states from motion + upcoming beat
> "My mind wants the glove to rotate according to the direction I'm moving in. So if we are moving from down to up, the gloves rotate into an uppercut position. If we are moving towards a straight punch, even though we are technically moving up in camera space, in the reality of the athlete's real-world space, their wrist is moving forward towards the camera, so if the upcoming beat in boxing is a straight punch, then instead of rotating towards the uppercut rotation while moving up, we move into the straight punch rotation values. Then for guards, we rotate the gloves into a guard rotation, and when we detect we are moving to a horizontal punch, we rotate the glove to match that horizontal punch via its velocity. We would need to talk through this a bit, but we would expose these locked rotations to a config and allow us to configure and save it in our Test mode like we do for our other transform values."

Key insight (Derrick): **camera-space up ≠ real-world motion** — select the rotation from (motion) × (upcoming beat type). Rotation states: straight / uppercut / hook (horizontal) / guard. Locked rotations exposed to the I-4 config. **Depends on I-4.**

### I-8 — Feature: saber rotation by grid position (swing feel)
> "I think our sword for flow could do something similar, but its rotations could be based on where we are in the grid, we would rotate the blade to be down, left, right, up depending on how close we are to the edge of the grid. If your near the center of the grid, that could be another rotation as well. That way as you move your wrists it feels like your 'swinging' the sword instead of it feeling like a static attachment to your markers."

Design + implement: grid-zone → blade-orientation map (4 edges + center); interplay with the current direction-based rotation (replace vs blend); locked orientations exposed to the I-4 config. **Depends on I-4.**

### Note (no bead) — external art-asset swap path
Derrick: may create the art assets with a specialized model and swap them for the test versions. The GLBs here are **parametric Blender build scripts** (not AI-generated) — the swap contract for external assets is: same per-asset frame convention (documented in each build-script header), same material-slot names (`mat/glove_body`, `mat/saber_hilt`/`mat/saber_blade`), extents inside the detection box (what-you-see-is-what-hits). A swap then flows through the existing sync/pipeline (inventory sha, proof, re-pins, gates). File a bead when assets arrive.

## GATE 1 — SIGNED OFF (Derrick, 2026-09-21 — all confirmed; coding authorized)

- **D1 (I-1/I-2): CONFIRMED.** (a) marker outside calibrated area → no pause, no fresh-calibration flag, tracking continues; (b) wrist outside grid → marker + equipment keep staging, no dim/omit; (c) existing lost-tracking path (pose genuinely lost: marker gone from camera / confidence collapse) unchanged — it already does not pause.
- **D2 (I-4): CONFIRMED.** Test mode shows markers + equipped models; per-mode (flow/boxing) YAML: per-hand scale + rotation; build loads defaults; **export button downloads a `.yaml`**; finalize = Derrick returns the file → baked as new build defaults (source file in repo, re-pinned through gates).
- **D3 (I-7): CONFIRMED.** States: straight / uppercut / hook-L / hook-R / guard; "upcoming beat" = next note within a **~500 ms window**; transition **~100 ms lerp**. **Addendum (Derrick):** all rotation angles/values exposed to the YAML config for tuning, **ease type exposed too** (not just duration).
- **D4 (I-8): CONFIRMED.** Zones: 4 edges + center (center = neutral); zone map **replaces** the current direction-based rotation with a **distance-blend between adjacent zones** (swing feel, no steps). **Addendum (Derrick):** easing/timing values + per-zone orientations exposed in Test mode via YAML.
- **D5 (I-5): CONFIRMED.** Slice plane at **blade∩note crossing at cut time** (not fixed midpoint); halves separate **~+0.3 WU outward each** (tunable constant).
- **D6 (I-3): CONFIRMED.** Fix (a): glow capsule start **0.15 → 0.18** (BLADE_Y0), gain stays 1.0. **Direction note (Derrick):** iterations expected — the glow should sit around the **blade mesh** with the proper mesh/material/shader; a mesh-conforming glow may supersede the primitive capsule after iteration (file a bead if sweep/playtest shows it).
- **D7:** no decision needed — art-swap contract documented in the Note above.

## Lanes (post-GATE-1 sign-off)

| Lane | Beads | Notes |
|---|---|---|
| L-A tracking | `xc01` (pause bug) + `ixpj` (out-of-grid markers) | one diagnosis, two behaviors; state-machine fix in input/gameplay deps + oracles (fail-before/pass-after) |
| L-B saber seam | `l7bk` (glow/hilt seam) | small: one constant (or falloff), re-sweep, re-anchor saber locked-look oracle |
| L-C config | `376l` (config system) → `2m10` (glove rotations) → `6ax2` (saber grid rotations) | sequential: foundation first, then the two rotation features |
| L-D aftermath | `cv0o` (cut position + half separation) | scene-model aftermath tuning + pixel oracle |
| L-E corpse variations | `5y6i` (velocity-based variations) | design artifact only in this cycle |

Standard tail: gates (all repos) → ABCCBA → exactly-one build 0.0.63 → immutable audit → serve switch → hand playtest link.

## L-C design (W0, 2026-09-21 parent) — Test-mode equipment config + rotations

Recon of the staging path: equipment records are `{role, x, y, mode, dimmed?, direction?}` (assembly `gameplay-equipment-records.js` → renderer `stageGameplayEquipment`, strict key validation, `direction` = JUDGE-space in-plane vector consumed by `stageSaber` only; boxing gloves stage unrotated). Design:

- **I-8 (saber zones, `6ax2`) = assembly-only.** The zone map replaces the motion-derived `direction` as the saber's in-plane orientation source: per-wrist grid position → 4-edge + center zone → zone orientation vector, distance-blended between adjacent zones (no hard steps); center = neutral (keeps the motion-derived direction). No renderer change — `direction` already carries in-plane orientation.
- **I-7 (glove rotations, `2m10`) = assembly + renderer.** Add OPTIONAL per-record `rotationZDeg` (default 0) + optional `scale` (default 1) to the equipment record contract (renderer `stageGameplayEquipment` validation + `stageGlove` Z-rotation + per-hand scale; JSDoc + contracts note). The assembly computes the per-frame value: rotation state from (real-space motion) × (upcoming beat = next note within ~500 ms window) → straight / uppercut / hook-L / hook-R / guard locked angles from config, ~100 ms lerp, ease TYPE from config.
- **I-4 foundation (`376l`)**: new assembly module `src/equipment-config.js` — per-mode (flow/boxing) YAML schema: per-hand `{scale, rotationZDeg}` base transform; glove `{states: {straight, uppercut, hookL, hookR, guard: {rotationZDeg}}, ease: {type, durationMs}, upcomingBeatWindowMs}`; saber `{zones: {edgeTop, edgeBottom, edgeLeft, edgeRight, center: {rotationDeg}}, ease: {type, durationMs}, blendRadius}`. Build loads defaults (source file); Test mode (purpose `visual_test`) shows markers + equipped models — the current `visual_test` suppression in both record builders is lifted for the Test-mode surface; UI panel shows current values + **export button downloads the `.yaml`** (D2 confirmed); finalize = Derrick returns a file → `scripts/bake-equipment-config.js` bakes it as the new build defaults (re-pin gates). All locked values (angles, ease types, timings) are in the YAML per D3/D4 addenda.
- Child sequence (small tasks): C1 config module + schema + export/import oracle (assembly) → C2 renderer `rotationZDeg`/`scale` + record contract + Test-mode record emission (renderer) → C3 glove rotation states (`2m10`, assembly) → C4 saber zone map (`6ax2`, assembly).

## Log

- 2026-09-21: W0 intake complete (Derrick playtest, 7 items + 2 screenshots); parent grounding: pause/marker-loss share the `freshCalibrationRequired`/`gameplayPaused` gate (setters in body-grid-service + session-coordinator); saber seam = glow capsule start 0.15 inside the hilt (GLB is continuous at 0.18). GATE 1 discussion items D1–D7 drafted. 8 beads created (open): `xc01` `ixpj` `l7bk` `376l` `cv0o` `5y6i` `2m10` `6ax2` (ids in the lanes table). **LANDED for chip fleet convergence** (Derrick: land before coding): plan + beads committed/pushed, memory + handoff written, no coding started, 0.0.62 still served on 5173 + tailnet (DSH restart kills the 5173 serve — restart recipe in the 0.0.62 handoff). Resume: GATE 1 discussion with Derrick (D1–D7 sign-off) → lanes L-A…L-E → standard tail (gates → ABCCBA → exactly-one 0.0.63 → audit → serve switch).

- 2026-09-21: **W0 complete.** L-A diagnosis child mapped every setter/consumer of the pause gate (read-only; `xc01` in_progress); parent verified topology + rejected the child's stale-build root cause (proof pin = F4 HEAD); freeze-vs-pause diagnostic added as input oracle (PASS, parent re-ran); I-2 fix landed in `aerobeat-web-input` `891ebba` (valid-by-signal-validity, scoring untouched, oracles updated; parent verified diff + consumer safety: boxing collider range-guards 0≤x,y≤1). L-B child landed renderer `2d5583b` (glow 0.15→0.18, end 0.78 preserved, gain 1.0; renderer suites PASS; `l7bk` closed by child, sweep + oracle re-anchor completed by parent in assembly). Housekeeping: closed stale `mk1a` (0.0.62 sweep harness shipped).
- 2026-09-21: **L-A + L-B assembly-side complete.** Gates require all 10 dependency worktrees clean (renderer evidence dirs + input diagnostic committed: renderer `6daa9a0`, input `04c9d4b`). Combined re-pin (renderer `6daa9a0…`/`3d911bd8…` + input) → sync verify + env-assembly gate + full `test:unit` all PASS. D6 sweep (144 cases) in `.plans/evidence/2026-09-21-0.0.63-saber-d6/`: saber diffPx/box shifted as expected (glow shorter), glove byte-identical; parent eyeballed the sheet + old-vs-new crop — **seam fixed** (bright band off the hilt gone; glow starts clean at blade base). Saber locked-look oracle re-anchored to D6 measured values (box [55,58]/[58,54]/[59,59], diffPx, edge) — PASS both embeddings (dim drop 75, per-hand Δ45). Pre-existing stale test `validate-eight-way-flow-direction.js` (failing on input main since 0.0.61 L-F2 `97e928a` detector swap; bisection-verified STALE-TEST, primitives correct) re-choreographed by child → input `db31439`, full input suite PASS (parent re-ran). Assembly re-pinned to input `db31439` (`490a7964…`). `l7bk` + `ixpj` closed after gates; `xc01` held open pending Derrick's I-1 pause-appearance answer (source-change vs freeze perception).
- 2026-09-21: **L-C design recorded** (see "L-C design" section): I-8 zone map = assembly-only via existing `direction`; I-7 glove rotations = new optional per-record `rotationZDeg`/`scale` (renderer apply + contract); I-4 foundation = `src/equipment-config.js` YAML schema + Test-mode record emission + export/bake. Child sequence C1→C2→C3→C4.
- 2026-09-21: **L-D (cv0o) complete with real-pixel proof.** Renderer `43e0644` + `e0a4cae` (D5 pixel evidence): optional `sliceT` (0..1, 0.5 = legacy midpoint; clip plane offset along the glyph long axis via GLSL variant, midpoint byte-for-byte backward compat) + `aftermathSliceWiderSeparationWU: .46` (was .16; +0.30 WU per-half D5 target; legacy/non-slice entries keep .16). Assembly `7dbd95c`: `computeSliceT` re-derives the blade centerline from `saberWristHistory` + shared `saberGeometry`/`saberDirectionFromWristHistory` (what-you-see-is-what-hits), projects onto the arrow tail→head axis (orb: blade direction), clamps [0.15, 0.85], OMITS the field when no fresh wrist sample (no silent degradation); Test-mode entries omit `sliceT`. Parent re-ran both D5 oracles (PASS) + eyeballed the side-by-side (legacy narrow gap | mid 0.5 wider | off 0.25 off-center cut + mass shift; diff 950px). C1 (`376l` foundation) landed `5e8281b` (config module + strict custom YAML parser, no new dep; parent-verified round-trip/merge/rejection). L-E design doc `51d7b01` (+ `cd2cc10` units fix) — 9-row velocity matrix, open questions relayed to Derrick. C2 running (transform plumbing + Test-mode emission).
- 2026-09-21: **GATE 1 SIGNED OFF** (Derrick: D1–D6 confirmed, D7 N/A). Addenda recorded: D3 rotation values + ease type to YAML; D4 easing/timing + orientations to YAML; D6 direction note (glow should conform to blade mesh; iteration expected). Housekeeping: closed stale `mk1a` (0.0.62 sweep harness shipped). Launched in parallel (independent repos): L-A child = W0 diagnosis of the `freshCalibrationRequired`/`gameplayPaused` gate (read-only, claims `xc01`); L-B child = renderer glow-start fix 0.15→0.18 (claims `l7bk`). Next: verify both → assembly re-pin to new renderer commit → gates → sweep → re-anchor saber locked-look oracle → L-A fix child.
