# 0.0.69 — Neutral-center Flow blend and latest equipment defaults

> **STATUS (2026-09-24): ACTIVE — Derrick approved the square-radius neutral fade, per-hand-base center neutral, continuous smoothstep response with no dead zone, and canonical v4 migration. Raw 0.0.68 remains immutable and served until one audited successor passes every gate.**

## Goal

Bake the latest downloaded equipment configuration as the canonical defaults and replace center-direction instability with a continuous quaternion field that is exactly neutral at the center, smoothly increases edge influence with square radius, continuously blends adjacent edge targets, saturates orientation outside the square, and never alters truthful unbounded equipment position.

## References and decisions

- Owning repo: `/home/derrick/.dsh/projects/aerobeat/aerobeat-web-assembly`
- Downloaded source: `/home/derrick/Downloads/aerobeat-equipment-config.yaml`
- Accepted predecessor: immutable raw `0.0.68`, tree `e5b93744ec11531321c10fcced9839920d396d02`, served by `aerobeat-raw-0068.service`.
- Latest YAML is strict v3. Compared with current baked defaults, Boxing is unchanged; Flow changes both per-hand base rotations to `(0,-90,0)` and edge locals to top `z=45`, bottom `z=-45`, left `y=-45`, right `y=145`.
- Approved blend: square-radius neutral fade.
- Approved center: per-hand base rotation only; spatial edge adjustment is identity at exact center.
- Approved response: `smoothstep(0,1,r)` with no dead zone, where `r = clamp(2 * max(abs(x-.5), abs(y-.5)), 0, 1)` after orientation-only clamping.
- Approved identity: canonical config v4. Strict v3 input migrates deterministically to v4 with the same visible fields; runtime/export/defaults are v4 only. This prevents identical config identity bytes from labeling different resolver semantics.
- Position stays exact and finite outside `[0,1]`; only orientation input/radius saturates.

## Beads

- Umbrella: `aerobeat-web-assembly-gbvd`
- Latest defaults: `aerobeat-web-assembly-s978`
- Neutral-center blend: `aerobeat-web-assembly-v8jp`
- QA/audit/release: `aerobeat-web-assembly-9y1q`
- Deferred unrelated far-plane edge: `aerobeat-web-assembly-yygh` (not part of this release unless Derrick changes scope)

## Roles

- Orchestrator: Cookie.
- Coder, QA, and auditor roles will be recorded here with verified child IDs as they launch.

## Implementation tasks

1. Add strict canonical equipment config v4 and deterministic complete v3→v4 migration; reject mixed/partial records and authored center fields.
2. Bake the latest downloaded YAML values as v4 defaults and preserve the exact downloaded v3 file as a migration fixture.
3. Replace exact-center history/bootstrap semantics with an identity spatial target and square-radius smoothstep edge influence.
4. Continue using the perimeter field to blend complete heading-plus-local-XYZ quaternions between adjacent edges; slerp identity→perimeter target using radial influence before temporal easing.
5. Preserve unbounded finite anchors, orientation-only clamping, shared renderer/gameplay pose identity, pause-safe authoring, scoring reseed, privacy, lifecycle, and Play parity.
6. Update the Test panel/reset/export/import behavior and config/scoring identity to canonical v4.
7. Run dense pure paths through center/axes/corners/outside at 30/60/120 cadence, direct and genuine cross-origin mouse matrices, focused/full browser/pixel/privacy gates, independent QA and audit, headed target evidence, and one immutable successor build/audit/serve.

## Required validation

- Exact center resolves the identity spatial adjustment, so final orientation is only the configured per-hand base.
- Infinitesimal direction changes near center produce vanishing orientation deltas rather than full edge-target snaps.
- Center→edge and center→corner paths are continuous and monotonic under shortest-path quaternion distance; no long-path flips.
- Adjacent perimeter edge/corner blending remains continuous; outside points preserve position and share the corresponding saturated boundary orientation.
- The latest v3 fixture migrates exactly to baked v4 defaults; runtime/export/reset are strict v4 and authored center remains absent.
- V3 and v4 config identities are distinct and score/config locking remains truthful.
- Paused and playing Flow/Boxing edits, overlap queue, shared frozen poses, production Test judgement, privacy, reconnect, and Play parity remain green.

## Release gates

1. Focused config/field tests and browser authoring gates pass.
2. Full assembly and affected package gates pass.
3. Independent QA and audit return GO or GO WITH FOLLOW-UPS with no likely-risk finding.
4. Headed RTX 3080 evidence passes.
5. Exactly one immutable successor is built, independently audited, committed/pushed, served, and completed Beads are closed.

## Findings policy

Likely normal-use defects block and must be fixed. Stale documentation is filed and completed in this non-expedited plan. Unlikely edge cases are filed as linked Beads and reported with likelihood, outcome, and recommendation; they do not block unless Derrick expands scope.
