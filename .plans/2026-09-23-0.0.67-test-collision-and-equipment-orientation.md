# 0.0.67 — Visual Test collisions and equipment orientation

> **STATUS (2026-09-23): ACTIVE — Derrick approved full autonomous execution. Raw 0.0.66 remains immutable and served until one audited successor passes every gate.**

## Goal

Make Visual Test capable of exercising real Flow/Boxing collision, judgement, score, combo, and hit-feedback behavior with mouse-driven equipment; correct the Flow saber glow; and make static plus animated equipment orientation tunable across independent X/Y/Z axes with improved default scales.

## Beads

- Umbrella: `aerobeat-web-assembly-odii`
- Real Test collision/scoring: `aerobeat-web-assembly-h9an`
- Saber glow and XYZ orientation: `aerobeat-web-assembly-hjxn`
- Config schema and scale defaults: `aerobeat-web-assembly-xmho`
- Discovered visual/hit geometry parity defect: `aerobeat-web-assembly-dlg6`
- Cross-package pose/Test-evidence integration: `aerobeat-web-assembly-zan3`

## Direct playtest intake

Derrick supplied screenshot SHA-256 `fe8218ad4dac6d8df54e8cf42ec80887d2c4fe736bb67170f816070fde7619e4` and requested:

1. With automatic GREAT/MISS disabled and mouse control active, equipment/beat contact must run normal production collision and hit behavior so hit visuals can be evaluated.
2. Flow saber glow must align with the blade rather than appearing attached beside it.
3. Flow rotation authoring must support combinations across independent X/Y/Z axes.
4. Default saber scale becomes `2` instead of `1`.
5. Boxing automatic/state rotation must support combinations across independent X/Y/Z axes.
6. Default boxing glove scale becomes `0.75` instead of `1`.

## Confirmed decisions

- Test collision behavior uses **full gameplay judgement and score**, not renderer-only feedback. Valid mouse-driven Flow/Boxing collisions create the same hit, score, combo, and aftermath truth as Play. Normal production miss behavior follows when no valid collision occurs. Test remains unranked and must not write durable leaderboard/progression state.
- Rotation authoring uses **independent X/Y/Z degree controls**, allowing any combination to be tuned directly.

## Scope and invariants

- Version target: one immutable audited raw `0.0.67` after diagnosis, coder/QA/audit, full browser gates, and target-hardware evidence.
- No `ui_fidelity` declaration; behavioral browser and pixel evidence are authoritative.
- Raw 0.0.66 and all accepted predecessors remain byte-immutable and served until successor GO.
- Mouse control remains Test-only and does not acquire camera/CV. Its synthetic wrists may enter gameplay only through an explicit Visual Test collision seam.
- Visual Test scoring is real session truth by Derrick's decision: snapshots/events may contain ordinary judgement/score fields, but private authoring controls, pointer coordinates, equipment configuration, and camera data remain absent.
- Visual Test remains unranked and non-persistent; no leaderboard, library package, setup, storage, bridge-private-state, or production Play truth is mutated by authoring controls.
- Automatic GREAT/MISS enabled preserves the deterministic synthetic presentation and does not double-score. Mouse collision scoring is active only when automatic feedback is disabled and a mouse hand is selected.
- Collision must reuse the exact Flow saber capsule / Boxing swept-contact, timing, direction, chord, miss, score, combo, and aftermath logic rather than reimplementing an assembly approximation.
- Paused Test never advances timeline or collision truth; pointer/config edits still render immediately.
- Rotation schema migration is strict and deterministic. Existing scalar Z-only YAML may be accepted only through an explicit compatibility migration if required; newly exported YAML uses one canonical XYZ schema.
- Base orientation, animated orientation, and authored scale are hit-bearing: Flow/Boxing visuals and deterministic analytic collision geometry must derive from one documented resolved pose. PlayCanvas/browser physics bodies are forbidden as scoring authority.
- Scale defaults apply to both hands: Flow `2`; Boxing `0.75`.

## Diagnosis before code

1. Reproduce and trace why Visual Test bypasses `commitInput()`, evidence capture, production collider evaluation, miss finalization, and scoring.
2. Define the narrow synthetic input/evidence seam that lets mouse wrists use the real gameplay coordinator while retaining Test lease/calibration behavior and preventing automatic-feedback double outcomes.
3. Reproduce the screenshot glow misalignment under default and scaled sabers; inspect GLB pivot/local axis, primitive cylinder axis, scale-dependent midpoint, transform order, and both GLB/fallback paths.
4. Trace base equipment rotation, Flow direction/zone orientation, and Boxing state easing through config → record → renderer. Establish the correct local/world Euler composition and collision impact.
5. Preserve debug reports under `.plans/debug/` before source edits.

Diagnosis completed in:

- `.plans/debug/2026-09-23-0.0.67-test-production-collision.md`
- `.plans/debug/2026-09-23-0.0.67-saber-glow-transform.md`
- `.plans/debug/2026-09-23-0.0.67-equipment-pose-authority.md`

The diagnosis found two additional likely-risk defects that this approved slice must correct: Flow zone/base/scale visuals already diverge from collision geometry, and both Flow/Boxing transition trackers are cadence-dependent. Derrick confirmed authored visual and collision geometry must match.

## Proposed architecture

### Real Visual Test collision

- Build a deterministic, production-shaped synthetic input/evidence frame from the retained Test wrists, timeline, and active Test session identity.
- Add an explicit gameplay-owned Visual Test interaction mode rather than broadening all `visual_test` sessions implicitly.
- When automatic feedback is off and mouse hand is selected, commit the synthetic frame and run the same Flow/Boxing live judging path used by Play.
- Keep standard judgement/score/combo/miss snapshots and renderer aftermath. Remove renderer-synthetic feedback for those events so one beat has one outcome authority.
- Reset collision histories on hand change, pointer invalidation, seek, pause/resume, fresh Test, hidden/terminal, disconnect, or source identity change to prevent teleports from becoming swept hits.

### Multi-axis orientation

- Replace scalar `rotationZDeg` base transforms with canonical `rotationEulerDeg: {x,y,z}` per hand.
- Flow keeps its existing 2D gameplay direction/zone heading for saber collision orientation and gains per-zone local XYZ visual orientation offsets so direction remains collision-authoritative while model pose can be tuned.
- Boxing states become per-state XYZ Euler targets, eased independently using the existing state tracker timing/curve and composed with per-hand base XYZ orientation.
- Renderer applies one documented quaternion composition order to GLB and fallback models; equipment records carry finite bounded XYZ values.
- Add one pure resolved-pose contract shared by gameplay and renderer. Gameplay uses deterministic transformed analytic colliders—not engine physics: a scaled 3D saber capsule from canonical local endpoints/radius and a scaled 3D glove OBB from canonical half-extents, tested against beat target volumes at the judge plane. Existing timing, direction continuity, chord, miss, and score rules remain authoritative.
- Lock canonical equipment config identity into score partition identity because authored scale/pose now changes scoring geometry.
- Track animated poses as fixed-endpoint shortest-path quaternion slerps so dense and sparse frame cadences resolve identically.

### Saber glow

- Make the glow share the blade's exact root transform, local axis, scaled blade midpoint, and effective orientation. Verify GLB and primitive fallback separately at scales `1` and `2`, multiple directions, and combined XYZ rotations.

## Active implementation roles

- Contracts pose/math coder — `8dcfcfcb-b7a0-4bb9-94b0-9e8a9c3b23c7` (verified running): strict quaternion/resolved-pose/geometry contract and tests.
- Assembly config/tracker coder — `3891c083-3720-4055-92f2-64b6d801a10e` (verified running): canonical v2 schema, migration, XYZ controls, defaults, and cadence-independent trackers.
- Renderer equipment-root coder — `7d2fef17-ef4a-4a65-bee0-3be36e6fb9c6` (verified running): shared pose roots, GLB axis correction, fallback geometry, and glow alignment.
- Gameplay Test-authority coder — `bfe5ec70-de0d-42e6-ab55-a0187f1ae4b2` (verified running): exact optional interaction authority, production evaluator reuse, future-only activation, and epoch/history safety.

### Contracts pose/math coder result

- `@aerobeat/web-contracts` pose commit `af4150a10e51e08419701646b654bbc6ad03db5d` plus current follow-up `51c2b42805f5aa008386dc8bc779cfad8542af34` are pushed/clean and independently reverified. The follow-up admits truthful `play | visual_test` judgement purpose without changing the pose API.
- Canonical private resolved pose is exact `{role,mode,anchor:{x,y,z},scale,orientation:{x,y,z,w},geometryIdentity,configIdentity}`. The package owns strict/frozen validation, quaternion composition/slerp, fixed endpoints, canonical config-identity input, and transformed +X saber capsule / glove OBB geometry.

### Renderer equipment-root coder result

- `@aerobeat/web-renderer` implementation `94a691ac82f3dbf1b9204ae4b8d1d29b8943bdce` plus current provenance pin `6043cc09be39e72dab2cb6409288f07662f4f39d` are pushed/clean. Full `npm test` and `npm run test:browser` pass; parent reverified the focused pose-root oracle. Renderer and assembly now converge on contracts `51c2b42805f5aa008386dc8bc779cfad8542af34`.
- Renderer accepts only the canonical shared pose record. One positive-scale root per wrist owns judge→presentation anchor, uniform scale, and final quaternion; GLB source-axis correction, native-cylinder correction, hilt/blade/glow geometry, glove offset, and role mirroring are child-local. GLB/fallback pool transitions remain bounded.
- Isolated Chromium masks prove blade/glow overlap `1.0` at scales `1`, `2`, `.75`, and combined XYZ pose. Parent rerun generated temporary oracle evidence and removed only those exact untracked outputs afterward; repository remains clean/upstream.

### Gameplay Test-authority coder result

- `@aerobeat/web-gameplay` commit `6f70bafcfad0c58da40de5b130db706ab74de213` is pushed/clean and independently reverified with `npm test` and `npm run test:browser`.
- Exact optional per-frame authority admits strict synthetic evidence only for playing Flow/Boxing Visual Test, truthfully emits Test judgements and local-only/unranked score, seeds histories on monotonic epochs, excludes event centers at/before activation, and deactivates across discontinuities. Omission preserves legacy zero-outcome Test behavior.
- Judgement contract seam is closed by contracts commit `51c2b42805f5aa008386dc8bc779cfad8542af34`; strict v2 validation admits exactly `play | visual_test`.

### Assembly config/tracker coder result

- `aerobeat-web-assembly-xmho` source slice implemented: complete canonical `aerobeat/equipment_config` v2, boundary-only wholly legacy YAML migration, mixed legacy/v2 rejection, deterministic v2 export/bake/reset, XYZ authoring controls, Flow `2` / Boxing `0.75` defaults, scale/angle normalization, and fixed-endpoint shortest-path quaternion trackers.
- Flow tracker ownership now converts input Y-down to authored/judge Y-up, uses the gameplay timestamp domain for wrist history, and resets transitions on Visual Test seek.
- Focused pure/static gates pass. Browser/full npm gates are currently blocked before app boot by concurrent dirty/pin drift in companion `@aerobeat/web-contracts` and renderer gameplay source checkouts; rerun after companion lanes commit and assembly pins converge.
- Deliberate integration seam: existing renderer equipment records still receive a temporary Z projection because this slice was explicitly barred from changing renderer record shape; companion contracts/renderer lanes must replace that seam with the canonical resolved XYZ pose.

## Verification

### Pure and package tests

- Visual Test interaction off/default preserves current behavior.
- Automatic feedback off + mouse hand runs exact production Flow/Boxing hit, miss, direction, chord, score, combo, and aftermath boundaries.
- Automatic feedback on never double-judges/scores.
- Pause, seek, hand switch, and pointer teleport clear swept-contact history.
- Play collision/scoring remains byte-identical.
- Config schema/default/export/bake/migration tests cover XYZ bounds and scales `2` / `0.75`.
- Quaternion/Euler composition tests cover combined axes and shortest-path easing.

### Browser/pixel tests

- Direct and genuine cross-origin Test sessions drive real mouse-controlled Flow and Boxing hits with public judgement/score truth but no private authoring leakage or storage persistence.
- Wrong timing/direction/contact misses fail exactly as Play.
- Paused Test does not score; resume does not create a teleport hit.
- Flow glow overlaps the blade at default/moved cameras, scales `1` and `2`, multiple directions, both hands, GLB/fallback, and combined XYZ rotations; use framebuffer overlap/centroid/extent metrics.
- Boxing and saber model silhouettes visibly change for X, Y, Z, and combined-axis controls; reset/export/reload remain deterministic.
- Full direct/iframe responsive matrix and zero unexpected console/page errors.

### Release gates

1. Independent QA and pre-release audit.
2. Full assembly/gameplay/renderer unit suites and complete browser/pixel suites.
3. Headed-X11 RTX 3080 source-bound ABCCBA evidence.
4. Exactly one immutable raw 0.0.67 build and independent raw audit.
5. Commit/push accepted raw, switch serving, verify local/tailnet proofVersion, close/push Beads, finalize plan.

## Completion criteria

- All six requested changes pass real-browser and pixel evidence.
- Full Test scoring behavior matches production Flow/Boxing logic without double outcomes or persistent/ranked leakage.
- Glow alignment and XYZ orientation are visually proven across required states.
- No likely-risk Play, scoring, privacy, renderer, lifecycle, schema, or performance regression remains.
- One audited immutable 0.0.67 is committed, pushed, served locally/tailnet, and all linked Beads are closed.
