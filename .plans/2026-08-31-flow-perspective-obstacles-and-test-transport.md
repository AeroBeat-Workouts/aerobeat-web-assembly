# AeroBeat Flow Perspective, Obstacles, and Test Transport

**Date:** 2026-08-31
**Status:** Approved — execution authorized
**Owner:** `aerobeat-web-assembly`
**Umbrella Bead:** `aerobeat-web-assembly-5gf`
**Release target:** next deterministic web assembly release after `0.0.26`
**Physical boundary:** Derrick will test the resulting release; automated evidence must not claim a physical pass.

---

## Goal

Repair missing Flow obstacles, replace the occlusion-prone stationary Flow presentation with an experimental 2.5D perspective approach that preserves the exact 4×3 screen-space hit grid, make Flow arrows/outline crisp at supported DPRs, and add a Visual Test Play/Pause + scrub timeline with live forward/backward rendering and a right-side `mm:ss` timecode.

Keep the implementation fundamentally 2D in WebGL2 and preserve a clean normalized depth/travel seam so a later approved plan can replace projection with full 3D if physical testing justifies it.

## User-approved direction

Derrick reported on the same Easy DDR song used for prior Flow testing:

- Flow obstacles do not appear.
- Stationary same-cell cues obscure one another and make future beats hard to read.
- Flow arrows and their white outline are visibly low resolution while the timing ring is crisp.
- Visual Test needs Play/Pause and a bottom seek timeline; scrubbing must animate cues forward and backward, with `00:00`-style timecode at the right.

Derrick approved:

- explicit obstacle repair;
- 2.5D perspective sprites first, reserving full 3D for a later decision;
- higher-resolution icon rendering;
- a Visual Test transport with deterministic live scrubbing.

## Current diagnosis

### Missing obstacles

`session-render-projection.js` currently recognizes notes, guards, squats, and weaves, then treats every other authored type as a punch. The hypothesis is confirmed: the minimal authored Flow fixture `{type:"obstacle",start:2,end:4,cells:[1,2,5,6]}` projects as `{kind:"punch",family:"straight",cell:null,cells:[]}` and therefore produces no target rectangle. Source/runtime contracts retain exact obstacle `start`, `end`, and occupied `cells`; the repair must also preserve its active interval rather than emitting a one-frame hatch.

### Same-cell occlusion

Flow cues currently remain fixed at their destination cell. Time is represented by fade and a contracting procedural ring. Same-cell targets therefore share one rectangle, and current same-layer ordering can allow a later cue to cover a nearer cue. Merely reversing ordering is insufficient; cues need distinct projected positions/scales by approach depth.

### Low-resolution arrows

Canonical SVG masks are rasterized into an atlas with a default 64 px cell and an 8% inset, leaving roughly 54 px of glyph detail. The white outline magnifies the same mask. The timing ring is not DOM; it is procedurally generated by the WebGL fragment shader at the DPR-aware canvas resolution.

### Scrubbing seam

The audio service already owns pause/seek/duration, and the gameplay coordinator already supports explicit paused-clock synchronization. Visual Test is unscored and derives synthetic presentation from current timeline position, making it the correct bounded surface for backward/forward inspection. Scored Play must not gain arbitrary rewind semantics in this plan.

---

## Product and rendering model

### 2.5D Flow approach

- Each pending Flow cue has a normalized linear approach progress derived only from authoritative event time and approach lead.
- At progress 0, the cue is small and centered at a bounded vanishing point.
- At progress 1, the cue lands exactly on its existing 4×3 cell rectangle.
- Position and physical size interpolate deterministically; no scoring or source placement changes.
- Far cues draw first and near cues draw last, independent of source-array ordering.
- The Flow approach horizon and assembly publication horizon must agree; cues published beyond the motion lead may not pile up at one exact vanishing-point rectangle.
- Same-cell cues at different times therefore occupy distinct positions/scales throughout the visible approach horizon until arrival.
- The procedural approach ring remains centered on the final cell and contracts to the hit instant, retaining the exact 2D timing affordance.
- Flow obstacles use the same approach-depth model and become translucent projected planes over their exact occupied cells; duration/extent must remain truthful.
- Flow feedback remains attached to the final hit cell with the existing exact 350 ms lifetime.
- Boxing Lanes and Boxing Grid presentation remain unchanged.

### Crisp icon path

- Rasterize canonical SVG masks at a bounded DPR-aware size sufficient for the largest supported Flow cue/outline.
- Minimum accepted atlas cell target is 256 px unless measured browser evidence proves a smaller dynamic bound is equivalent.
- Preserve alpha-only semantic recoloring, white outline behavior, cancellation, context restoration, and truthful degradation.
- Validate visible edge quality at portrait/landscape DPR1/3, including enlarged near-hit arrows.

### Visual Test transport

- Visible only while a downloaded package is in Visual Test or paused Visual Test state.
- Bottom safe-area-aware bar: Play/Pause button, native accessible range input, right-side current `mm:ss` timecode.
- Duration comes from authoritative audio status; seek bounds clamp to that duration.
- Beginning a drag freezes Visual Test audio, records whether it was playing, and keeps the visual frame live.
- Input updates are generation-bound and animation-frame-coalesced: audio seeks, paused gameplay timeline synchronizes, and the renderer immediately projects the selected time.
- Backward scrubbing must remove future synthetic feedback/cues and reconstruct only the selected timeline state.
- Releasing remains paused unless an explicit approved interaction rule and test preserve prior playing state; default for this plan is remain paused for inspection.
- Scored Play does not expose the scrubber and retains all judgement/score invariants.
- Host/iframe public privacy remains bounded; no audio bytes, raw events, or service objects cross UI intents.

---

## Task 0 — Confirm obstacle failure and lock contracts

**Status:** In progress
**Bead:** `aerobeat-web-assembly-5gf.1`
**Owner:** assembly diagnostic coder → QA

### Work

- Reproduce the selected Easy Flow obstacle path through authored package → selected events → session projection → renderer plan.
- Record exact authored obstacle shape, cells, duration, and current disappearing branch.
- Add failing focused tests before changing projection.
- Define normalized Flow depth/travel and Visual Test transport intent/snapshot contracts without changing scoring truth.

### Acceptance

- Minimal fixture proves the current obstacle disappears and distinguishes projection failure from renderer failure.
- Test names exact obstacle cells/duration and expected draw commands.
- No destructive package migration or content regeneration is introduced.

## Task 1 — Build perspective Flow and crisp icon rendering

**Status:** In progress
**Bead:** `aerobeat-web-renderer-der`
**Owner:** `aerobeat-web-renderer` coder → independent QA → auditor

### Work

- Add deterministic 2.5D Flow position/scale/depth ordering.
- Keep rings at exact destination cells and retain fixed 4×3 endpoints.
- Render Flow obstacle approach planes with truthful occupied cells and duration inputs.
- Increase/adapt atlas rasterization resolution and retain alpha-mask lifecycle safety.
- Keep both Boxing presentations byte/command compatible unless a shared renderer contract requires an intentional audited update.

### Acceptance

- Equal timeline deltas produce equal perspective progress.
- Progress 0/1 maps exactly from vanishing point to current destination rectangle.
- Three same-cell cues are separately visible and deterministically far-to-near ordered.
- Obstacles visibly approach and occupy the correct endpoint cells for their truthful interval.
- Directional/directionless arrows and white outlines are crisp at DPR1/3 portrait/landscape.
- Procedural rings remain crisp, destination-centered, and timing-correct.
- Boxing Lanes/Grid regressions remain green.
- Check, unit, browser pixels, pack, independent QA/audit, commit, and push pass.

## Task 2 — Add Visual Test transport presenter

**Status:** In progress
**Bead:** `aerobeat-web-ui-ytx`
**Owner:** `aerobeat-web-ui` coder → independent QA → auditor

### Work

- Add a compact accessible Visual Test transport Web Component.
- Consume only bounded immutable state: visibility, playing/paused, current milliseconds, duration milliseconds.
- Emit scalar Play/Pause and seek intents; range interaction remains keyboard/touch accessible.
- Render bottom safe-area-aware layout with right-side `mm:ss` timecode.

### Acceptance

- Hidden outside Visual Test.
- Touch targets, labels, focus, keyboard range behavior, portrait/landscape layout, reduced motion, reconnect listener exactness, and scalar privacy pass.
- No service/media/package/event records cross the presenter boundary.
- Check, unit, browser, pack, independent QA/audit, commit, and push pass.

## Task 3 — Integrate obstacle projection and live scrubbing

**Status:** Pending
**Bead:** `aerobeat-web-assembly-5gf.2`
**External prerequisites:** `aerobeat-web-renderer-der`, `aerobeat-web-ui-ytx`
**Owner:** `aerobeat-web-assembly` coder → independent QA → auditor

### Work

- Explicitly project Flow obstacles rather than falling through to Boxing punches.
- Supply exact approach/depth/obstacle interval data to renderer frames.
- Wire Visual Test transport to generation-bound pause/seek/synchronize/render behavior.
- Coalesce drag updates without hiding the live frame.
- Preserve Test audio-only/unscored/camera-free semantics and all Play scoring/privacy/lifecycle behavior.
- Keep current Flow orientation and all five gameplay variants unchanged.

### Acceptance

- Actual selected Easy Flow chart displays obstacles.
- Forward and backward scrubs live-render exact events at beginning/middle/end and across same-cell sequences.
- Backward seek removes future synthetic outcomes and never retains future judgement visuals.
- Timecode and seek thumb match authoritative audio clock/duration.
- Test remains unscored and Play remains unscrubbable.
- Direct/iframe portrait/landscape DPR1/3 pass with no console errors.
- Existing `dev:tailscale` server remains running and updates through the normal current server workflow.
- Check, unit, browser, live selected-chart QA, audit, commit, and push pass.

## Task 4 — Deterministic release and physical handoff

**Status:** Pending
**QA/release Bead:** `aerobeat-web-assembly-5gf.3`
**Final audit Bead:** `aerobeat-web-assembly-5gf.4`
**Owner:** assembly release coder → independent QA → final auditor

### Work

- Patch release after `0.0.26` using the established deterministic workflow.
- Compare two raw releases and two dry packs.
- Update README, active plan, Beads, release proof, and a focused physical checklist.
- Keep existing secure server running for Derrick.

### Acceptance

- Exact selected-chart obstacle proof passes.
- Pixel/browser evidence proves perspective separation, endpoint truth, obstacle planes, crisp icons/rings, and transport behavior.
- All relevant repos are clean/upstream.
- Release and dry pack reproduce byte-for-byte.
- Physical checklist remains Pending until Derrick tests; automated evidence claims no phone pass.
- All intentional changes are committed and pushed.

---

## Required regression matrix

- Correct Flow top-left orientation remains externally bound to the existing exact 3C9D proof.
- Flow, Boxing Lanes, Boxing Grid and separate Boxing Conversion labels remain exact.
- Boxing lane 180+180 ms shared band, cue semantics, and 350 ms feedback remain unchanged.
- Flow/Grid 4×3 endpoint geometry remains physically square and aspect-correct.
- Stale package preservation/reimport/list/export/delete behavior remains unchanged.
- Start/Test/menu/restart, leases, privacy, Preview, cadence, countdown, compositor, cursors, context loss, direct/iframe boundaries, and lifecycle teardown remain green.
- No production gameplay/conversion winner is selected.

## Execution record

- Diagnostic coder `6a041b51-91f5-4f73-bc0f-0128f5122fe1` is tracing the selected Flow obstacle through authoring, projection, and renderer contracts for Task 0.
- Renderer coder `9501dd69-a354-4a85-9ade-612ad23d47b7` is implementing Task 1 under `aerobeat-web-renderer-der`.
- UI coder `4868399b-1e95-42b2-a6e8-7edff7c5a12c` is implementing Task 2 under `aerobeat-web-ui-ytx`.

## Approval record

Derrick approved the complete direction in chat on 2026-08-31: restore obstacles, implement the 2.5D perspective approach, improve arrow resolution, add Play/Pause and live scrubbing for testing, and reserve full 3D for a later decision if needed. Execution is authorized without another plan-shape approval.
