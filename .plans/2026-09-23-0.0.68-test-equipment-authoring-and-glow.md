# 0.0.68 — Visual Test equipment authoring and saber glow

> **STATUS (2026-09-23): ACTIVE — Derrick reported release 0.0.67 playtest regressions and requested fixes. Diagnose before editing. Raw 0.0.67 remains immutable and served until one audited successor passes every gate.**

## Goal

Make Visual Test a stable equipment-orientation workbench: equipment edits never alter pause/play state, Flow and Boxing rotation previews remain live while paused, mouse-driven markers/equipment remain visible and positioned beyond grid bounds, Flow heading changes continuously without a center snap, and the aligned saber halo reads visually as glow rather than an opaque white cylinder.

## Beads

- Umbrella: `aerobeat-web-assembly-gxrm`
- Saber glow appearance: `aerobeat-web-assembly-15p5`
- Pause-preserving equipment edits/paused preview: `aerobeat-web-assembly-unoj`
- Continuous off-grid position/orientation: `aerobeat-web-assembly-gl68`
- Gameplay finite off-grid evidence: `aerobeat-web-assembly-xlma`
- Config v3 / continuous square-radial Flow field: `aerobeat-web-assembly-4ijp`
- Cross-package assembly integration: `aerobeat-web-assembly-lk00`
- Blocking config-identity v3 correction: `aerobeat-web-assembly-dtti`
- Stale direction-era cleanup: `aerobeat-web-assembly-a0qd`
- Unlikely beyond-far-plane projection edge: `aerobeat-web-assembly-yygh` (non-blocking follow-up)
- Existing overlapping-edit follow-up: `aerobeat-web-assembly-1n48` (fixed by pause-authoring commit; closure awaits QA)

## Active diagnosis roles

- Saber glow appearance — `8fbd4983-b595-4fdf-80c5-e1823a10ae27` (diagnosis complete).
- Pause/edit coupling and paused pose staging — `a6e58fda-0358-4c6e-a27f-3fa7069eaa55` (diagnosis complete).
- Off-grid disappearance and center snap — `f21214e5-f398-4ad2-88f1-93f2281e2606` (diagnosis complete).
- Renderer soft halo coder — `4e2af1d0-864f-48f3-9d25-d77642b9f4e2` (verified running).
- Assembly paused-authoring coder — `62bf789e-6128-4558-a5da-23d6d0e1920b` (complete; pushed `bc4d519`).
- Gameplay off-grid evidence coder — `19d783f1-2a8a-47aa-9886-687c606aa8b6` (complete; pushed `e83cf131`).
- Config v3 / continuous Flow field coder — `70cc5cb0-4c51-4fdc-9752-ca3f97a732e6` (complete; pushed `4d38f21`).
- Cross-package assembly integrator — `98ac9925-4b51-4a41-aca0-c89f73dfa672` (complete; pushed `5d8177e`, `1239205`).
- Independent QA — initial `fa359e03-0bb6-401d-8bc9-7d8f64523bda` failed without a result after launching the complete shell matrix; repositories remained clean. Bounded fallback `eb60833e-7829-4cdc-9e14-04bb0d01f029` returned **GO**, no likely-risk findings: all eight requested pure/browser/pixel gates passed, including pause invariants, overlap queue, cross-origin eight-side/corner identity, graded halo negative control, and Play/Test off-grid geometry parity.
- Independent source auditor — initial `2a19e900-ff50-4216-abe7-fb9d27669b51` failed without a result and made no changes. Bounded fallback `25d6de5d-5247-49d5-9bfb-953c92bd41c6` returned **NO-GO** on one likely-risk semantic identity defect: strict v3 configs were hashed under a hard-coded v2 contracts envelope. Contracts fix role `c95c8e20-0621-44a1-aa70-56805e54cbb9` pushed `a1dce4ef13554d8367d71d55f03a776b8e52c893`, preserving exact v2 bytes while adding truthful/separate v3 authority. Assembly correction/cleanup role `8ca345f4-76ed-4d2e-b802-6450284c3fc8` pushed `ad7700d598b20dc5d8baaea4bab4df6bb98d77f2`: canonical v3 envelope, exact v2/v3 SHA separation/rejection, live score lock coverage, contracts pin convergence, and stale direction-era API/name removal. Auditor re-review returned **GO** with no likely risks, stale paths, or new unlikely bugs. Unlikely beyond-far-plane pointer `yygh` remains filed for later.

## User evidence

- Screenshot SHA-256: `4205397d43e7b391e0fbbdbb01d5cb063f76dbb4af55c79ec41216d4907b8d27`, 825×226. The halo is aligned around the saber but reads as a white/opaque cylinder, not emitted light.
- Equipment rotation edits in Test unpause gameplay. Editing authoring state must not control playback state.
- The desired authoring loop is: pause on targets, move the mouse hand, adjust Flow/Boxing rotation values, inspect the equipment pose immediately, and repeat without resuming playback.
- Leaving the gameplay grid makes marker/equipment disappear or snap to a default location. Position and visibility must not be grid-gated in Flow or Boxing. Orientation may saturate at its authored maximum while position remains truthful.
- Approaching the interior/center causes Flow heading to snap. Remove the semantic center orientation state if diagnosis confirms it is the discontinuity; derive a continuous edge/corner heading everywhere with stable behavior at the exact center.
- Derrick's downloaded candidate config is `/home/derrick/Downloads/aerobeat-equipment-config.yaml`: v2, Flow scale 2, edge headings top 90/bottom -90/left -180/right 0, linear 100 ms and blend radius .15; Boxing per-hand Z 45, uppercut Y 45, hooks X 45, guard Z -70, easeOut 100 ms, upcoming window 500 ms.
- **Decision:** Derrick chose **Bake as new defaults**. Migrate these values into canonical reset/default config after the center-field schema decision, and preserve the exact file as a deterministic fixture/reference.

## Invariants

- Raw `0.0.67` is immutable; no rebuild or mutation.
- Automatic feedback and real production judgement remain mutually exclusive.
- Renderer and gameplay continue receiving the exact same resolved pose/config identity.
- Scoring remains analytic and deterministic; no PlayCanvas physics authority.
- Pause/play truth is owned only by explicit playback controls/lifecycle, never config-authoring callbacks.
- Pointer/marker position is not clamped to the gameplay grid and does not disappear solely for being outside it.
- Orientation inputs may be bounded/saturated, but heading interpolation is continuous across edge/corner/interior transitions and shortest-path quaternion behavior remains cadence-independent.
- Test remains local-only, unranked, non-persistent, and private-authoring data stays out of public/cross-origin surfaces.

## Diagnosis tasks

### 1. Glow appearance

- Trace root/core/halo geometry, blend mode, depth flags, material opacity/color/gain, tonemapping, and screenshot pixel profile.
- Distinguish geometry alignment from shader/material readability.
- Produce a debug report before implementation with a focused framebuffer oracle that fails the opaque-cylinder look while preserving core containment.

### 2. Pause and paused preview

- Trace native input/change → async config identity commit → Test restart/reseed → audio/playback coordinator → render loop.
- Prove exactly which transition unpauses playback and whether paused frames stop pose tracker updates or equipment staging.
- Add a browser reproduction: pause, move/rotate equipment, edit config, assert song time/playback state unchanged and rendered pose changed for Flow and Boxing.

### 3. Off-grid and center discontinuities

- Trace pointer normalization/validity, grid conversion/clamping, cursor/equipment filtering, Flow zone classification, and renderer staging.
- Reproduce beyond all four edges and near/exact center in Flow and Boxing.
- Define continuous heading behavior at exact center without reintroducing a hidden default snap. Prefer last stable heading at the mathematical singularity if direction is undefined; verify with deterministic dense paths through center and outside bounds.

## Diagnosis results

- Glow (`.plans/debug/2026-09-23-0.0.68-saber-glow-readability.md`): confirmed larger full-gain `ONE,ONE` double-sided cylinder with no attenuation; dark isolated scanline clips to a flat `98,255,255` plateau. Existing masks prove alignment only.
- Pause (`.plans/debug/2026-09-23-0.0.68-paused-equipment-authoring.md`): confirmed config commit calls full `startSession("visual_test")`, which seeks/plays/restarts RAF. Paused one-shot rendering exists, but trackers use frozen gameplay time. Use preserve-state gameplay reseed plus duration-0 paused endpoint poses.
- Off-grid/center (`.plans/debug/2026-09-23-0.0.68-off-grid-continuous-heading.md`): confirmed renderer projection rejection, Test clamp/default, and gameplay `[0,1]` sample rejection; confirmed midpoint disks leave most interior and all corners in center fallback. Preserve finite anchors and replace center semantics with a square-radial adjacent-edge quaternion field; exact center retains last stable target with deterministic role-side bootstrap.
- The direction resolver/config identity semantics will be versioned rather than silently changing v2 behavior. The canonical next schema removes authored `center`; Derrick's downloaded v2 is migrated deterministically and baked as the new defaults.

## Implementation results

- Gameplay finite off-grid evidence commit `e83cf1311c736150ab9753693af86bd93a3cf751` is pushed/clean. Wrists accept finite off-grid x/y while nose/body semantics remain bounded; strict freshness/source/calibration/confidence/identity checks are unchanged. Four-side, non-finite rejection, just-outside Flow capsule/Boxing OBB hit/miss, and Play/Test parity gates pass. Assembly must emit off-grid wrists with null cell/subcell and exact shared pose anchors.
- Renderer soft-halo base `74ed1d6b4b7a76db13453844a515619e602b241b` plus off-grid projection commit `61648c5d0b43574b220bd402d8eff7e3ab0087a6` are pushed/clean. A bounded radial/endpoint ShaderMaterial uses alpha-aware additive composition and one surface contribution while preserving the canonical root/core geometry. Twenty dark/bright × GLB/fallback × cardinal/combined rows prove ordered inner/middle/outer energy, core overlap `.97814–1.0`, bounded clipping/hue, and core readability; the old solid-cylinder negative control fails with `.88307` clipped fraction. Camera-ray projection now returns exact finite normalized points outside `[0,1]` across four sides/corners and moved cameras while retaining viewport/ray/lifecycle rejection.
- Assembly pause-preserving authoring commit `bc4d519e08bef52ce0372714675c28b6b29c72c5` is pushed. Config edits reseed gameplay identity/run truth through the preserve-state configure path without changing transport, timeline, pause reason, audio, RAF, or assembly generation. Paused Flow/Boxing resolve immediate endpoint quaternions. A serialized pending-draft queue with duplicate suppression and lifecycle ownership also fixes `1n48`; focused pure and real Chromium pause/playing/overlap/privacy/reconnect gates pass. Full graph validation awaits final renderer/gameplay pin convergence.
- Assembly config/field commit `4d38f21` is pushed. Runtime/export schema v3 has four edge zones and no center; strict full v2 migration deletes center and the downloaded v2 fixture maps exactly to baked defaults. Pure square-radial APIs blend complete heading+local-XYZ quaternions across adjacent edges, retain last stable exact-center targets with role-side bootstrap, and pass 4,097-sample radial/perimeter/outside, ±180 shortest-path, and 30/60/120 cadence gates.
- Assembly integration commits `5d8177e` and `1239205` are pushed at exact renderer `61648c5d0b43574b220bd402d8eff7e3ab0087a6` / tree `a99d0f28d5fa692104f0b53443eb1f52d3c918f2` and gameplay `e83cf1311c736150ab9753693af86bd93a3cf751` / tree `b29494749ca74c89a4ee5782520ebc704ce2dcf0`. Runtime center/vector compatibility authority is deleted. Test pointer/evidence/poses preserve exact finite off-grid coordinates, null cells/subcells outside the grid, retain last valid position on genuine projection failure, and share one frozen identity-bound pose across gameplay/renderer. Direct and genuine cross-origin eight-side/corner matrices, focused panel, full unit/build/browser suites, privacy/lifecycle, and parent focused rerun pass.
- After the v3 identity correction at `ad7700d5`, the parent reran the uncapped complete `npm run test:browser`; it completed with exit 0 across the full shell/camera/pixel/equipment matrix.
- Corrected-source headed RTX 3080 ABCCBA evidence `.plans/evidence/2026-09-24-0.0.68-pre-release-target-abccba.json` passes source/hardware and hostile-evidence validation: source `44350d994bd9e4960656f066726285b2baa1921797d3a5dc6cd8c45105d3684c` / 235 inputs, staged minimum `59.671 FPS`, B/A `0.9999`, C/B `1.0001`, CV max `14.980`, pose max `113.0 ms`, exact `59..64` projected-target bounds at 6,000 events, and all 14 provenance/hostile plus 12 backend mutations rejected.

## Implementation tasks

1. Implement a convincing aligned saber halo with bounded additive falloff/softness and GLB/fallback parity.
2. Separate equipment config identity/reseed from playback state; render authoring poses while paused for both modes.
3. Preserve truthful off-grid mouse marker/equipment position and visibility; clamp only orientation intent where required.
4. Replace center-zone heading snap with continuous edge/corner heading derivation and stable exact-center handling; migrate/remove center config only if the diagnosis proves it obsolete.
5. Import Derrick's candidate config into deterministic fixtures and decide explicitly whether it becomes the baked default or remains a test preset.
6. Run focused pure/browser/pixel/privacy/lifecycle gates, independent QA, and pre-release audit.
7. Run full package/browser and target-hardware gates, then bump/build/audit exactly one immutable successor only after source acceptance.

## Required validation

- Pixel evidence demonstrates a bright readable core plus softer/lower-opacity halo rather than a solid second cylinder, at representative backgrounds, scale 2, combined XYZ, and GLB/fallback.
- Direct Chromium proves config edits preserve paused state/song time and update visible Flow/Boxing equipment while paused.
- Direct and genuine cross-origin matrices prove off-grid marker/equipment visibility on every side without private-state leakage or persistence.
- Dense pointer paths through center, edges, and corners prove bounded continuous orientation without snapping, long-path quaternion flips, or cadence dependence.
- Existing production Test hit/miss/score/combo, auto-feedback exclusion, Play parity, config identity, and lifecycle reset oracles remain green.
- Independent QA and auditor triage findings using likely-risk/stale-doc/unlikely-bug/accepted rules.

## Release gates

1. Diagnosis reports accepted and linked to source evidence.
2. Focused unit/browser/pixel/privacy tests pass.
3. Independent QA and pre-release audit return GO or GO WITH FOLLOW-UPS with no likely-risk finding.
4. Full assembly/gameplay/renderer gates and headed target-hardware evidence pass.
5. Exactly one immutable successor is built, independently audited, committed/pushed, served locally/tailnet, and scoped Beads are closed.
