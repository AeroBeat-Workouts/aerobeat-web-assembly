# 0.0.66 — Visual Test equipment authoring controls

> **STATUS (2026-09-22): ACTIVE — Derrick directly authorized immediate execution from the served 0.0.65 physical playtest. Raw 0.0.65 remains immutable and served until one audited successor passes every gate.**

## Goal

Make Visual Test a useful equipment-tuning workbench: equipment remains visible while Test transport is paused, automatic synthetic GREAT/MISS feedback can be disabled, and one selected synthetic hand can follow the mouse over the renderer so live grouped equipment values can be tuned visually before deterministic YAML export.

## References

- Predecessor plan: `.plans/2026-09-22-0.0.64-playtest-feedback-0.0.63-regression-successor.md`
- Served predecessor: `release/raw/0.0.65`
- Umbrella Bead: `aerobeat-web-assembly-psa0`
- Pause visibility Bead: `aerobeat-web-assembly-tq6l`
- Feedback/mouse authoring Bead: `aerobeat-web-assembly-wff8`
- Camera-aware mouse projection bug: `aerobeat-web-assembly-9zfe`

## Direct playtest intake

Derrick confirmed equipment is visible in Test and requested:

1. Equipment must remain visible when Visual Test transport is paused.
2. Test must expose a control to disable its automatic alternating GREAT/MISS renderer feedback.
3. Test must expose an Off/Left/Right mouse-hand selector; the selected synthetic wrist follows the mouse position over the renderer as the equipment attachment marker.
4. These controls exist to visually tune live equipment swing/punch configuration and export finalized equipment YAML.

## Declared scope and invariants

- Version target: 0.0.66 after diagnosis, coder/QA/audit, full browser gates, and target-hardware gate.
- No `ui_fidelity` declaration: there is no approved mockup; behavioral browser evidence is the authority.
- Raw 0.0.65 and all earlier accepted releases remain byte-immutable and served until successor GO.
- Rejected raw 0.0.64 remains ignored, byte-immutable, and never served/committed as accepted.
- New Test controls are private authoring state only: no public snapshots, events, iframe payloads, storage, persistence, scoring, judgement records, input/CV mutation, or camera acquisition.
- Defaults preserve current behavior: auto feedback on; mouse-hand off; Test equipment visibility off on a fresh connection/session.
- Paused Test may render equipment and accept authoring pointer/config changes without advancing its timeline or producing gameplay truth. Normal Play session/tracking/menu suppression remains fail-closed.
- Pointer coordinates are finite and clamped to the renderer CSS content box; the last valid position latches while controls are edited, and Off/fresh-Test/hidden/terminal/disconnect/teardown resets private pointer evidence safely.
- Existing free-fly camera controls remain usable when mouse-hand control is off; any conflict while a hand is selected must be explicit and deterministic.

## Diagnosis before code

1. Reproduce paused Test disappearance through the real transport and inspect the exact equipment visibility gate.
2. Trace synthetic alternating feedback from Test purpose through renderer projection; find the narrow private disable seam.
3. Trace renderer pointer/camera event ownership and define a non-leaking Test-only hand-position path.
4. Preserve a debug report before implementation.

## Diagnosis result

- Parent pure reproduction and child `d9b9e163-909a-4704-8912-8d1796206865` proved the paused defect: `pauseVisualTestTransport()` explicitly renders after entering `paused_manual`, but `gameplayEquipmentRecords()` rejects that state and sends `equipment=[]`. Playing Test emits two records; paused Test emits zero. The toggle and renderer are not the cause.
- Child `04f4cbba-74e0-4feb-8ec3-dfe82af76685` proved auto feedback has two independent private seams: target hit/miss in `projectSessionTargets()` and persistent even-index Test hit aftermath in `projectAftermathEntries()`. One shared default-on private state must gate both; Play real truth remains unchanged.
- Test hand input already has the correct private seam: replace fixed `TEST_EQUIPMENT_INPUT` selection with a private per-frame projection that overrides only one deterministic wrist from finite normalized canvas CSS coordinates.
- Renderer owns right-click pointer lock, keyboard/DOM movement, and touch free-fly inputs. Left/Right hand selection will release and suspend those authoring inputs through a private renderer API without resetting camera pose; Off restores them. The linked renderer has no Beads database, so assembly task `wff8` owns the cross-repo change.
- The last valid canvas position will remain latched while Derrick moves into the control panel to tune values. Explicit Off/fresh Test/hidden/terminal/disconnect/teardown resets it; pointerleave alone does not erase the tuning pose.
- Renderer coder `595e8bcb-c704-4305-ae30-9f1437aa9569` failed before edits/results; renderer remained clean at `e8ee29d`. Idempotent retry `d1af012f-ed98-41cc-a167-969b0c960235` landed/pushed renderer `269b192` / tree `41e78f8`: strict private authoring-input gate releases/ignores camera inputs while preserving pose; focused and full renderer suites PASS. Assembly provenance advanced in `646f209` and exact dependency/fingerprint gates PASS.
- Pure assembly coder `eddc2c36-bd31-4972-a2a3-e34bbf346201` landed/pushed `318f0b1`: paused Visual Test-only equipment admission plus default-on shared gates for synthetic target feedback and independent Test aftermath; all three focused pure validators PASS.
- UI/browser coder `aa5f68fa-b5d8-44de-bd8b-44a8e6b6e0b1` landed/pushed `cbc0f7d`: private defaults/resets, accessible controls, normalized/latching selected-hand projection, shared feedback/aftermath gate routing, camera ownership, immediate paused renders, and browser coverage. Focused pure, `npm run check`, equipment browser, and eight-context shell matrix passed; parent repeated focused pure gates. Independent QA `bed7d1ca-32a1-4b91-a334-11a487bb880d` launched and registration verified.
- Pre-release auditor `5996d22c-3f96-45ba-b582-77411b1002bc` found no runtime defect but issued NO-GO on one likely-risk proof gap: focused authoring behavior was direct-only, while genuine cross-origin coverage inventoried controls/snapshot privacy without mutating the new states or observing parent bridge payloads/storage/input/CV. Recovery coder `1200f803-b3e2-43a3-af07-eb3d415def8b` added a canonical iframe row that passes in isolation: 7 child snapshots, 5 parent messages/4 events, zero child+parent storage writes, unchanged input/CV, clamped Left/Right evidence, fresh-session reset, and YAML privacy. Final matrix/commit waits for concurrent renderer provenance to become clean.
- Independent QA `bed7d1ca-32a1-4b91-a334-11a487bb880d` confirmed all requested commands/browser rows pass with zero noise, but issued NO-GO on a likely normal-workflow defect: direct CSS/body-grid normalization does not preserve pointer-to-equipment screen alignment after free-fly camera authoring, even though selecting a hand deliberately retains that pose. QA also requested primed hidden/terminal/disconnect reset rows and rendered pixel alignment proof. Created/claimed linked bug `aerobeat-web-assembly-9zfe`; read-only debugger `0c46df53-6f7a-444d-b517-4c5d3981a158` proved the root cause is a coordinate-space mismatch and recommended a private live-camera `screenToWorld()` ray/equipment-plane query with strict fail-closed bounds/lifecycle behavior. Report: `.plans/debug/2026-09-22-0.0.66-camera-aware-mouse-hand.md`.
- Renderer coder `e57023bd-f081-4b16-8ef5-9e2b4a5956c8` landed/pushed `8b525ec` / tree `bef2597`: private `projectDebugEquipmentAnchor()` with strict lifecycle/geometry rejection and no telemetry mutation. Full renderer suite and a real Chromium default+moved-pose, portrait/landscape, DPR request 1/3, full/non-full viewport oracle PASS with root error under 0.000008 CSS px and framebuffer role-marker centroid under 2 CSS px. Assembly provenance advanced in `f507e66`; exact dependency/fingerprint gates PASS.
- Assembly integration coder `83b69b2f-0901-4629-939d-81b5529a2b16` landed/pushed `5f8241b`: whole-canvas normalization removed; pointer handling uses the renderer query, latches only valid results, preserves prior latch on null, and covers moved-pose preservation plus primed fresh/stop/hidden/completion/disconnect/reconnect resets. Parent repeated focused/static, direct equipment browser, and all eight direct/iframe shell contexts; every gate PASS with zero reported noise.
- Re-auditor `5996d22c-3f96-45ba-b582-77411b1002bc` verdict GO: both prior blockers closed, exact renderer pins/provenance/immutability consistent, no likely-risk defect, stale current documentation, or unlikely bug found.
- Re-QA `bed7d1ca-32a1-4b91-a334-11a487bb880d` verdict GO: renderer projection pixel/root oracle, direct moved-pose equipment authoring, genuine cross-origin privacy, all eight shell contexts, primed lifecycle resets, and static checks PASS with no unexpected browser errors and no remaining likely-risk or credible unlikely follow-up.
- Debug report: `.plans/debug/2026-09-22-0.0.66-visual-test-equipment-authoring.md`.

## Implementation tasks

### A — Paused Test equipment

- Admit `paused_manual` only for `session.purpose === "visual_test"` in equipment presentation.
- Keep menu-open, Play pause, tracking pause/fresh-calibration/countdown-frozen, and invalid-anchor suppression unchanged.
- Render immediately after Test pause/resume and after live config changes.

### B — Auto feedback control

- Add an accessible Test-only checkbox labeled clearly for automatic GREAT/MISS feedback.
- Default on for compatibility; off keeps eligible targets pending/visible without synthetic hit/miss aftermath.
- Route the private option into pure session render projection without altering real Play judgements.

### C — Mouse hand control

- Add accessible Off/Left/Right selection in the equipment authoring panel.
- While Test authoring and equipment preview are active, map renderer pointer position to one selected synthetic wrist; keep the other deterministic wrist unchanged.
- Trigger immediate rendering so position, Flow zone direction/easing, base transforms, and current Boxing state are visible while paused or playing.
- Release/reset private pointer state on mode off, Test end, disconnect, hidden state, and relevant lifecycle resets.

### D — Oracles and browser proof

- Extend pure equipment/projection oracles for paused-Test-only admission and disabled synthetic feedback.
- Extend the equipment browser oracle for labels/defaults, pause persistence, auto feedback on/off, left/right pointer movement, live transform/zone response, and privacy across direct + genuine cross-origin iframe where applicable.
- Confirm camera pointer controls remain unchanged with mouse hand off.

### E — QA, audit, and release

1. Coder implementation with focused gates and pushed commit.
2. Independent QA of direct physical acceptance paths and likely regressions.
3. Full assembly `npm test`, complete browser/pixel suite, linked-input suite, and headed-X11 RTX 3080 source-bound ABCCBA gate.
4. Independent pre-release audit, then bump/build exactly one immutable 0.0.66.
5. Independent raw audit; force-add only accepted 0.0.66, update README/plan, commit/push.
6. Switch managed serving from raw 0.0.65 to accepted raw 0.0.66; verify local/tailnet proofVersion; close/push Beads.

## Completion criteria

- All three requested behaviors pass real-browser evidence.
- No likely-risk Play, privacy, camera, scoring, renderer, or lifecycle regression remains.
- Plan and Beads are current; intended source/release/docs changes are committed and pushed.
- Exactly one audited immutable successor is served locally and over tailnet.
